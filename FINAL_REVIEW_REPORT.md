# 最终方案复核报告

复核对象：`FINAL_IMPLEMENTATION_PLAN.md`  
方案版本：`LIANGJIAN_INDEPENDENT_FUNNEL/1.0.0-draft`  
方案 SHA-256：`E2F85787FCC574299E6862FE766D14A4CA1DEAFF4A6EF8D43F6011292347837F`  
复核日期：2026-08-24

## 结论

**条件通过。** 方案的层级、权限、时序、原子发布、模型隔离、盘中否决、内部模拟、T+1、幂等、恢复和复盘闭环已经定义完整；未发现仍可绕过 G0→A1→A2→A3 血缘产生新买入的设计路径。

本结论不是“完整工作流已验收”。真实探测已证明同花顺不提供 1m/5m，现由 mootdx 承担沪深分钟线；其节点轮换、长历史分页、分钟缺口和跨源价格一致性仍须通过 P0 门禁，未通过时禁止继续构建交易计划链。

## 第一轮复核：架构、权限和时序

已检查：

- 新目录与原项目隔离。
- 每日只有 09:25、15:10 两个逻辑研究时段。
- 三个研究模型共享事实输入但不共享模型结论。
- `A3 ⊆ A2 ⊆ A1 ⊆ G0`。
- 早盘只收紧，收盘才完整生成次日计划。
- 收盘计划必须通过次日早盘复核才能激活买入。
- 09:32 最早发布、09:40 统一截止和过期处理。
- Flash 按模型 lane 隔离，只有否决权。
- RiskGovernor 独立负责数量、风险和强制退出。

本轮修正：

1. 关闭 FAST_TRACK，消除池外旁路。
2. 未完成早盘复核时禁止新买入，只管理已有持仓。
3. 09:25/15:10 只创建 run，数据完整后才一次性冻结快照。
4. Flash 恢复原 `IntradaySignalV1` 动作枚举，否决使用 `llm_veto=true`。
5. Flash 上下文按 lane 隔离，避免跨模型污染。
6. 强制风险退出不等待 Flash。
7. 固定 120m 上午/下午 Bar，禁止跨午休。

复核结果：通过。

## 第二轮复核：故障、恢复和审计

已检查：

- 数据陈旧、Bar 缺口、日线未结算。
- 模型超时、非法 JSON、429/5xx。
- Flash 超时和跨分钟重叠。
- 重复调度、进程崩溃、半写文件。
- 磁盘/SQLite 写失败。
- T+1、负现金、卖空、重复成交和对账异常。
- A3 计划到期后持仓风险规则的持续性。
- 同分钟买卖冲突、公司行动和重启后的过期信号。
- 用户 Markdown 与内部审计数据的分层保留。

本轮修正：

1. `research_run` 使用交易日+时段+模型作为稳定逻辑幂等键。
2. 信号使用状态代际而非分钟作为幂等边界。
3. 新增持久化 `PositionRiskPlan`。
4. 新增冲突动作固定优先级。
5. 停机期间的 BUY/ADD 不补发，确认计时遇数据断点自动重置。
6. Parquet/Markdown 使用临时文件校验后原子重命名。
7. 磁盘或账本无法持久化时禁止任何新模拟动作。

自动结构检查：

```text
Markdown 行数                  603
代码围栏平衡                  通过
重复二级标题                  0
嵌入 API 密钥                 0
旧 minute_bucket 信号键       0
自定义 KEEP 动作枚举          0
跨 lane Flash 混合批次        0
```

复核结果：通过。

## 已确认的保留行为

- 不修改 `liangjian-astock-ai`。
- 不接 GM、券商、掘金模拟盘或真实账户。
- 三个模型分别维护虚拟账户，不进行投票后统一交易。
- Flash 每分钟运行，但用户 Markdown 只保留有效事件。
- 模型输出 JSON，最终由确定性渲染器生成 Markdown。
- A5 只提案，不自动改变参数或提示词。

## 第三轮复核：真实外部能力与分钟数据替换

真实验证确认同花顺历史 K 线只支持日线，因此将分钟责任切换为 mootdx，而不是降低门禁。已复核：

- mootdx 真实 `1m` 与 `5m` 均可取。
- `5m` 分页 16 次取得 12,240 根，满足 `120m MA255` 预热长度。
- 交易时段以 Bar 结束时间表示：`09:31/13:01` 为 1m 首根，`09:35/13:05` 为 5m 首根；午休不误报。
- 节点空响应、连接失败、乱序、重复冲突、非法 OHLC、历史不足全部失败关闭。
- 分钟价固定为不复权；复权继续由同花顺公司行动事件统一推导并版本化。
- 同花顺快照与 mootdx 最新价按盘中严格时效、午休/收盘后合法闭合 Bar 分开校验。
- 北交所 `920002` 真实返回为空，保持 `RESEARCH_ONLY`，不得发布入场计划。
- SQLite 缓存使用唯一 Bar 键、事务写入、幂等重放和冲突整批回滚。

真实总门禁结果：同花顺 PASS、四模型 PASS、mootdx PASS。

仍需后续阶段证明：官方公告真实披露时间线、全市场批量吞吐和额度、三个隔离 Flash lane 在 50 秒截止内的连续稳定性，以及重启/磁盘/缺 Bar 的端到端恢复。

上述后续门禁失败时，正确行为仍是停止并报告缺口，不是降低数据标准、换 GM 或关闭思考模式继续运行。

## 第四轮复核：端到端组合、真实小样本和无人值守调度

复核范围：全市场分页、冻结快照、因子、三模型 lane、计划发布、A4、模拟账本、跨进程恢复和 Windows 计划任务。

本轮发现并修正：

1. 同花顺真实 `last_price` 字段和空分页时间字段不匹配。
2. 31.7 MB 原始分钟/聚合 Bar 被重复发送给模型；改为 raw 快照保留、约 42 KB 模型事实包只注入摘要。
3. 提示词使用 `SHSE.600519`，内部使用 `600519.SH`；现统一 canonical 化。
4. 精确模型名未注入 envelope，造成有效输出误判；现由运行时强制提供。
5. A1 `monitor_pool` 曾可能被当作 A2 上游；现只允许 `active_research_pool` 下传。
6. 三 lane 顺序执行无法满足早盘截止；现真正并行，lane 内仍严格串行。
7. 每分钟新进程会丢失连续确认；现从 SQLite 恢复上一分钟状态。
8. 计划失效后持仓曾可能失去行情范围；现按虚拟持仓继续硬风险监控。
9. Windows 分钟任务最初被注册为单次任务；现为每日 09:25、每分钟、持续 5:45，下一次运行时间已验证。

真实小样本结论：数据和 deepseek A1 链路通过；因缺少提示词要求的政策/行业/板块/资金/新闻事实，ACTIVE 池为空，A2主动 BLOCK，未生成虚拟单。Kimi/GLM 完整 A1 返回上游 5xx，未跨模型代答。该行为符合 fail-closed 合同。

复核结论：运行框架可无人值守执行并生成审计结果；当前业务输出为无单。只有补齐事实源后，才可能进入 A3计划和 A4/模拟成交的真实非空路径。
