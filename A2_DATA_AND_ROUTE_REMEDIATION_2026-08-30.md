# A2 数据与路由根因修复验收记录

日期：2026-08-30
范围：A2 主题情绪、板块梯队、龙头/中军辨识、资金流与结果状态
原则：缺失数据不等于负面事实；可选数据不得一票否决；禁止用成交额冒充资金流。

## 1. 根因结论

2026-08-28 的 A2 空结果同时包含数据接入和决策逻辑问题，核心不是当日市场没有主线：

1. 个股资金流快照未配置，120/120 候选缺失该字段；旧逻辑把它与梯队、龙头结构一起视作必须齐全的关键事实。
2. 同花顺涨停梯队已经存在于冻结快照，但旧逻辑只检查候选个股自身是否涨停，没有把同板块涨停梯队用于中军/容量核心确认。
3. A1 研究池中已有农业、粮食、化工、有色等方向，旧 A2 本地门因事实齐全度计算错误，只把极少数股票送给模型，模型没有机会审阅真实主线。
4. 历史结果和新特征代际混用时，数据库中可能保留旧 `REVIEW_CANDIDATE`，造成前端、运行结果和本地门统计不一致。
5. 数据源失败、成功但空、过期、格式异常此前没有严格区分，容易把“未获取”误解为“观测为零”。

## 2. 已落地的数据架构

### 2.1 点时状态模型

新增统一状态：

- `OBSERVED_VALUE`：成功取得非空事实；
- `OBSERVED_EMPTY`：成功查询且结果确实为空；
- `SOURCE_UNAVAILABLE`：源不可用或未配置；
- `STALE`：缓存过期；
- `MALFORMED`：响应或缓存格式/哈希异常；
- `OUTSIDE_SCOPE`：当前截面不适用。

只有前两种可以作为市场事实；其余状态必须保留为数据缺口，不能填零。

### 2.2 交易日缓存

- 个股资金流和板块资金流均按交易日写入 `fact_store/a2_market`；
- 历史回放只能读取完全匹配交易日且哈希正确的缓存；
- 历史缓存缺失时禁止调用实时接口并改写为历史数据；
- 当前交易日允许采集，并保存失败/部分成功状态，避免无界重复请求。

### 2.3 真实板块资金流

接入东方财富公开行业/概念资金流排行：今日、5 日、10 日三个窗口。由于东方财富与同花顺板块代码体系不同，只允许：

- 同代码精确匹配；或
- 规范化后的板块名称精确匹配。

不做模糊名称猜测。输出保留原始净流入额、净占比、排名、窗口及缓存哈希。跨窗口评分只使用供应商资金流排名的分位数，标记为 `SECTOR` 级供应商衍生证据。

### 2.4 板块健康与梯队

板块健康快照覆盖同花顺行业和概念，组合：

- 成员上涨广度和涨跌平衡；
- 日线/行业历史相对强度与持续性；
- 涨停池、连板梯队、最高板和梯队成员；
- 板块资金流窗口；
- 弱转强仅在历史序列完整时计算，单一当日快照保持 `UNKNOWN`。

个股没有涨停只表示 `OBSERVED_ABSENT`，不再推导为板块没有梯队。板块存在梯队且候选具备相对强度、流动性时，可确认 `CORE_ARMY`，同时保留“板块梯队确认”而非伪造个股板数。

## 3. A2 路由修复

A2 只保留两个顶级路由：

1. `MARKET_CORE`：主题明确、产业链节点明确、业务证据成立，并在广度、成交占比、龙头结构、梯队结构、产业链共振五类市场事实中至少具备两类；
2. `SUPPLY_CHAIN_ALPHA`：稀缺环节、价值链位置、结构化瓶颈评分卡和至少两条可追溯证据满足约束。

资金流是可选增强证据。缺失时：

- 从评分分母中移除并按可用权重归一；
- 路由状态标记 `DEGRADED`；
- 候选仍可进入模型复核；
- 不得以成交额、热度或涨幅代替资金流。

`EMOTION_LEADER` 是 `MARKET_CORE` 内部角色，不再是第三条绕过规则的顶级路由。

## 4. 状态与结果约束

- A2 决策级“关键充分性”按五类市场事实中至少两类计算，不再把资金流列为必需项；
- 全市场来源都缺失时仍 `DATA_GAP/INSUFFICIENT`，不会为了产生结果而放行；
- 数据充分但未达到身份/路线阈值为真实 `LOCAL_MONITOR/HARD_REJECT`，与数据不足严格分开；
- 每次重跑必须按 `run_id + lane + stage` 原子替换特征和决策，禁止读取前一代旧结果补数。

## 5. 验收标准

1. 2026-08-28 粮食、农业、化工夹具能识别板块梯队和非涨停中军；
2. 个股资金流缺失时，其他事实充分的 `MARKET_CORE` 仍可进入模型；
3. 板块资金流存在时进入 A2，明确标注 `source_scope=SECTOR`；
4. 所有资金流源缺失时不造数，输出原因和降级状态；
5. 历史日期不会请求当前实时源；
6. Python 全量测试、Node 测试、类型检查、生产构建全部通过；
7. 虚拟机新一代正式运行中，A2 本地评估数等于 A1 活跃池输入数，并输出非空送模候选或可解释的真实无机会结果；
8. A1、A2、A3 结果和前端进度属于同一 `run_id`。

## 6. 仍需运行态验证的边界

- 2026-08-28 历史板块资金流若此前没有缓存，系统不会用 2026-08-30 数据回填；该回放只使用当日已冻结的同花顺梯队、日线、行业/概念归属和历史序列。
- 当前交易日首次采集东方财富板块资金流后，后续同日运行才可稳定命中缓存。
- 免费供应商接口存在限流和字段变更风险；失败只降低证据丰富度，不再清空整个 A2 池。
- 历史 A2 回放禁止调用当前个股新闻接口，只能使用冻结快照内同日新闻，防止跨日事实污染。
# A1 覆盖目标契约修正（2026-08-30）

全市场点时回放发现，A1 已形成 75 只有效研究候选，但旧实现把
`active_research_target: [100, 250]` 同时当作诊断目标和硬验证门槛，进而以
`A1_ACTIVE_COVERAGE_UNDERFILLED` 阻断 A2。该行为与配置中的“证据不足可低于
下限”及提示词中的“不得强迫单批次凑数”相矛盾。

本轮统一契约如下：

- 研究池目标只记录 `A1_ACTIVE_TARGET_UNDERFILLED` / `A1_CLUE_TARGET_UNDERFILLED`
  诊断，不改变 A1 分类；
- 只要 A1 输出通过结构、事实、月度研究等真实性校验且存在有效候选，就标记为
  `VALIDATED` 并允许 A2 执行；
- 数据质量、月度研究和结构校验仍是硬门槛，不因本修复放宽；
- 不降低目标、不补虚假候选，也不把数据缺失伪装成“市场无机会”。

## 模型容量失败的批次降级

8 月 28 日点时验收中，A2 确定性层已从 75 只 A1 候选中选出 47 只复核候选，
但完整主题批次在 `393216 → 262144 → 131072` 输出预算降档后仍被网关拒绝。
小请求在 131072 tokens 下实测可被同一模型接受，说明这不是 A2 事实为空，也不能
据此清空候选池；它是输入与输出总容量相关的传输边界。

因此 A1/A2 将 `OUTPUT_BUDGET_*` 归入可拆分的传输错误：保持候选、主题和冻结事实
不变，把失败批次按确定性顺序二分后重试，直至成功或单只批次仍失败。只有后者才
以模型容量故障阻断，数据质量与业务分数不会在此过程中被修改。

进一步检查冻结快照确认，原 A2 提示词每个批次都重复携带约 77 万字符的全市场
个股资金流和约 217 万字符的完整板块健康结构，其中板块结构还同时保留
`by_taxonomy` 与 `industry` / `concept` 两份等价数据。这使“按股票拆批”没有真正
缩小输入，是容量失败和性能退化的根因。

修复后的模型投影遵循以下边界：

- 个股资金流、龙虎榜/关注度记录严格过滤到当前批次股票；
- 板块数据保留当前批次股票关联的全部行业/概念，同时保留每类全市场确定性排名
  前 40 个板块作为比较基准；
- 删除传输视图中的重复 `by_taxonomy` 副本和逐日收益原始数组，但保留 1/5 日收益、
  广度、强度、持续性、梯队、涨停成员和数据状态；
- 完整全市场快照及哈希不变，确定性 A2 仍对全市场事实计算，模型投影不改变任何
  候选、分数或来源状态；
- 投影显式记录完整数量、批次关联数量和模型可见数量，供前端与审计复核。

## 可选资金流与主题评分契约统一

首次成功返回的 A2 结果又暴露出一处契约冲突：五项关键市场因子覆盖率均为 100%，
MARKET_CORE 路线覆盖率为 89.3%，但缺失的个股资金流仍被按 0 分计入总权重，且
`A2_CAPITAL_FLOW_UNAVAILABLE` 被状态机当成硬数据缺口。该行为与配置中的
`capital_flow.required_for_focus=false` 以及提示词的“按可用权重归一”相矛盾。

统一后的规则是：

- 资金流源不可用时保留原始分 0 和缺失原因，绝不以成交额替代；
- 从主题总分分母中移除不可用的资金流权重，并记录 available/total weight、覆盖率和
  `normalized_over_available_weight=true`；
- 可选资金流缺失仍显示为降级诊断，但不再单独构成 `DATA_INSUFFICIENT` 或阻断 A3；
- 只有实际可用权重低于 65%、关键市场事实不足或合法路线不成立，才属于真实数据缺口；
- A2 模型直接读取服务端确定性角色、因子、覆盖率与合法路线；它可以基于其它明确反证
  降级候选，但不能重新猜测这些数值，也不能只因可选资金流缺失否决 MARKET_CORE。

## 东财资金流主备域与历史点时恢复

生产网络对 `push2.eastmoney.com` 的 TLS 连接会被中途终止，但同一供应商的
`push2delay.eastmoney.com` 延迟公开域可正常返回完整榜单。采集器因此采用有序主备域，
仍然只接受东财供应商字段，不使用成交额、涨跌幅或关注度冒充资金流。

延迟域返回的 `f124` 供应商时间戳同时解决历史缓存缺失问题：只有各成功窗口的供应商
交易日期与目标回放日期完全相同，才允许补建该日期缓存；任何缺失或日期不一致都返回
`HISTORICAL_*_PROVIDER_DATE_MISMATCH`，不会写入缓存。个股榜单、行业板块和概念板块
执行同一校验，并把 provider trade dates、是否点时验证和是否历史恢复写入哈希保护的
结果文件。

## 因子级数据源优先级（腾讯 / 同花顺 / 通达信）

数据源不再按“一个供应商承包 A2”配置，而是按其真实字段能力拆分：

- 同花顺正式 API 继续作为行业/概念归属、涨停池与连板梯队、龙虎榜、热度和结构事实主源；
- 腾讯 `todayFundFlow` 作为个股当日订单规模资金流主源，读取主力流入/流出、散户流入/流出、超大单和大单净流字段；
- 通达信/mootdx 作为日线、1m/5m 量价和技术确认主源，不把普通成交额或成交量冒充主力资金流；
- 东财仅作为腾讯资金流覆盖不足时的个股资金流后备，以及当前板块资金流来源；部署网络优先访问已验证稳定的延迟域，失败后才尝试普通主域。

腾讯逐股接口采用最多 16 个工作线程，每个线程复用连接；请求前先读取腾讯行情时间戳，
供应商交易日期与目标日期不一致时整批拒绝，防止周末/停盘日把旧数据标成新数据。快照记录
主源、备源的可用性、原因码和当日覆盖率；只有主源不可用才尝试备源，二者都失败时资金流
因子降级但不会清空其他事实充分的 A2 候选。
