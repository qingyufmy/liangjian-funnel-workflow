# Phase 1–2 中间验收报告

日期：2026-08-25  
结论：`PASS_WITH_KNOWN_GAPS`

## 已通过范围

1. 同花顺 9 类市场事实进入统一 `FactEnvelope`，并冻结来源、事件时间、抓取时间、内容哈希和来源健康。
2. 涨停、跌停、炸板、连板天梯与全市场涨跌幅生成确定性 `MARKET_EMOTION_SNAPSHOT`。
3. 龙虎榜和热榜只生成局部拥挤代理，不替代基金持仓、融资或新闻数据。
4. 日线与 5m 闭合 Bar 生成确定性 `KLINE_PATTERNS` 和 `PRICE_LEVELS`。
5. 价位包含触发区、失效位、止损距离、第一阻力、盈亏比、最高追价和来源 Bar 时间；不满足门槛时保留事实并明确判定失败，不调宽参数。
6. 同花顺行业目录保持 `THS` 语义，不写入 `SW_INDUSTRY_MAP`。
7. 同花顺官方尚未提供个股反查行业/概念成员接口，因此 `SECTOR_CYCLE_SNAPSHOT` 失败关闭，`SECTOR_PERMISSIONS` 自动降为 `RESEARCH_ONLY`。

## 真实验收样本

- 全市场：5,559。
- 研究域：5,343。
- 沪深内部模拟域：5,008。
- 同花顺事实：9/9 可用。
- 市场广度：上涨 3,684、下跌 1,777，breadth 0.6746。
- 涨停 47、跌停 3、炸板 19、连板高度 5。
- 确定性市场温度：`STRONG`。
- 样本股票：`300308.SZ`。
- K 线识别：`BEARISH_ENGULFING`，来源为 2026-08-24 闭合日线。
- 系统价位已生成，但止损距离和盈亏比均未通过门槛，因此不得形成可执行新入场计划。

产物：

- `storage/snapshots/snapshot-20260825T120039+0800-2a10458620c6.json`
- 对应事实清单由快照内 `fact_store_relative_path` 定位。

## 验证证据

- 离线测试：204 passed。
- Python compileall：通过。
- 真实研究快照 SHA-256：回读一致。
- 事实清单及侧车 SHA-256：回读一致。
- K 线来源时间不晚于冻结时间：通过。
- 两个 `.env` 密钥未出现在研究快照或事实清单：通过。

## 已知缺口与处理

| 缺口 | 当前行为 |
|---|---|
| 个股所属同花顺指数反查尚未开放 | 不扫描 710 个板块冒充稳定映射；板块权限降为 `RESEARCH_ONLY` |
| 申万行业版本映射 | `SW_INDUSTRY_MAP` 不可用，不用同花顺行业替代 |
| 基金持仓反向索引 | `CROWDING_SNAPSHOT.available=false`，龙虎榜/热榜仅保留为局部组件 |
| 板块指数历史和资金流 | `SECTOR_CYCLE_SNAPSHOT` 不可用 |
| 新闻 | 保持独立缺失，不用热榜补齐 |
| 价位不满足 6% 止损或 2.0 盈亏比 | 保留确定性价位，A3 必须拒绝，不自动放宽 |

## 下一批实施顺序

1. 可验证的申万行业版本文件。
2. 行业利润统计与产业链版本注册表。
3. 补齐上述事实后再次执行三模型 A1→A2→A3 端到端验收。

## 巨潮接入追加验收

- 公开公告元数据已逐候选接入 `DISCLOSURE_EVENTS` / `RISK_EVENTS`。
- 查询失败会淘汰对应候选，确认零公告保持健康空结果。
- HTML 标题清洗后仍标记为不可信外部文本；疑似提示词注入标题在模型投影中替换为阻断标记。
- PDF URL 只允许 `https://static.cninfo.com.cn`；拒绝跨域重定向，限制 20 MiB、200 页和 200,000 字符，429/5xx 有界重试。
- 只对风险、订单/产能和定期财报类候选按需下载，每票最多 3 份；缓存以 URL 键定位并以 PDF SHA-256/侧车复验，损坏缓存自动重取。
- 模型只接收最多 12 条、每条最多 500 字且带页码的确定性证据卡，不接收整篇 PDF。扫描件、加密件和解析失败均显式降级，不伪造“无内容”。
- 真实贵州茅台样本：8 页、4,748 个提取字符、12 条证据，首页渲染核对通过。
- 真实工作流快照 `snapshot-20260825T132053+0800-d1dae1d031a0.json`：亨通光电质押公告 5 页、2,779 字符、12 条页码证据进入 `RISK_EVENTS`，PDF 覆盖率 100%、无源失败。
- 贵州茅台近 30 日真实查询：6 条公告，5 条披露事件、1 条风险事件。

## 国务院政策文件库追加验收

- 官方搜索页真实使用的 `GET https://sousuo.www.gov.cn/search-gov/data` 已按公开 Web 契约接入。
- 查询使用显式 `mintime/maxtime`，并校验 `paramsVO` 日期、页码和页大小回显；错窗失败关闭。
- 只纳入 `gongwen` 与 `bumenfile` 正式文件；`otherfile` 政策解读和 `gongbao` 重复公报排除。
- `code=1001 + data=[] + searchVO/paramsVO=null` 识别为确认零结果，其他业务码视为故障。
- `pubtime=0` 或缺失保持 `None`，不拿抓取时间补造发布时间；无发布时间记录不进入普通事实。
- 最新真实查询得到 3 条正式文件，发布时间覆盖率 100%，政策投影明确禁止直接个股映射和财务传导推断。
- 联合快照 `snapshot-20260825T123725+0800-eff1498fa91e.json` 的事实清单含 12 条事实、11 个来源健康项，哈希及无密钥检查通过。
