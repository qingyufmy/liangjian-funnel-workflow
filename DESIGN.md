# Design System

## Direction

量见运行控制台采用专业、克制、可信的浅色研究终端。首页先回答系统是否正常，再展示研究漏斗、盘中盯盘、数据源和日志。结构参考成熟的运行平台，而不是行情大屏。

## Color

所有颜色通过语义变量使用。主色是低饱和交易所青绿 `#0f766e`，仅用于当前导航、运行中状态、焦点和关键链接。页面背景为带轻微青绿色相的浅灰白，正文使用深墨色。成功、警告、错误和信息状态同时使用图标与文字，不单独依赖颜色。

| Token | Value | Purpose |
| --- | --- | --- |
| `--color-accent` | `#0f766e` | 当前项、运行中、关键交互 |
| `--color-accent-strong` | `#0b5f59` | 悬停与高对比文本 |
| `--color-accent-soft` | `#e4f1ee` | 选中背景与轻量状态面 |
| `--color-canvas` | `#f3f6f5` | 页面画布 |
| `--color-surface` | `#fbfcfc` | 主内容表面 |
| `--color-surface-subtle` | `#f0f4f2` | 工具栏和次级区域 |
| `--color-border` | `#dce4e1` | 分隔线与控件边框 |
| `--color-ink` | `#101a2c` | 标题和正文 |
| `--color-muted` | `#5f6c74` | 辅助信息 |
| `--color-success` | `#18794e` | 成功 |
| `--color-warning` | `#a15c00` | 警告 |
| `--color-danger` | `#b4232c` | 错误 |
| `--color-info` | `#1d5f9a` | 信息与等待 |

## Typography

使用系统无衬线字体栈，中文优先 `"Microsoft YaHei UI"` / `"PingFang SC"`，不加载外部字体。数据、时间和日志使用系统等宽字体与等宽数字。固定字号阶梯：12、13、14、16、20、24 像素；不使用流体大标题。

## Spacing

采用 4 像素基础尺度：4、8、12、16、24、32、48、64。相关信息保持 8–12 像素紧密分组，独立区域使用 24–32 像素分隔。控制台桌面密度适中，移动端通过改变结构而非单纯缩小内容适配。

## Layout

桌面端为 216 像素固定侧栏加自适应主区域。总览页顶部是一条主要运行状态带，中部为宽研究漏斗和窄运行摘要两列，实时日志横跨底部。窄屏侧栏收为顶部导航，双列结构变为单列；数据表允许横向滚动并保持表头清晰。

## Components

- 状态标记采用图标、文字和轻量色彩三重表达。
- 面板只用于真正独立的信息域，内部层级使用分隔线而不是嵌套卡片。
- 按钮、输入框和导航项具有一致的 8 像素圆角、44 像素触达高度和清晰焦点。
- 加载态使用骨架结构；空状态说明为什么为空以及系统接下来会做什么。
- 日志以表格方式展示时间、级别、任务和消息，保留原始语义但自动脱敏。

## Motion

仅对导航、折叠和连接状态使用 150–200ms 状态过渡。遵循 `prefers-reduced-motion`；不使用进场编排、跳动数字或装饰性行情动画。

## Accessibility

正文目标对比度不低于 WCAG AA。所有交互可通过键盘完成，焦点环清晰可见；图标按钮必须有可访问名称；日志和异步状态使用适当的 live region，但避免逐行日志打断屏幕阅读器。
