# 回滚边界

## 当前开发阶段

当前没有生产变更。回滚等于删除隔离 worktree 或丢弃该开发分支；不得对主工作树执行 `reset --hard`，不得覆盖用户未跟踪文件。

## 后续代码回滚原则

- 每阶段独立提交，业务合同、账本迁移、交易规则和 UI 分开审查。
- feature flag 只能回退功能路径，不能假定能回滚已经写入的新 schema 或账本事件。
- 账本迁移只做幂等增量；上线后如产生新事件，禁止恢复旧数据库快照丢弃事件。
- 生产切换、重启和数据库变更均需单独授权，并在生产一致性副本先演练。
