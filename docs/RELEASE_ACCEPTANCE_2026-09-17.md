# 9月17日盘前发布验收

## 已完成

- 06:29执行原有 `deploy.sh` 成功。生产提交 `bd5c941df8ec31657c9e8405961b6540c3c78ea0`，Python重新安装，前端及服务端构建通过，Node调度器启动，旧进程已退出。
- 全量测试1,687项通过；生产实际导入的publication、early_discovery、A5 daily/fact_guard、monitor、lark_notifications、workflow七个模块与源码SHA256一致。
- 9月16日两份A5已在生产复验落库，无新模型调用、无历史交易补发、原事实及响应文件哈希未变。报告状态仍为资料受限。午间ID `4f75282d-6464-5d74-8d3f-6ff6f6740fdc`，盘后ID `8dbf501d-4d2a-5564-a66c-2c42b9c64642`。
- 飞书补发触发权限拒绝，已单独向用户询问；未获确认前不补发报告。此次恢复不影响系统原定的新复盘任务。

## 今日原正式计划

来源 `2026-09-16-close-24e80e034214`，不是新版重跑结果。只读验收工具 `verify_day_plan_readiness.py` 返回READY，15项断言全部通过。

| 项目 | 核验结果 |
| --- | --- |
| A1有效范围 | 1,494只 |
| A2 | 聚焦67、观察200、板块范围外1,227 |
| A3 | 核心研究66，符合发布权限的正式计划62 |
| 正式策略 | 趋势五日线58、520为4 |
| 状态 | 62份均为待盘前确认，未提前激活；有效期9月17日15:00 |
| 集合与权限 | A2属于A1、A3属于A2；研究观察无越权发布；待确认批次唯一 |
| 520 | 四只日线MACD完整，每只具有48根历史十五分钟预热输入 |

这只是静态准备通过，不代表今日分钟行情、模型决策或交易信号已完成验收。

## 新版研究验收仍在执行

systemd任务 `liangjian-release-a2a3-20260917.service`，06:31启动，最长5,400秒，约08:02前退出；早于08:30盘前任务。使用新版代码、原密封A1与当前盘前可用资料，运行前缀 `2026-09-17-release-validation`（最终文件可能带快照哈希后缀）。

本次为研究验证，`publish_plans=False`，不覆盖或提前激活上表62份计划。06:36已经进入板块历史同步阶段，不能标为重跑完成。原自动任务临时每15分钟跟进，任务完成或明确失败后恢复工作日15:35安排。

## 明确保留的缺口

9月16日13:05腾讯一分钟聚合与原生五分钟的持续价格差异尚未取得独立核验，通达信两端查询失败。新代码仍会阻断未解决的实质冲突，不以减少告警代替解决数据问题。分钟端到端耗时、真实盘中通知和持仓风险处理还需自然交易窗口验收。

生产盘剩余约8GB、使用率90%；未删除或覆盖历史证据。后续验收应关注增量空间和长任务耗时。

## 62只标的采集负载补验

06点使用生产采集与发布确认代码，只读请求昨日14:55闭合窗口；将采样相对时钟置于窗口后3秒开始，仅测试网络负载与预算，不运行策略、不写行情、不证明盘中当时可用。

首轮3.10秒、全批28.75秒，未超过30秒预算但余量较小；18只观察稳定，44只仍存在跨周期实质冲突。不能把44只说成今日实时故障，也不能把此测试说成A4信号链全量通过。原冲突保持阻断，没有通过增加容差消除差异。
