# 9月7日 A1–A3 验收阻断：复盘与针对性修复

## 结论及证据边界

隔离运行 `acceptance-20260907-5ffa424` 于18:06:37启动、20:24:03结束，业务状态为阻断；进程正常退出不是业务通过。A1月度方向分析两次语义尝试未通过引用及关联校验，A2、A3没有执行。没有生成或发布生产计划，不得用空集合证明三级包含关系通过。

本轮生产代码为 `5ffa424f56b4697011d49f2f590801bbb9876ea4`。冻结快照为 `snapshot-20260907T200431+0800-9af65094fc91`，哈希 `9af65094fc917a213da118823abe39c57acf9de31464c978bb7a4101970e42ee`。在本地按生产LF换行重建输入，得到与失败审计完全相同的提示词哈希 `b5f8e05e1860572356703c57a86e6e8d22e8be5bd9bac4db4e6cef34c8213032`、输入哈希 `a769aac9650aab112fb093fdbc48744a4875396fd1b2c605da954c22ff8a388c`。没有重新抓取数据。

原程序没有保留两次被拒的结构化响应，只保留最后的字段形状及错误码。因此，不能事后声称已经定位到原响应中某个具体错误字符串，更不能用新响应冒充原响应。本次通过输入复现、代码路径和回归测试定位可证实缺陷，再进行同快照的有限复验。

## 时间及数据

- 数据准备约123分钟：市场资料同步约31分钟，公告目录约68分钟，公告原文及特征准备约22分钟；各阶段由进度时间戳近似计算。
- 月度方向分析约14.25分钟，2次尝试，最后响应形状为15个主题、50个节点、13条行业映射。原始失败不是单纯主题数量不足。
- 公告原文共处理5754份，5634成功、120失败。3907只研究候选的 `MAIN_BUSINESS_EVIDENCE` 全部为可用且证据非空；文档失败数不等于缺证据股票数。该结果只证明这3907只的快照证据覆盖，不代表全市场5567只均合格，也不证明所有股票都采用最新报告。
- 进一步按公告标题核对：3876只存在2026年半年报证据，31只没有该报告期标题，其中11只使用招股书、20只使用较早报告；9只已有 `uses_older_filing=true`。该标记比较的是快照内已知完整报告的发布时间，不是“是否为2026年半年报”，因此不能用标记false推断最新报告期达标。全部3907只有可追溯引用、URL和文本，未发现晚于9月7日的公告发布时间。明细保存于隔离的 `business-coverage-audit.json`。
- 模型实际输入约38.6万字符、估算16.45万输入token；本次不扩大模型重试次数和时限。

## 已确认问题与修改

| 问题 | 可复现证据 | 针对性修改 |
|---|---|---|
| 研究包丢失快照身份与截止时间 | 生产快照同时实现Mapping，包构造先走字典分支，真实属性被丢弃；原包 `snapshot_id=UNKNOWN`、`as_of=null` | 优先读取快照对象的data及身份属性；兼容普通字典与嵌套字典 |
| 失败响应不可复盘 | `_run_stage`两次失败后返回output=null，只保留形状 | 为每次月度发现保存独立、带哈希、去推理字段并脱敏的输入/结构化输出/校验问题；标记不可执行，绝不作为成功输出或可恢复检查点 |
| 重试缺少被拒记录及精确位置 | 第二次调用只有初始输入与汇总错误码，无前次assistant结果；主题关联错误也没有专门修复说明 | 带上前次结构化响应、逐条字段路径、非法引用和合法主题ID，明确它们是数据而不是指令；保持两次上限，重新检查重试输入预算 |
| 出现误导性的二次错误 | 对失败后的空output重新检查月度覆盖，追加主题不足、节点不足、轮动缺失等 | 仅对已验证输出执行后续月度复核；保留最早真实阻断，不增加虚假覆盖缺口 |
| 混合合法/非法主题关联可漏过 | 原校验只要求关联集合与合法主题有交集 | 每个关联ID必须是本响应已有精确ID；合法ID不能掩盖另一个非法ID |
| 资料索引与授权证据容易混淆 | 原包索引154项，其中144项合法，10项属于非授权线索引用 | 最终运行契约明确source_index不是授权表，所有引用均须在原白名单；明确各类字段数组和精确ID，不扩大证据域 |

原失败原因中的主题/节点/映射引用错误与节点主题关联错误均仍然严格拦截。没有按相似名称猜测主题、没有自动补证据、没有降低数量或基本面门槛。

## 实施和验收顺序

1. 保存原结果和冻结快照，按原代码重建输入并校验哈希。
2. 修复身份、诊断、重试和关联校验；运行针对性回归。
3. 同一冻结快照仅重放A1月度发现，仍只用DeepSeek；每个响应保存完整复核材料，不重新采集市场数据。
4. 若月度发现通过，再依据新生成的有效结果评估A1、A2、A3后续验收；月度发现通过不等于全漏斗通过。
5. 本轮默认仅本地代码和隔离验证，不隐含生产部署或计划发布。

## 两次审查

第一轮：从原始失败倒查。确认白名单非空，排除把PDF失败数量直接等同缺证据股票数量；发现原始响应丢失，限制根因结论到有证据的范围。

第二轮：检查修复是否放宽策略。保留合法引用交集和全部策略门槛；非法输出仅落隔离审计；审计落盘失败阻断，不会生成计划；增加重试后的输入长度检查；补上混合合法/非法关联不能通过的负向测试。

## 验证记录

初次相关回归130项通过；最终全套1325项测试通过。同快照DeepSeek月度方向复验已通过，2次模型尝试累计约789秒；没有重新采集数据，也没有部署或发布生产。

第一轮真实复验：12个主题、27个产业链节点、13条行业映射；原来三类证据引用错误和节点关联主题错误全部未再出现，逐条引用/关联问题列表为空。仍因27个节点低于既有40节点要求被拒绝，并进入原有两次上限内的第二次修正。该27是产业链节点数，不是股票数；没有以降低门槛的方式通过。

第二轮真实复验：12个主题、53个产业链节点、13/13条必需行业映射。现有全部月度发现校验通过；独立复算确认无缺失、未知或重复行业映射，144项原始证据白名单保持不变，引用校验问题为空。两个逐轮审计文件的内容哈希均校验一致，合计约1.30MiB，不复制249MiB全量快照。

量化衔接检查：使用同一冻结快照、通过校验的主题图、服务端月度决策与成熟主题映射执行正式 `screen_a1`，没有模型调用或数据采集。3907只研究范围加2只范围外对照，共记录3909条判断：1267只本地活跃候选、21只模型代表候选、2497只本地观察、31只主题外、2只范围外、91只硬排除。合计1288只量化候选；候选来源中没有凑配额或券商金股直接入选。21只代表的个股LLM审核尚未执行，因此1288不是正式已发布A1数量。

验证文件位于 `outputs/evaluation/acceptance-20260907-5ffa424/discovery-fix-v1/`：`result.json`、`verification.json`、`quant-smoke.json`和两份逐轮审计。修改后请求哈希为 `a56250a3f7f0d72b215f3612ae71f710116309c7f88c0cc1f9a5ef3af3ff04f1`，输入哈希为 `ccb27a5bf4749b75b5dceb10f1c4248afbdc15615ddbd9a013a07ec4405081c4`。

剩余边界：尚未重新执行A1个股LLM审核、A2、A3，也未对修复后的完整漏斗重新验收或发布。原运行两次响应不可恢复，不能把新响应当作原响应的逐字重放。31只非2026半年报来源已逐只列出，后续报告期更新须独立核对，不应为了全覆盖把旧报告伪装成最新报告。

新增 `scripts/replay_a1_discovery.py`：默认只准备输入，不调用模型；必须显式使用 `--execute` 才会执行现有两次上限的月度方向复验。要求指定预期快照哈希和全新输出目录，拒绝覆盖已有证据和损坏快照。真实快照预览已验证，其提示词/输入哈希与本轮修复复验一致；重复目录、错误哈希两个负向检查均按预期退出，未调用模型。

示例（默认只生成可复核请求）：

```powershell
.\.venv\Scripts\python.exe scripts/replay_a1_discovery.py --snapshot outputs/evaluation/acceptance-20260907-5ffa424/snapshot.json --expected-snapshot-hash 9af65094fc917a213da118823abe39c57acf9de31464c978bb7a4101970e42ee --output-dir outputs/evaluation/a1-discovery-new-preview
```

旧定时轮询已删除，避免与本次主动修复重复执行。原生产记录和计划保持不变。
