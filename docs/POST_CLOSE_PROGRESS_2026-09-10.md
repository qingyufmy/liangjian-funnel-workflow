# 9月10日盘后分批进度

## 15:49—15:51 北京时间：生产连通性阻断

本地北京时间已超过15:35，项目XSHG交易日历确认2026-09-10为交易日。当前本地提交为423a55f，午间A5修复已推送；上次已验证的生产提交为283dd4a，不能将此旧记录视为当前部署状态。

本轮只读检查结果：

- SSH别名aurum-vm实际解析到root@192.168.31.254:22；连续两次连接均被远端关闭。
- HTTP健康接口http://192.168.31.254:3210/api/health返回空响应；显式绕过代理后仍为空响应。
- ICMP两次超时。ICMP单项不足以证明停机，但SSH和应用入口也未取得可用响应。

尚不能区分虚拟机停机、网络路径或主机服务异常。未取得VM时间、实时任务和运行日志，因此未部署、重启、替换计划、重跑研究或修改生产数据库；不能确认15:10收盘研究是否仍在运行，也不能确认今日收盘A4、T+N或后续A5送达。

16:00盘后A5和16:10表现补齐尚未到执行时刻，不判定漏执行。午间12:45飞书成功属于既有证据，不冒充收盘送达证明。

下一批：先恢复/确认VM连通性，读取关键任务与当日真实结果。无关键任务且冻结验收通过后，才通过既有deploy.sh部署；连接未恢复前不尝试额外研究或重启。自动检查保持启用，以便后续核验恢复状态。

## 后续核验更正

SSH 已恢复；VM 启动时间为 15:47:05，原 15:10 收盘研究被重启中断。3210 仅绑定回环地址，Windows 直连失败不能作为应用停机证据，后续以 VM 内健康接口为准。盘后已完成两批部署，最新进展、A4 全量冻结复算及新出现的模型账户 403 阻断，见 [全日诊断报告](A2_A5_SESSION_DIAGNOSIS_2026-09-10.md)。16:00 A5 实际输入超限失败，修复后又遇账户拒绝，不能标为报告完成；16:10 表现任务执行成功与到期标签价格齐全必须分开。
