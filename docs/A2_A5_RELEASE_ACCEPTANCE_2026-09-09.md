# 9月9日晚 A2—A5 发布与9月10日准备验收

最终结论（北京时间2026年9月10日00:30核验）：修复已部署，完整9月9日收盘研究已完成，22个9月10日正式计划已原子发布、等待早盘复核。服务部署准备状态为true、阻断项为空。已验证的是盘前准备，不是尚未发生的9月10日盘中表现；免费源缺项、历史A5证据不足和策略收益改善仍分别保留边界。

## 已完成的发布与验证

- 第一批提交 `4bc9e035b96377924a834032d8621e017d45be8f` 已推送并通过既有 `deploy.sh` 部署至 `192.168.31.254:/www/wwwroot/Agu/liangjian-funnel-workflow`。北京时间21:51健康接口正常，11个关键已安装Python模块与仓库源码哈希一致。
- 发布前全量1420项Python测试、83项调度/控制台测试、TypeScript类型检查通过。趋势冻结窗口的6328次有效时点对照详见本轮修复方案，不宣称完整账户回放。
- 新生产研究批次 `2026-09-09-close-release-4bc9e03-215120` 已启动：复用月度A1，刷新9月9日日度数据，仅DeepSeek主模型，不复用旧A3，目标为下一交易日9月10日。
- 新版A5已经完成今日收盘真实数据核验、模型生成和投递，复盘编号 `f8c3ba22-2890-597f-99d2-19e919c8fe92`，来源仍是今日实际使用的9月8日收盘计划，未混入明日计划。读取827只完整A2候选、3只实际历史信号；信号表现卡和A5报告各有SENT回执。状态DEGRADED指历史证据仍有不足，不是运行失败，也不能改称全部健康。
- Windows独立告警任务启用，最近执行返回0、漏执行数0；21:53测试飞书投递成功。主动只读探针确认虚拟机可达、服务健康、最后完成分钟为15:00。此独立探针仍依赖Windows开机并登录。
- 生产T+N表已具备信号基准的1/3/5/10日收益字段；原始历史成交与旧收益口径未覆盖。

## 真实A5运行后追加复查

第一轮复盘发现提示词实际241682字符，距250000硬上限仅8318字符。增加无损重复结构字典；不删除股票、事件、日期、证据或异常，不提高上限。旧冻结样本从约236571缩至约229560字符，仍需继续观察实际更大批次，不声称无限容量。

第二轮复盘发现确定性注入缺陷缺少证据编号，以及Schema要求最少1天、示例10天与“工程修复按回归验收”冲突。已增加证据编号和最终证据验证；数据/工程修复允许0天并明确按回归验收，策略影子测试仍要求正数观察天数。相关57项测试通过。追加变更只影响A5及其通知/验收脚本，不修改已运行A2/A3的策略代码、提示词或当日原始交易记录。

实际新报告的飞书渲染再次揭示中文化残留：嵌入句子的策略名、未成交、轮动范围外、价格字段被错误替换成“系统内部状态”。补齐准确映射，并要求模型叙述使用中文、证据编号只放专门字段；未知说明明确标记未识别，不编造成业务状态。价格字段组合与风险等级“高/低”分开，相关58项测试通过。

只读准备检查新增实际归档验收：读取与A4相同的本地五分钟库，仅以目标交易日定位连续历史，重算此前闭合十五分钟MACD，不产生虚拟的当日行情或A4事件。旧9月9日520计划能准确检出“日线指标存在，但此前十五分钟历史未预热”，不会用下载数量替代可用性。

执行契约补充防御性检查：主调度原本已在14:45后禁止新开仓，本轮不改变时段。底层有效事件入口也检查下一分钟是否可交易、是否超过正式计划有效期、价格与风险线是否有效；不成立只落观察记录，不发有效买入卡。不是确认今日存在15:00错误信号。相关106项策略、监测、模拟、A5与通知测试通过；历史兼容分支和T+1不变。

## 真实批次中发现并修复的阻断

第一轮真实研究在A2被阻断：812只量化评价，98只提交单批模型，输出固定4096词元，最后返回11542字符且JSON未闭合；同容量重试8次、耗时472秒。此记录保留，不称为已发布。将A2输出预算改为按候选规模有界配置（本次16640，上限32768，不超过部署配置），明确识别模型finish_reason=length且不按同预算重复请求。不能从没有保留finish_reason的旧记录绝对断言全部8次均为同一截断原因。

同时修复日度断点恢复误用全市场数量契约：当前A1与冻结当日热度池的并集，必须精确等于冻结全量研究域中的对应交集，才允许复用已校验摘要的同日快照；A1范围改变、缺股、多股或摘要不符均拒绝。相关158项模型/研究/编排/缓存测试通过。新执行继续使用冻结快照`232725c40f3a27ad1c2b6f4bcb8c75993b11d1d5b5088567d30cb98060940ebf`，不重复40分钟资料准备。

第二轮已消除JSON不闭合，但出现板块覆盖阻断。诊断批次`2026-09-09-close-a2-audit-225505`捕获已解析的模型输出和原样执行的后处理结果：模型确实选择600026、601975、000520（航运），000878、000807（有色），000506（贵金属）；三个板块标识却被放进月度theme_reviews。模型输入省略明确的A1主题字段，后处理再把三个板块归并为RESOURCES_ENERGY，评分未对应，触发A2_THEME_SCORE_LINEAGE_MISMATCH并降级。不是模型没看这三个方向。新增逐股a1_theme_id与唯一主题范围、保留独立板块覆盖范围；非规范主题ID和重复月度主题要求模型重新输出，不拼接多个板块评分，不删除覆盖校验。诊断脚本仅记录已解析JSON和确定性结果，不改变选择和审核结果。

修正后的代码提交 `0b5847161be508815cbb4fbf84abb03f2d9ade2a` 已通过 `deploy.sh` 部署，虚拟机Git版本一致，健康接口正常。相关144项研究、编排与轮动回归通过。真实批次 `2026-09-09-close-a2-audit-230124` 的A2已通过：812只量化评价，98只单批模型复核，15只聚焦、83只观察。此前三个板块覆盖错误不再发生；航运、贵金属、有色仍分别验收，而非合并成一个“资源能源”方向。完整发布验收见文末。

## A5最终实测与历史完整性

- 最终A5复盘编号 `ed263210-e9ed-51a1-b1b4-cbb536c6dfe3`，输入摘要 `8946b34092504deae6fea623097cf43c98c22be810c0df244280bb85ae0ea061`。该次运行使用 `808b306`；随后 `0b58471` 只更改A2及验收工具，A5代码没有再变动。
- 报告已投递，回执 `3b677a42-716b-56d5-8595-03e749f03ac5` 为SENT。三信号当日表现卡已有成功回执 `71498157-581c-585b-89bd-f07c14e72fd6`，本次正确幂等，没有重复发送。三只历史信号仍未成交，不计账户持仓收益。
- 报告状态DEGRADED、评价需要关注：历史520计划600389的十五分钟MACD未预热、600691存在异源价格超容差。确定性缺陷保留有效证据编号，工程修复按回归验收而非强行等待10天；策略影子建议仍保留观察期。历史缺陷没有因新版修复被擦除。
- 以实际最终报告重新执行卡片渲染（截获发送函数，不发网络、不写账本）：1张卡、2506字符，“系统内部状态”0处、“未识别”0处，无残留的大写内部状态词。此处核验的是渲染正文，不是投递账本中不含正文的元数据。
- 9月9日9083条原始判断记录哈希仍为 `5406170174c34497a4ad248c654b9d3b4c496fd68130cbd42550dcc6de987e68`；全部2条原始模拟成交哈希仍为 `d32af9466c7ffed7a1b0f5863beac9d6a6140e3e56250a6876cb2d60759a7e92`。与发布前基线一致。

## 数据边界

冻结日度快照摘要 `232725c40f3a27ad1c2b6f4bcb8c75993b11d1d5b5088567d30cb98060940ebf`，资料采集截至22:23:24；不能将全部资料声称为21:51已知。东财当日人气榜完整100只。月度A1继续复用，日度入口融合当日热度后再通过G0筛选，不因每日热榜变化重建月度基本面池。

本次PDF抽取失败16份、涉及15只股票；逐股核对冻结资料，这15只仍有可用2026年半年报主营证据与完整核心财务数据。另301699的财务指标数据集缺失，保留缺项状态，不假造指标。最终执行池的具体影响在发布核验中记录。

免费源板块排名仍有成交额分母、三/五日动量等字段缺口，已保留降级标记。不能将成员价格广度称为同花顺专有板块强度，也不能据不同口径排行强制调股。候补方向本轮已进入A3研究审核，但盘中自动晋升尚未启用；新旧热点切换的收益改善尚未得到实盘验证。

## A3真实阻断与修复过程

批次`2026-09-09-close-a2-audit-230124`最终在A3第三批阻断，原因MODEL_WALL_CLOCK_TIMEOUT，前两批已校验但没有部分发布。第三批16只候选的请求达到1,185,721字符，A3_DETERMINISTIC_CONTEXT占1,067,154字符：通用提示词投影漏将该字段列入按股票过滤的字段，导致每批重复收到全部98只股票的量化上下文，其他价格和因子字段却只有当前16只。现已补齐同一批次范围过滤，保留该批每只股票的全部字段，不截断证据，不改技术资格；完整98只量化证据仍保存在冻结记录。新增0/10/16只批次范围、字段不丢失及快照不变回归；相关142项测试通过。诊断运行器同时记录A3请求字符数及已解析结构化结果（不保留模型思考），供真实运行验证。

补丁`e9602ae8c97329d623d064143e243762091f56b3`已通过`deploy.sh`部署；新批次`2026-09-09-close-a2-audit-232804`恢复同日快照耗时2秒。实际A3第一批请求290,921字符、第二批292,420字符；第一批模型189,523毫秒、单次通过。两次运行的股票优先级允许受模型结构化判断影响，不声称不同批次完全等同；完整性仍由量化域与输出覆盖校验约束。

补充免费源实测：对600389只调用与生产相同的通达信/腾讯适配器和520历史准备函数，使用内存存储、不建立生产计划、不写行情归档。通达信取得9月7日至9月9日完整144根闭合五分钟线，合成48根闭合十五分钟线，MACD预热完成。此项是新准备链路的真实取数验收，不用于改写9月9日旧计划“当时未预热”的历史事实，也不冒充明日某个正式520计划的发布证据。

完整A3批次已经完成；正式计划数量、策略分布、集合包含关系、目标日期、指标与已安装模块的一致性验收见文末。

第二次A3第三批阻断的进一步核对：保存的模型结果覆盖了正确的16只股票，均声明PASS，但16只的scenarios全为空对象，违反四类情景容器契约，触发A3_SCENARIO_SET_INCOMPLETE。前两批有完整容器，因此通过。第一次离线复算未装载阶段价格覆盖层而出现PRICE_LEVELS_UNAVAILABLE，这是诊断重建不完整，不能当作生产价格缺失；补回阶段价格与因子后定位到情景容器，且原始模型JSON可直接验证缺项。

情景现由服务端根据冻结的入场区、禁止追价线、失效位和A4必要条件确定性生成，模型可省略容器。正常/弱开盘等待原策略确认，高开禁止追价，入场前失效取消计划；持仓退出继续遵守T+1。只在新情景合同标记下生效，保留旧记录兼容；不覆盖模型PASS/VETO/DATA_GAP，不放宽价格、技术、风险或发布校验。提示词更新为agent-3/3.5.0，避免让模型重复抄写可直接生成的展示字段。新增缺容器、模型篡改追价上限、VETO保留测试，相关210项回归通过。诊断工具允许显式指定已收盘且非未来的研究时点，以便跨午夜继续9月9日研究、仍准备9月10日，而非误跑9月10日尚未发生的行情。

不提前激活明日计划；08:30为盘前报告、09:26为行情复核与激活、09:31—11:30及13:01—15:00为分钟任务、11:35与16:00为A5，16:10为T+N补齐。明日真实执行尚未发生，验收结论只覆盖已验证的准备条件。

## 最终发布批次与全量验收

- 最终应用提交：`ebdf17288e989751121b83d70f9648c9352ef09b`，通过`deploy.sh`部署。后续验收文档、只读复算脚本提交不改变该应用代码。
- 唯一正式批次：`2026-09-09-close-a2-audit-235914`。研究逻辑时点为9月9日23:57，实际执行跨午夜，目标交易日仍为9月10日；耗时1757秒，最终READY。没有使用前面阻断批次的部分结果发布。
- 最终代码全量1433项Python测试通过；前端/调度代码自83项测试与TypeScript检查通过后未再修改。后续新增只读指标复算脚本也已在虚拟机实际执行通过。
- 月度A1继续复用782只；当日热度补充、全市场研究域交集与G0过滤后，日度A1入口为812只。不能把812称为重建后的月度池数量。
- A2量化评价812只，98只单批提交DeepSeek：17只聚焦、81只观察；714只留在范围外/本地观察。五个主方向全部进入模型审核，且都有聚焦股。
- A3量化评价全部98只：58只具备技术研究条件提交模型，33只本地观察、7只本地不合格。最终结果容器为22只核心、36只候补研究、40只不形成日线计划；后者包括上述本地观察和不合格，不代表40只都遭模型否决。
- A3四批全部完成，请求分别约29.2万、29.3万、29.3万、19.2万字符；模型记录耗时414734、255750、518043、226623毫秒，第三批记录两次尝试。大批次延迟仍应持续观察，不能宣称已经实现秒级研究。

### 五个主方向逐层对应

以下采用本次冻结的免费数据源轮动口径，不等同于同花顺专有强度。股票可以关联多个概念；表内是这次筛选选中的轮动方向，不代表公司只有这一项主营业务。

| 轮动方向 | A2聚焦 | A2观察 | A3正式计划 | 未形成正式计划 |
| --- | ---: | ---: | ---: | ---: |
| 航运 | 4 | 0 | 3 | 1 |
| 国防军工 | 3 | 5 | 8 | 0 |
| 贵金属 | 1 | 1 | 0 | 2 |
| 有色金属 | 2 | 7 | 4 | 5 |
| CPO与光互联 | 7 | 3 | 7 | 3 |
| 轮动候补研究域 | 0 | 65 | 0 | 36只研究合格、29只不合格/观察 |

候补采用化工、机器人、半导体先进封装、电力设备新能源四个符合条件的方向，未为凑满五个候补而降低条件。原始行业/概念映射可能比这四个归纳方向细；不能把其原始标识数量当作新增主方向数量。36只合格候补的发布阻断均为`A3_ROTATION_RESERVE_RESEARCH_ONLY`，不是模型审核失败。

贵金属的具体判断：000506招金黄金已经是A2聚焦股，A3未满足日线主升/突破/回踩路径；002237恒邦股份进入A2观察，A3未满足520右侧确认。记录中的`MARKET_RISK_OFF`是风险背景，分别对应的实际技术阻断为`TREND_DAILY_PATH_MISSING`和`MA520_RIGHT_SIDE_NOT_CONFIRMED`。002155湖南黄金的映射已存在，但本次识别为情绪首板、情绪优先于趋势，受情绪周期新开仓权限限制留在A2本地观察。此政策是否过度保守只能作为后续冻结样本研究，不能仅因当天上涨直接放行。

### 正式计划与技术真实性

22只正式计划全部为趋势五日线：

- 航运：000520凤凰航运、600026中远海能、601975招商南油。
- 国防军工：000576甘化科工、002111威海广泰、002297博云新材、002389航天彩虹、002985北摩高科、300034钢研高纳、600150中国船舶、688239航宇科技。
- 有色金属：000807云铝股份、000878云南铜业、300811铂科新材、300855图南股份。
- CPO与光互联：000070特发信息、002787华源控股、300607拓斯达、300852四会富仕、301041金百泽、301312智立方、688205德科立。

`verify_day_plan_readiness.py`全部14项断言通过：A2全域包含于日度A1、A3研究包含于A2有效域、正式发布集合精确等于22只核心、单一待复核批次、全部有效期至9月10日15:00、未提前激活、候补未进入执行、量化合格且模型PASS、趋势规则版本冻结、T+N字段存在、13个已安装关键模块与源码逐个哈希一致。

`verify_day_plan_prices.py`从只读事实库按9月9日23:57知识截止查询不复权日线，不调用生产指标计算函数，独立复算五/二十/六十日均线与MACD。22/22全部一致，MACD完整输入序列摘要22/22一致，每只使用530—544根历史日线；四类服务端情景来源22/22一致。不使用只有30天的展示窗口冒充六十日均线验算。

本批没有520或龙头正式计划，因此“本批520历史预热”属于不适用，不以空集合校验通过声称完成了正式520计划验收。520新增历史准备链路另有600389免费源144根五分钟线实测及回归证据，见上文。没有为凑策略数量将观察股强行提升。

301699的财务指标缺口没有被填造，该股未进入正式计划。A2仍有8只量化记录标记资料缺口、免费源存在部分板块字段降级，不宣称全市场资料无缺失；正式22只的日线技术证据已逐一复算。

### 9月10日运行准备与历史保护

00:30读取虚拟机实际控制台：服务healthy，configurationReady/stateHealthy/deploymentReady均为true，blockers为空；最新研究为本次READY批次，activeJob为空。调度运行中，盘前、09:26复核、分钟盯盘、两次A5和T+N任务均启用，额外对比模型关闭。22只计划全部为待早盘复核、valid_from为空，不是已触发买入。

9月10日尚未到A5执行时间，当前日的A5状态为空是正常的；最近9月9日最终A5仍为有历史证据缺口的DEGRADED且已成功投递，不能混淆成今日任务失败。盘前行情复核仍有权撤销或保留计划，不能承诺22只全部获准开仓。

虚拟机磁盘79GB、约18GB可用，系统内存可用约1.7GB；运行期间交换区占用约1.8GB，后续应继续关注资源趋势。Windows独立告警任务Ready，最近执行返回0、漏执行数0；该机制仍要求Windows开机登录。既有调度租约没有残留RUNNING，最后盘中分钟记录为9月9日15:00，模拟账户当前无持仓。

发布后再次计算历史数据：9月9日9083条判断记录与全部2条模拟成交的摘要均与本文基线完全相同。没有覆盖历史行情判断、没有补发历史交易信号，也没有把未成交信号算作账户收益。

复现命令（在虚拟机项目目录、以www用户运行）：

```sh
.venv/bin/python scripts/verify_day_plan_readiness.py --run-id 2026-09-09-close-a2-audit-235914 --target-date 2026-09-10
.venv/bin/python scripts/verify_day_plan_prices.py --run-id 2026-09-09-close-a2-audit-235914 --as-of 2026-09-09T23:57:00+08:00
```
