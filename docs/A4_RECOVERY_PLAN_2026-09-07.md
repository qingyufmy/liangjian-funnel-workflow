# 9月7日收盘回放与运行告警实施计划

用户已确认：今日中断来自手动重启虚拟机服务。本次不再把该中断推断为未知生产崩溃，但仍修复无告警、恢复无缺口记录的问题。

## 工作顺序

1. 只读冻结今日指定40计划、早盘激活结果、全日监控事件、通知及原始分钟归档，附生产提交和文件哈希。
2. 在本地隔离目录收集通达信、腾讯收盘分钟行情，核对240分钟覆盖、时间戳与价格差异；不覆盖生产原始行情。
3. 固定计划和策略，分别分析生产原始输入、收盘核验行情、停机缺口。市场广度缺失与模型未重放明确标识，不能将假设结果当作实际成交或当时可获得的信息。
4. 增加独立的只读运行探针、持久告警账本、交易日/午休识别、任务停滞和恢复告警。探针不依赖应用进程，也不运行交易命令。
5. 完成针对性测试及全量40计划诊断报告，交付实际生效范围与剩余部署边界。

## 第一轮审查及调整

- 现有回放工具会用09:31开盘推导09:26竞价，并默认允许市场和模型；不能原样用来声称复现今日生产。改为使用真实早盘激活记录，量化潜在信号与完整执行分开。
- 历史归档可能含首次未定型值，回放须保留原始分支与独立终值分支，不能修订原始证据后再宣称生产正确。
- 现有定时任务仍照常运行，不抢占、不重复提交15:10或16:00任务。

## 第二轮审查及验收边界

- 停机63分钟内的行情虽可补齐，当时市场快照和模型判断不可凭空补造。反事实结果必须列出假设。
- 同机守护无法覆盖整台VM停机，独立探针必须在Windows或其他持续在线设备运行；安装和实际告警投递与代码测试分别验收。
- 告警按故障生命周期去重，发送失败保留待重试，恢复不能先于故障发送成功而被标为全部送达。
- 不触碰用户既有主营证据修改；不自动提交、推送或部署主应用。只读独立探针的启用作为本次增加告警的实施步骤，不重启主服务。

## 主要剩余风险

外部探针宿主机也关机时无法告警；数据源历史终值不等于当时可获得版本；一天40计划全为趋势策略，不能代替龙头与520的专门验收。
