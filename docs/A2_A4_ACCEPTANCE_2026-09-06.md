# 9月7日前A2修正与A4运行验收

本次目标：纠正A2板块映射和强制晋级，使用相同代码复核9月3日、9月4日，并检查9月7日A4就绪状态。

## 已确定的合同

- 月度主题和日度板块成分分别维护。保险使用BK0474，一般零售使用BK0482；不同供应商之间是语义核对，不宣称成分完全一致。
- A2全量量化筛选后一次模型复核。覆盖意味着有聚焦代表或明确NO_FOCUS结论；服务器不能为了数量晋级。
- A3继续接收满足策略路由条件的A2聚焦、观察候选，保留来源；由技术规则判断三类策略是否就绪。
- 9月3日事后成分回放只用于诊断，不作为当时可得数据证明或生产发布依据。
- A4使用DeepSeek Flash，量化先行、模型否决复核；显式关闭隐藏思考，不修改MACD、KDJ、止损、入场及退出条件。

## 两轮审查

第一轮：删除服务端强制晋级；保留宽月度主题、新增窄板块；验证无聚焦原因必须绑定输入方向和代表股；分离观察和采集时间。

第二轮：发现A3本来接受可路由观察股，应保留覆盖合同；发现A4省略思考参数无法关闭网关隐藏思考，真实Flash探针确认显式参数有效，因此修正A4传输。

## 本地验证

- A2、映射及回放时间修改后Python全套1233项通过。
- Node 67项通过，TypeScript检查与生产构建通过。
- 2026-09-06实际采集：保险5只；一般零售60条原始记录，排除2只B股后58只A股。
- Flash探针：省略参数有隐藏推理；显式关闭后无隐藏推理、返回有效JSON。

部署、历史回放、计划和调度验收结果待追加。测试通过不等于交易日真实运行已经发生。
