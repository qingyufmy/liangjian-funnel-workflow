# A3 第一批修复与盘后交接

日期：2026-09-08。状态：R1 展示子批本地验收通过；08:48 用户要求完成部署，已核验盘前空闲窗口，正在提交发布。未重跑研究、未修改今日计划。不是全部 34 项完成。实际发布结果以后续部署验收记录为准。

## 今日生产安排

用户要求今日 A4 照常运行，每日迭代避开盘中。原定盘后发布；08:47 后最新指令要求完成部署，核验当前 08:48、无活动研究或 Python 任务后，改为开盘前通过 deploy.sh 更新并验收。不替换计划、不重跑 A1—A3。盘后仍核对当日原始执行记录，继续后续分批迭代。

07:27 左右通过 `aurum-vm` 对 192.168.31.254 的 `/www/wwwroot/Agu/liangjian-funnel-workflow` 进行了只读核验：

- 生产提交：`7c3aa6292a3e40803ecdaa2ad6548ffa01a413b2`。
- 最新生产研究：`2026-09-07-close-34ab6b52c8d2`，READY，无活动研究任务。
- A1 738（复用 9 月 4 日研究）；A2 聚焦 5、观察 55；A3 输入 49、核心 26。
- 26 个正式计划均等待早盘复核，今日活动数当时为 0；抽验计划与上述源批次匹配，有效期截至 9 月 8 日 15:00。
- 这说明已有今日目标日计划，不代表本次新代码已重算，也不代表早盘复核或盘中执行已经完成。盘前状态不可用于宣布全天稳定。

## 本批修改

1. A3 原始非核心池仍保持 `rejected_candidates` 身份，新增只读显示分类；冻结 49 条分为研究合格 26、技术待观察 16、技术否决 5、证据待核 2。旧接口和逐行索引接口都在分页前筛选分类。
2. 投影原记录中的确定性策略、资格和未满足条件，修复“未提供策略／资格”。不生成价格、不提升资格、不写正式计划。
3. 大周期谨慎提示与 A4 待确认单独呈现，不再充当 A3 淘汰主因。未满足条件、背景、盘中待确认、研究校验提示分开。未翻译条件明确视作说明缺陷，保留折叠诊断，不抹为“系统内部状态”。
4. “明细完整”对非核心改为“分流说明齐全”，明确不代表研究数据齐全或能够发布计划。数据缺口仍展示独立技术否决，不因缺口修补而自动放行。
5. Python 报告与中文导出提供非核心核对说明；导出附表不加入合格 A3 集合，不改变 A1 包含 A2、A2 包含 A3 的原检查。飞书修正本批已知大周期及否决原因中文。
6. `deploy.sh` 增加北京时间工作日 09:00—15:35 阻止发布的保护，覆盖午休；无法解析时间则停止。节假日也保守阻止该窗口，其他时间仍需检查任务状态。首次发布前旧生产脚本尚无此保护，需要先人工核验时间，不能仅依赖待部署代码。

界面按 Impeccable 的说明清晰性要求，沿用现有视觉系统，重点分开研究资格、证据缺口和正式发布，不进行视觉重设计。

## 验收证据

- `npm test`：81 项通过；包含 49 条冻结数据不变、分类数量、所有当前说明已翻译、索引及旧文件分页筛选、未知状态不提升资格、下游未晋级不能冒充合格。
- `npm run typecheck`、`npm run build` 通过；`git diff --check` 无空白错误。
- Python 定向测试 22 项通过：`test_a3_display.py`、`test_chinese_export.py`、`test_research_stage_reports.py`、`test_lark_notifications.py`、`test_deployment_window.py`。
- 部署保护 9 个用例覆盖 08:59、09:00、午间、15:00、15:34、15:35、周末及无效时间，仅抽取函数测试，未运行部署正文。`bash -n deploy.sh` 通过。
- Impeccable 检测未报告问题。
- 本地真实详情组件与只读 ProjectFiles API 使用 `test/fixtures/a3-20260907-display.json` 实测：观察 16、否决 5、缺口 2，WATCH 中文正常，无旧“大周期仍需盘中确认”文案，默认 1280 宽度无页面横向溢出，控制台无错误。预览不连接生产，也不证明生产新页面已发布。
- 冻结样本是精简投影，缺省主题、计划价格和全量指标没有伪造；预览中这些缺省字段不能反推出生产本身缺失。

## 盘后待执行

已建立工作日 15:35 本任务跟进（自动化标识 `a`，启用状态已读取）。启动后先核验项目交易日与当前北京时间，检查关键任务是否仍运行；依赖 Codex 自动化实际被唤起，不等于业务已完成。

1. 只读保存今日 A4 原始事件、分钟行情缺口、停机／恢复告警、T+N 表现落盘、A5 与飞书送达；区分正常未触发与链路故障。
2. 查看工作区和远程提交变化，精确提交本批文件，保留 `.codex_tmp`、竞品报告与截图等其他工作。当前尚未提交或推送。
3. 通过已有 `deploy.sh` 发布，核验实际提交、Python 安装代码、服务和 UI 静态资源；该动作必须等盘后且不打断关键任务。
4. 冻结完整原始数据后单独重放 A2→A3 题材阶段交接，重点光迅科技、华兴源创。当前只有旧记录投影验收，不是完整策略重放。不可直接把 2 只缺口转合格。
5. 对需要更新的研究阶段运行最新代码，A1 是否重跑以事实／版本变化为准；检查批次、截止时间、目标交易日、A1⊇A2⊇A3 和正式发布记录。严禁补发历史信号或覆盖原始结果。
6. 后续按主方案 R1 其余证据输出与完整回放、R2 状态和日期、R3 单股档案、R4 板块视图、R5 三策略及 A5 闭环推进。不得把本批页面修复称为全量策略正确性验证。

## 已知未覆盖

- 未来出现新原因码仍须维护映射，未知码要暴露缺陷而非成为淘汰理由。
- 全量技术规则、提示词、五视角研究档案尚未迭代；执行策略未调整。
- 中文导出增加的是行投影附表；消费端若只枚举 A1/A2/A3，需后续检查实际工作簿生成器是否接入附表，不能宣称已交付新工作簿。
- 自动化只是唤起盘后工作，不能替代生产验收；今日 A4 的自然调度、告警和结果必须盘后用实际证据确认。
- 本次本地预览服务和浏览器页已关闭。两个约束在临时目录中的精简样本目录 `a3-preview-dFteeN`、`a3-preview-L4Vxxj` 清理命令被环境策略拒绝，未绕过；它们不在生产目录，不包含完整行情归档。
