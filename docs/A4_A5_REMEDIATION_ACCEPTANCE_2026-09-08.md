# 2026-09-08 A4 / A5 修复验收

## 范围与结论

本次落实 `A4_A5_T1_REMEDIATION_PLAN_2026-09-08.md` 中有事实支撑的执行、数据及归因缺陷。不调整选股门槛，不把当天少信号认定为策略失败，不连接真实账户。盘中原始事件与模拟成交保留。

生产目标为 `192.168.31.254`，应用目录 `/www/wwwroot/Agu/liangjian-funnel-workflow`；部署前版本 `9af654fd29d7a0c4024df6835823f3442265031b`，发布入口为原有 `bash deploy.sh`。

## 已实现

| 问题 | 修改及验收依据 |
| --- | --- |
| T+1 技术退出混同可成交 | 技术动作与执行资格分开。锁定/可卖数量不明时为等待退出；硬止损判断保留，不能突破可卖数量。|
| 跨日及部分可卖 | 按实际交易日、买入账本解锁，同日重启幂等，拒绝日期倒退/非交易日；卖出旧仓后保留全量退出的锁定余仓，后续交易日使用独立幂等意图。|
| 减仓覆盖首次离场 | 分别保存首次退出、最高优先级退出和最新观察。已知全量退出优先于减仓；历史汇总可由原事件及成交重建。|
| 最终成交校验 | 在账本事务中再次校验可卖数量、资金及持仓前后值；普通股票与科创板区分数量规则，价格按分位取整，未支持证券类型拒绝执行。|
| 15:00 尚未定稿 | 腾讯尾部零成交量尝试通达信独立窗口；仍未确认则保留原始观察并阻断该标的判断和模拟成交，不补造历史买卖信号。|
| 520 指标证据 | 日线因素增加确定性 MACD；15分钟 MACD 使用本地此前交易日连续5分钟归档预热，明确输入序列。缺失、断档不伪装成预热完成，不新增模型票决或放宽技术门槛。|
| A5 真实晋级归因 | 从 A3 核心、观察及淘汰集合提取实际参与证据，下游记录优先于 A2 的聚焦/观察标签；缺失血缘标未知，模型归因与事实冲突时拒收。|
| A5 完成收益 | 仅已清仓生命周期进入完成收益统计；部分退出、未卖出不算已完成交易。补充退出汇总与指标覆盖的事实字段。|
| A5 请求失败 | 16:00任务实际启动后失败；原错误码把泛化400误标为输出预算拒绝，实际部署本来已有16384上限，单纯加调用参数不能解决。实测请求2721199字节，包含5767条重复确认。改为完整事实落盘、模型读取按计划/动作的原因汇总、首末和最近触发样本、全部有效事件；另设上下文上限并修正A5错误分类。|
| 飞书及 T+N | 退出通知明确“触发技术离场”不等于卖出成交。新标签读取原事件及嵌套策略原因；已存在空原因支持预览、保留旧值后审计修正，不更改收益数据。|

## 本地验收

- 完整 Python 测试：1367通过，0失败，0跳过。证据：`outputs/audits/a4-a5-20260908/remediation-tests.xml`。
- TypeScript 类型检查通过；本轮未修改网页交互。
- 新增覆盖同日重启、交易日解锁、混合持仓残余退出、原始事件/成交不变、退出汇总修复幂等、T+N修复不变收益、零成交量收盘保护、三类A3池真实归因、历史指标防未来与断档、数量及价格精度等场景。
- 今日37个选定冻结窗口、三种行情版本共111项复算，1项原始通达信取数缺失。伯特利买入不变；离场/减仓技术动作不变，执行状态改为T+1等待。该结果不是全日5810条重放，不证明所有行情版本下收益一致。
- 冻结复算输出：`outputs/audits/a4-a5-20260908/remediation-window-comparison.json`。原诊断输出未覆盖。

## 生产验收程序

1. 确认主应用无研究/盘中任务在执行、磁盘空间足够，提交并推送本轮明确文件。
2. 使用 `deploy.sh` 安装当前源码并重建网页，核对提交、已安装Python模块路径/摘要、健康端点。
3. 对9月8日退出汇总及空原因标签先执行 `scripts/repair_a4_derived_evidence.py` 预览；确认仅派生字段，再使用 `--apply`。保留 before 文件、来源事件编号及修正摘要，校验原始事件和成交摘要未变。
4. 独立补跑盘后A5，保留原16:00失败记录，核对新报告和飞书投递。复盘的归因建议还需按证据筛查，不自动改变生产策略。

## 尚不能宣称的事项

- 新代码不能让9月8日已经冻结且为空的日线MACD自动变成“当时完整”。历史复算只能标为补充证据；新计划需在新因素构建后取得该字段。
- 历史分钟归档不足的新标的仍需补齐，不能把所有520计划统一标为指标就绪。
- 涨跌停成交排队、所有特殊证券/风险警示状态、公司行为的全覆盖模拟并未在本轮证明；现有交易风险门槛不放宽。
- 跨日仿真测试不等于已经观察到9月9日自然成交；交易日正式执行及通知仍以实际账本为准。
- 生产部署和盘后A5补跑结论，以后续生产回执为准，不由本地测试代替。

## 生产首轮回执及A5追加修复

- `428735c56052ac17f81d80c0f837b34c1e4a0705` 已经由原 `deploy.sh` 部署；已安装Python模块与源码摘要一致，VM55项核心测试通过。
- 生产预览及应用：`outputs/audits/a4-a5-20260908/repair-preview.json`、`repair-applied.json`。伯特利首次退出汇总恢复13:10、参考价30.04、目标6600股；T+N空原因补为原事件的两条原因。原始事件/成交摘要未变，持仓仍6600、可卖0。
- 追加探查只捕获首个失败请求后停止，网关返回泛化 `bad_response_status_code`，并没有明确的输出预算限制信息。不能把这个响应本身认定为容量错误；后续紧凑请求的实际成功才是验证。
- 针对A5输入整理与错误分类追加69项相关测试通过。历史午间冻结样本的模型上下文由约130万字符降到约13.9万字符，2930条事件保留56条代表及有效事件，完整记录未改写。
- 上下文保护进一步暴露跨交易日污染：盘后30个明日待审核计划的 `valid_from=null` 被误作无起点有效期，使今日26变56并绑定到错误的新A2。现按目标交易日、实际当日观察记录、明确起止日期选择；无起点待审核计划以到期交易日归属，不能提前参与复盘。追加64项相关测试通过，覆盖明日待审核计划不得进入今日复盘。
