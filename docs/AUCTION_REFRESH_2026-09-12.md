# 集合竞价后 A2–A3 刷新：检查与实施

## 诊断

本次检查的生产版本为 `25bd0d0`。实际 Node 调度为 08:30 发送上一轮 A3 报告、09:26 核验已有计划价格。两个任务都不会重新筛选 A2/A3。普通 morning 研究准备还会读取上一个已收盘交易日的板块、人气榜，所以不能只补一个定时器。

## 本次实施范围

- 增加交易日 09:26 `run-auction-refresh`，启动补偿窗口至 09:29。Python 使用交易日历和持久租约去重，周末不执行；窗口外不补历史任务。
- 使用正式 A1 原池，量化筛选后调用当前生产模型执行 A2、A3，不重跑 A1、不启动对比模型。
- 板块采集明确要求当日日期；前 100 强制请求新数据，使用独立竞价缓存，不污染盘后日缓存。
- 腾讯批量报价覆盖 A1 和前 100，校验当日、09:25 之后、非未来、距采集时间不超过 180 秒、有效价格和至少 95% 覆盖。称为“竞价后行情”，不把 09:30 后连续交易报价伪称最终竞价成交价。
- A3 仍计算已完成的大周期技术条件，竞价报价单独记录，不拼成日线，也不要求虚构的 09:26 分钟线。
- 当日板块、人气或报价不满足条件时停止刷新，落盘阻断原因；不能将旧数据改日期作为成功结果。
- 刷新使用独立 review 进程槽，A4 分钟任务保留 primary 槽；30 分钟父进程超时。跳过大批量特征维护写入，并使用独立进度文件，不覆盖 A4/盘后进度。

## 研究与执行边界

此次新增的是 A2/A3 **研究结果刷新**，不是盘中切换 A4 交易计划。结果标记 `AUCTION_RESEARCH_ONLY`，不调用计划发布，不创建或激活执行计划，不改已有持仓和订单。既有 09:26 价格复核、09:31 起分钟盯盘继续运行。

不能承诺完整模型研究在 09:30 前完成。开始时间、行情采集时间、日线截止日、完成时间分别保留；任务可能在开盘后完成。若未来要求将新池自动切入 A4，需要另行实现带持仓保护、未成交意图处理、逐股现价复核及原子替换的执行发布协议，不能通过本次“保持研究新鲜度”默默放开。

## 两轮审查

第一轮：仅增加定时器会继续使用前日板块、人气；已增加当日来源约束及强制刷新。新行情不可被称为收盘技术数据。

第二轮：完整研究可能超过四分钟且大量写特征库；已隔离进程、跳过维护写入、禁止发布执行计划和复用普通 morning 断点。失败可在启动窗口内下一分钟重试，成功由租约跨重启去重。

## 验收

测试覆盖报价日期/时间/覆盖率/异常价格、非交易日、启动窗口、幂等、失败回执、A1 复用、禁止自动执行发布、缓存强制刷新、Node 单次调度和 A4 进程槽隔离。完整结果见本次交付说明。

周六不伪造周一行情，不提前执行盘前研究。下一交易日实际免费源返回和模型耗时仍须以自然运行回执为准。
