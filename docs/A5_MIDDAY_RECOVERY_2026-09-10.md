# 9月10日午间A5补跑修复

## 已确认的阻断

1. 11:35任务按时启动，但压缩后的299496字符仍超过250000上限，在模型调用前退出。今早新增的行情证据归档索引也进入了模型输入；完整索引含文件路径、哈希等存储信息，不应占用分析上下文。
2. 修复输入后，原配置`deepseek-v4-pro-0813`连续返回503。独立小请求同样失败，上游明确返回`model_not_found / No available channel`；模型目录不再列出该名称，列出的`deepseek/deepseek-v4-pro`小请求返回200和正确JSON。这是另一项上游通道路由问题，不是输入长度错误的延续。
3. 新通道首次完整请求达到16384 token输出上限，被以`MODEL_OUTPUT_TRUNCATED`拒收。不能把截断JSON修补后当成完整复盘。A5独立输出预算调整至32768、总期限600秒，仍有明确上限；A4的45秒/2048 token预算不变。
4. 后续完整输出被反向案例阶段冲突拦截。原流程在“按冻结证据归一化阶段”之前先强制验证阶段一致，导致已有量化校正逻辑无法执行。现在先严格校验股票与证据引用，再按真实阶段校正；发生冲突的原归因明确不采纳，不标记为已确认策略缺陷，最终再验证阶段一致。原始完整模型输出单独保存，不能用校正结果冒充原模型意见。

## 修改

- 完整证据索引保留在冻结事实文件。模型只接收各来源归档数量、成功数及所有失败项；不删股票、有效信号、异常核验或反向案例。
- 表格、重复结构和字符串保持可逆编码，增加可逆字段名词典。真实午间输入从299496降至约199464字符，低于200000目标，未上调250000硬上限。JSON本身原来已经紧凑序列化，本轮主要收益来自索引投影和字段名去重，不将缩进优化误报为主要原因。
- 增加冻结事实补跑入口：校验日期、复盘类型、11:30截止时间和内容哈希。只使用原冻结材料，不重新请求下午价格，不改写原失败文件。
- 升级复盘契约版本，记录实际模型，生成新的复盘记录和上下文诊断，避免覆盖失败诊断或误用旧模型结果。
- A5独立模型配置`LIANGJIAN_REVIEW_MODEL`，默认采用上游已实测的DeepSeek Pro通道；A1–A3研究模型、A4模型不修改，不自动切换Kimi/GLM。
- 完整模型响应在结构或证据校验前落盘，失败响应可复用，不能丢失后反复重发全部输入。新增小范围纠错脚本；股票、收益与阶段由冻结事实回填，分析字段仍逐项审校。该脚本目前仅用于没有有效信号或生命周期的材料，有信号时拒绝执行，避免遗漏信号审计。
- 增加不参与词典压缩的权威计数速查区，保留实际计数、阶段、排名可比性和各字段比对汇总。加入速查及新增提示词后，真实输入为204026字符，略高于200000软目标，但低于250000硬限制；详细证据没有截断。此前约199464为增加明文速查之前的版本。

## 测试与两轮复核

第一轮检查事实边界：完整原件未改、失败项未删、字段编码可还原、日期/哈希不符在调用前拒绝，不能以“模型请求成功”代替报告及投递验收。

第二轮使用VM真实午间输入实测大小，并以极小请求区分超长输入与上游通道故障。A5、运行归因、模型传输及配置相关132项测试通过。

增加冻结材料成功补跑及幂等测试，确认不重新构造事实、连续执行不会重复调用模型，原材料不变；连同上下文和A5服务共25项测试通过。

增加阶段冲突案例，证明原模型输出仍保留原阶段，正式行项显示真实阶段并明确原归因不采纳；最后一轮上下文、服务与轮动相关55项通过。A3未验证字段的文字改为按实际字段生成，避免MACD已经验过仍笼统显示“MACD未验证”。

主体修复全套1461项测试通过。最后新增明文速查、实际纠错提示词哈希留存后，运行相关49项测试通过，并对今天原始冻结文件重新验证输入大小。未宣称新增提示词已经经历多日自动运行验收。

## 运行边界

当前为交易日午休，不执行全量`deploy.sh`，不替换生产安装包，不重启服务。修复源码在独立补跑进程中运行，使用现有运行库记录新A5并按正常流程通知；不改变A4状态和原始判断。正式定时任务使用新修复仍需盘后部署，不能把一次补跑当作所有定时入口已更新。

原事实文件：`outputs/a5/2026-09-10/midday-23cd55b2a6b1-facts.json`，文件SHA256：`c5d15fb11e976a0386655206e36d5c4b94bc8214f3f7532647abf28cb78fd7ae`。补跑入口：`scripts/rerun_a5_frozen.py`，不带`--execute`时只验证，不请求模型、不投递。

## 验收结果

最终复盘已生成并通过正常飞书通道投递，事实截止仍为2026-09-10 11:30；原11:35失败任务记录保留，没有改为成功。

- 最终复盘编号：`d7813889-4b41-5737-bee4-cebcfbcd8023`。
- 最终输入哈希：`7845346d1c4442997794d47e8c742e351f8981e62d006f0c9b72ca4d8fe18bf7`。
- VM报告：`outputs/a5/2026-09-10/midday-7845346d1c44.md`。
- 飞书投递编号：`f1f5774a-3b40-5276-9a87-bdcea2644db7`，状态`SENT`。
- 原事实文件SHA256再次一致；未重新获取下午行情，不修改上午A4判断，不补发交易信号。

本次不是未经审校的模型直出：完整初稿存在计数、阶段和数据含义错误；小请求重建分析后，仍把“异源核验降级”误称为“组件缺失”，并保留了不适用于今天的520预热提案。最终逐项对照冻结证据审校，删除不成立判断，新增审校元数据形成独立修订，不覆盖原模型响应、初稿或原事实。原始模型、小请求提示词及响应、审校说明和完整修订内容均保存在当天A5目录。最终修订模型调用次数为0，明确记录为对已存DeepSeek响应的事实审校，而非再次模型推理。

### 今天午盘的实际结论

- A2聚焦17只、观察81只；A3有22个趋势五日线计划。
- A4有2500次观察，0有效信号、0生命周期、0完成收益；独立核验的编排遗漏为0。
- 10个反例：7个未形成A3计划、3个已有计划但无有效A4信号；不等于10个遗漏买点。
- 22个计划均有部分分钟成交量差异：跨源330条、归档对通达信336条，两项可能重叠，不能相加为独立异常总数。两类比较的开盘/最高/最低价不匹配分别为2/4/5条，收盘价不匹配为0。成交金额没有可比样本，不得称为金额大量不匹配。
- 今天没有适用520策略的计划，不能认定520预热失败。A3日线MACD复算22/22通过；KDJ及成交量仍未完成独立公式核验。
- 独立价格广度前五与生产所选主题重叠5/5，但口径不可直接比较；98只候选已抽验，不代表812只量化候选或全市场已完成独立核验。

最终状态仍为`DEGRADED`，表示报告已完成但异源证据有缺口，不是补跑失败。上述行情差异及独立市场横截面缺口没有在本次A5修复中消除；后续需针对今天数据单独诊断，不用放宽策略掩盖问题。

### 部署边界

盘中仍未运行`deploy.sh`，未重启Node/生产Python服务；VM生产Git仍为`283dd4a312ced7d9704af6b48dd9ff7c354b9d0a`。本次只用隔离源码运行A5补跑和正常投递。修复已提交推送，正式定时入口应用这些改动仍需盘后部署；不以补跑成功冒充永久部署完成。
