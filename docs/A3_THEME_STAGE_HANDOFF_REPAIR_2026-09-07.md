# 9月7日续跑验收：A2→A3题材阶段交接修复

## 发现与影响

部署 `16381e5` 后，冻结快照续跑完成 A1 活跃研究池1267只、A2聚焦24只；A3对聚焦和可继续观察的67只候选执行量化前置，得到23只候选（趋势五日线21、520两只）。其中9只情绪股被记为题材阶段未知。

逐条检查发现，A2同批次 `active_themes` 已给出 `stage=ACCELERATION`，A3策略评估只读取个股行及其上下文，`screen_a3` 未将题材表按精确主题标识关联到个股。因此不是日线缺失，也不是9只全部满足龙头策略。

## 修复边界

仅在个股及原上下文没有明确题材阶段时，从同一A2输出的唯一、精确匹配主题行补齐。保留来源与内容哈希，并将交接依据纳入判断标识。缺失、重复、未知或不匹配的主题不能借用市场整体情绪状态；已有个股退潮等明确状态保持原优先级。没有降低技术、连板、止损、收益风险比或A4确认要求。

## 两次复查与数据对照

第一轮复查：从A3的9条缺口追溯同批次A2输出，确认字段已存在、关联链路缺失。

第二轮复查：检验是否错误放宽条件。67只候选的修复前分类、均线、MACD、行情对象与价格区间哈希、首要及全部阻断条件、原因码全部复现后冻结输入。阶段实际计算时间为 `2026-09-07T21:33:21.246505+08:00`；不能误用原始市场快照的20:04时间，虽均线不变，时间字段会改变对象哈希。

冻结对照输入哈希：`dcc6afa5d8229fdc45a249e8b12ef460935d117100483b51db26992a4f81f3fc`。虚拟机隔离目录：`state/a3-handoff-20260907/`。

修复后，全部67只的行情与价格对象哈希保持相同。前置候选23→24：趋势五日线21、520两只不变，恢复播恩集团1只龙头候选。原9条缺口分解为：1只进入候选、5只继续观察、3只因技术条件不满足被排除。没有将首板观察、日线走弱等情况整体放行。

相关测试205项通过，包含8项新增交接回归、既有A3策略与量化流水线及研究编排回归。上述数量是量化候选，最终模型审核与生产发布必须分别验收。

## 剩余观察

A2本轮67只整体审核约599秒、累计11次底层尝试、2轮语义校验才通过；没有将67只缩减为3只，但调用耗时与重试偏高，不能仅凭本次通过宣称模型服务稳定性问题全部解决。消费电子14只经过量化扫描但未满足行为确认，观察分流应如实展示。
