# 9月14日盘后问题处理与验收

范围：修复已证实的工程、数据交接和复盘展示缺陷，不调整策略阈值，不补发历史交易信号。原研究、模型输出、决策与失败通知均保留。

## 已复现与执行顺序

1. 520预热：三计划需要近三交易日144根五分钟线，代码请求360根，超过腾讯320根上限，直接走通达信。虚拟机实测腾讯对000538请求144根，返回9月10日至14日完整144根。改为按实际所需根数请求，仍逐时间戳验收，不降低覆盖要求。
2. 东财前100：条件选股端返回100只独立股票，但两个28名、缺29名；不能自行重排。股吧原榜接口实测返回完整1—100名。增加同源原榜核对/后备路径，逐代码绑定当日条件选股事实并保留来源，日期或成员不一致仍拒绝；不伪装15:10历史采集。
3. A5证据：今日计划来自9月11日正式批次，其文件120456166字节，实际A2为8聚焦、70观察。64MiB读取上限误把存在的数据当缺失。使用有界流式读取目标阶段，避免简单全量载入大文件；保留文件大小、来源身份校验。
4. A5表述：冻结事实是32个趋势计划，模型写成32个520。策略计数及摘要由服务端冻结事实生成，不把模型文字当计数依据；风险意见保留。
5. 飞书：午间复盘网络错误未重试，盘后成功。对网络和超时增加一次有界重试；只对未送达的复盘/运维消息处理恢复，不补发历史交易信号。
6. T+N：16:10任务实际完成部分价格补齐，但仍有当日到期缺口，需区分每轮请求配额、真实接口缺失和无交易日证据。只补当前观察窗口，历史缺口单独保留，不写零收益掩盖缺项。

## 两轮审查与验收要求

- 数据审查：保留东财原榜排名；不合并不同时点的排名凑100；每只520逐根校验交易日与时间；不伪造停牌价格。
- 执行审查：修复不改变A3资格或A4门槛；部署前排除关键任务，使用deploy.sh；重算研究与计划发布分开留证。失败归档不覆盖，新复盘独立版本并注明纠正。
- 本地针对性回归后，虚拟机核验安装代码与源码一致、三计划144/144、同日东财100/100、A5读取8+70及正确策略计数、飞书投递与T+N当前到期缺口。未实际完成的项目不得标为通过。

## 已完成的生产验收

- 修复已通过 `deploy.sh` 安装到虚拟机，当前代码提交 `b85bc5a`；8个相关已安装模块与仓库源码逐字节一致。没有修改模型配置或A4交易门槛。
- 本地针对本次修改的10组测试全部通过，共164项；午间及盘后完整冻结生产样本也通过上下文容量验收。部署后的Node调度器启动日志和3210健康接口正常，次日自然调度仍需到时验收。
- 三个520计划（000538、002518、300562）均为144/144根、缺口0，目标交易日9月15日。恢复凭据为 `outputs/recovery/post-close-20260914-indicator-repaired.json`，原失败凭据保留。
- 东财正式缓存已取得9月14日100只，原榜排名1—100完整，原榜首末记录时间16:54；采集时间16:58:27。没有把这份盘后恢复数据冒充15:10输入。
- 流式读取原120456166字节研究文件成功，恢复A2聚焦8、观察70、轮动之外1542，A3筛选78，A1追溯4010；实测耗时19.18秒。保留全量候选与A1追溯字段，不加载重复大证据图。
- T+N补跑退出码0、当前观察任务完成：4831只跟踪范围，本轮1370次请求、1362只补齐、延后请求0、未解释缺口0。8只当日无日线均取得腾讯无成交证据，不伪造K线。今日到期A1为4004/4007、A2为2446/2447、A3为78/78；剩余到期缺项对应600301、601238、605303的无成交观察。旧历史缺口仍保留。

## 复盘恢复过程中发现并修复的次生缺陷

1. 完整A2证据恢复后，A5上下文原为273772字符，超过250000上限。候选证据编号按板块池与代码可逆派生，重复字符串使用带转义的可逆字典，未删候选、原因、有效事件或未比较字段。最终冻结样本：盘后197638字符、午间233859字符，均通过。
2. 模型把“均线复算32/32”误写为“通达信核价100%通过”。A3策略计数、均线、MACD、路径、价格层级、跨源价格的聚合结果改由冻结记录逐项生成：本日均线及MACD32/32，跨源核价0/32，不把无比较样本当通过；旧版本保留，更正版重新发布。
3. 午间模型把资料补充任务返回成带原因和证据编号的对象。增加已观察结构的无损兼容；每个证据编号必须属于本次冻结输入，未知字段或伪造编号仍拒绝。用完全相同提示词、输入及输出哈希复验已归档响应，未追加模型调用。

最终复盘及飞书记录：

- 盘后：复盘 `195d2e6e-c6ec-5fc5-954b-20b03d8b5bc0`，投递 `83972234-df79-5310-b289-68b11de597d2`，成功。
- 午间：复盘 `f56f4c4e-2af2-5a76-b0d7-7c754b9b1d06`，投递 `c98e7c7c-bf1f-5e6f-8cd5-7fe5ace71fda`，成功。截止时间仍为11:30，不混用盘后表现作为午间决策。
- 两份复盘均如实标注证据受限。正式计划来源仍为9月11日批次，不混入本日盘后为9月15日生成的计划。

## 仍存在的外部限制

四个配置通达信节点及三个代码内备用节点均未取得有效协议行情。腾讯当前取数恢复不等于通达信恢复，也不构成独立双源核验。KDJ、成交量的独立复算尚不能宣布齐全。今日10:10一次腾讯失败和15:00六个收盘未定稿状态仍保留原判断，不事后补发历史交易信号。

## A2→A3更新

新批次 `2026-09-14-close-hot100-repaired-165854` 已完成，耗时1599秒。复用封存A1并形成1519只当前有效研究范围，A2为12只聚焦、76只观察。A3本地评价88只，25只通过技术资格、63只过滤；模型分16+9两批完成，25只全部保留，无模型遗漏。

- 25个正式次日计划：22个趋势五日线、3个520；均为9月15日、等待盘前审核，未当日激活。新批次三份520历史全部144/144。
- 原15:47发布的24个次日计划已全部失效，不与新批次重复执行。今天32个执行计划仍为18个失效、14个到期，未被新研究改写。
- A2在A1之外数量0；A3在A2之外数量0。完整验收凭据为 `outputs/recovery/post-close-20260914-final-acceptance.json`。
- 情绪与趋势数据源均标记当日可用。本轮没有龙头计划并非继续取不到东财100：量化识别37只情绪股票，30只被既有情绪周期“禁止新开仓”规则拦截，另有单股主题证据等资格缺口。没有为凑三策略数量调整规则。5只单股证据缺项继续显式留在审计中，不宣称全市场映射无缺口。
