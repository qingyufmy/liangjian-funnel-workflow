# 9月16日上午“新增仓失败”提醒：复盘与补修

核验时间：2026-09-16 12:57起；证据截止11:31，交易判断覆盖至11:30。
VM：`aurum-vm`，运行目录 `/www/wwwroot/Agu/liangjian-funnel-workflow`。
生产提交仍为 `aa91997c04abcd68120e30e589683ad568351477`；上午本地修复未部署。

## 1. 结论

用户所述提醒在生产通知账本对应 **“A4新增仓数据阻断”**，不是买入委托失败。
这一提醒由行情源健康检查触发，所有23次汇总阻断均为 `EXECUTION_NATIVE_5M_CONFLICT`。
本次没有发现另一类模型、模拟下单或成交失败；上午没有买入信号、没有新增模拟委托、没有模拟成交。
没有成功成交可供失败归因，更不能把这些通知当成23次买入失败。

提醒仍大量出现的直接原因：盘中生产仍使用旧代码；上午的晚时点复核、逐分钟审计和通知去重仅在本地完成，按用户要求盘后统一发布。

## 2. 完整上午证据

只读工具：`scripts/audit_a4_entry_blocks_readonly.py`。
本地证据：`outputs/a4_entry_blocks_morning_20260916.json`。
SQL连接使用只读模式；冻结行情解压后逐份验证SHA-256；不调用外部模型、不写生产、不生成回放订单。

| 指标 | 实测 |
| --- | --- |
| 行情阻断汇总 | 23条，09:40至11:30的五分钟边界 |
| 恢复汇总 | 22条，随后一分钟 |
| 个股数据提醒 | 21条 |
| 相关通知总数 | 66条；均记录SENT，通知成功不等于买入成功 |
| 受影响股票×分钟 | 230；每轮6—14只，不是230只不同股票 |
| 实际留存DATA_BLOCK事件 | 21条；旧去重键抑制了209条后续分钟观测 |
| 下一分钟原冲突核对 | 222个无实质冲突，保留竞价/精度等已知口径限制；8个11:30窗口在截止前无下一分钟 |
| 买入信号 / 新模拟委托 / 模拟成交 | 0 / 0 / 0 |

对230个受阻窗口，使用各自原冻结输入执行生产策略纯函数：

- 220个为策略确认等待，没有买点通过。
- 10个为09:40的 `NO_CLOSED_15M`，尚无已闭合15分钟线。

这是隔离的原始输入诊断，不是全链路状态化重放；没有补充完整模型、历史预热、持仓、订单状态，因此不把它宣传成“证明没有任何潜在漏买”。下一分钟数据仅用于解释版本收敛，绝不拿来批准上一分钟买点。
上午实际正常等待中较多的是15分钟卖压未缓解、回调缩量条件未满足；这些不是接口故障，不因没有成交而放宽。

## 3. 本轮追加修复

### 通知明确区分数据、信号、订单

- 阻断标题改为：`A4行情校验未通过｜暂停新增仓`。
- 明示：`行情质量提醒，不是买入信号或委托失败回报`。
- 恢复卡明确：仅代表行情校验恢复，不代表买点成立或委托成交，不补发历史信号。
- 通知账本增加 `event_category=MARKET_DATA_QUALITY` 和 `order_outcome=NOT_AN_ORDER_RESULT`。
- 真实委托拒绝、成交失败等原有通道没有被屏蔽；行情首次待确认、持续阻断、恢复仍按上一批各自规则处理。

### A5兼容当日旧账本

旧代码为发送告警将DATA_BLOCK标成effective，容易让“有效事件数”看起来像交易成果。
新A5在读取时将其归为非交易观测，保留 `recorded_effective` 原始标记；原数据库、原事件、原通知均不改写。
原有买入信号、离场、减仓、计划失效的分类保持不变。
源健康证据明确标注“不是委托失败”，避免模型把基础设施阻断解释成订单执行失败。

## 4. 验收及发布

- 本轮定向回归：91项通过，覆盖行情确认、告警分类、恢复语义、旧DATA_BLOCK归类、原账本不变、A5、通知与持仓数据风险。
- 全量最终回归：`python -m pytest -o addopts='' -q`，**1,676 passed in 110.55s**，包含上午A1–A3及A4待发布修改和本轮补修。
- 生产没有部署、重启、改活动计划、发送测试飞书或补跑历史信号。
- 并入 `A4_PUBLICATION_REPAIR_ACCEPTANCE_2026-09-16.md` 的今日盘后批次，沿用既有自动任务和 `deploy.sh`。
- 发布后需核验新卡片语义、自然窗口下去重与恢复，以及A5不再把数据阻断计入有效业务信号。测试通过不等于这些生产验收已完成。
