# 9月14日竞价后刷新故障修复

## 事实与根因

生产版本 `8f983ba` 于 09:26:01 启动刷新，09:37:26 因 `MARKET_EMOTION_FACTS_NOT_READY` 阻断。尚未运行 A2/A3 模型。当天板块及东财前100均已成功采集。

只读接口复现：9月11日涨停40、跌停21、炸板18条，均完整；连板梯队返回30日，首日是9月14日、第二日是9月11日。旧代码要求首日必须等于上一完整交易日，因此把实际存在的9月11日数据整组拒绝。接口复查是盘中当前返回，不宣称保存了09:26每个接口的原始响应；但它确实复现了同一代码错误。

耗时问题：完整概念映射阶段从启动后341秒到684秒，约343秒。行业和概念成员缓存按日命名，盘前此前不会读取上周五的完整版本；行业历史也使用今天的缓存键。

## 修改

1. 连板梯队按行内日期及窗口日期双重校验，要求目标日真实存在，日期无重复且窗口与数据行一致。仅保留目标日及以前的数据，排除今日未完成部分。缺日、格式异常或接口不完整仍拒绝。
2. 剔除目标日个股的次日封板和标记字段，避免接口后来补写的次日表现进入已收盘背景；保留更早日期已经完成的晋级数据。不改原始返回对象。
3. 盘前行业、概念图允许复用近7个日历日的本地完整版本，必须满足目录哈希、内容校验和、完整标记及非未来采集时间。保留实际版本日期和采集时间，不伪造当日新采集；失效、变更或缺失则重新采集。普通盘后刷新默认行为不变。
4. 盘前行业日线历史明确读取上一完整交易日15:10的历史版本，避免重复拉取90个行业，同时不接入今日未收盘日线。
5. 四组情绪基础数据在行业历史/概念图读取前立即校验。保存逐源状态、失败代码、请求目标日、实际窗口首日、数量及采集时间，并随失败回执保留，不再只剩总括错误。

## 两轮审查

- 日期审查：不能简单删除日期校验，也不能把今天梯队改成上周五。已采用行级截取，并排除目标日的次日字段；真实返回验证保留29日、剔除9月14日。
- 缓存审查：不能不校验地读取任意旧图或复制为当天文件。复用范围最多7日，目录和内容哈希必须一致，观察时间不得晚于研究时间；没有合格版本则不声称缓存命中。

## 验收及发布边界

已通过故障重现、完整窗口投影、目标日缺失/重复/格式异常、接口不完整、前视字段、两类成员图跨周末复用、哈希损坏/目录变更/过期/未来缓存等测试。另用生产接口当前真实梯队响应在本地修复函数中验证：输入首日9月14日，输出首日9月11日，29日完整，通过。

修复完成时原先暂缓盘中部署。用户随后明确要求“直接部署，这次不要拦截，然后直接重跑A2-A3”，因此本轮执行一次显式盘中放行；默认部署时间保护仍保留，不覆盖09:26的失败回执。

## 生产部署与手动重跑记录

- 2026年9月14日11:21，通过项目 `deploy.sh --allow-intraday` 部署到 `192.168.31.254:/www/wwwroot/Agu/liangjian-funnel-workflow`。部署版本 `02d93f51291444dab2537bc0b74690ffbfc413eb`，Python重新安装、前端构建和3210端口健康检查通过。相关Python测试110项通过。
- 11:24:37，以 `www` 用户执行 `run-auction-refresh --manual-current`，研究批次 `2026-09-14-manual-current-refresh-112437-research`。复用封存A1，不新增或激活A4执行计划；数据使用真实当前时点，不回填伪装为09:26。
- 实际新采集的板块及东财前100快照时间均为11:24:37。四组情绪基础数据校验全部通过：9月11日涨停40、跌停21、炸板18，连板梯队29日；原接口包含9月14日，投影后只保留9月11日及更早数据。
- 行业历史90项直接读取缓存；概念成员图阶段从运行第71秒到第73秒。后续个股日线、财务和公告同步及模型耗时单独统计，不将这2秒冒称完整A2/A3耗时。
- 只读查询确认11:19至11:26每个分钟均有18条A4判断记录，包括部署所在的11:21。11:25自然分钟任务成功，耗时9.2秒。磁盘79GB、剩余约18GB。
- 已核对刷新入口、引用缓存、情绪源适配和工作流四个模块，安装包文件与部署源码SHA256一致。11:30自然A4任务成功，11:35午间A5按时启动；启动不等于A5已完成。
- 本轮数据入口量化前五板块为医药生物、电力设备、非金属材料、数字经济、商贸零售，仅为11:24数据快照排序，不提前冒称最终A2模型结论。医药生物资金覆盖使用成员数量分母并明确降级标记，未宣称所有资金字段完整。
- 个股日线与财务阶段约第327秒完成：1628只、1525个缓存命中、103个缓存未命中、4只取数缺项。第338秒进入全池公告同步；它仍是单独的耗时项，不因复用A1而自动跳过。

## 本轮最终结果：部署完成，A2通过，A3未通过

11:24:37启动，12:24:49结束，约60分钟。批次最终为 `BLOCKED`，不能称A2/A3完整成功。原09:26失败回执未覆盖；本次另有手动运行回执。

- A1：没有调用模型重新筛选；复用正式1569只，叠加本次东财前100中新增59只，日度有效研究范围1628只。正式月度池和每日情绪补充明确分开。
- A2：量化从1628只中送审110只，最终聚焦10、观察100、轮动池外1518；模型2次调用/修正，记录耗时89.452秒。A2的110只均属于日度有效A1范围。9只情绪补充股票存在主题/主营绑定缺项，保留逐股原因，不能声称全市场证据无缺口。
- A3：量化输入110只，送模型48只、观察54只、淘汰8只。计划拆成3批，每批16只；第1批未通过，已完成批次数为0。48只是量化送审候选，不是最终合格A3股票池。
- A3失败链：模型曾返回16只核心候选，但验证报 `A3_FACTOR_SNAPSHOT_NOT_READY`；3次语义尝试后耗尽600秒模型调用总时限，最终记录 `A3_BATCH_BLOCKED:MODEL_WALL_CLOCK_TIMEOUT`。不是单纯没有接口响应，也不是量化选不出票。
- 只读复算110只A3输入：109只的大周期因子满足准备条件，仅嘉立创（001232.SZ）不满足。该股本地不复权日线共29根（交易日期8月4日至9月11日），缺月/周/日正式样本及日线MA60。该复算使用12:13可知数据，不回填或覆盖原始研究。不能仅凭本地29根断言该公司只上市29天；仍需检查源历史覆盖。
- 文档最终统计2198份、可用2163份、不可用35份。公告查询阶段约第338至2516秒，是本次耗时主体之一；不能将“行业与概念映射命中缓存”扩张成全流程快速刷新已达标。
- 执行发布回执明确 `created=[]`、`activated=[]`，盘中A4仍用既有计划。11:35午间A5已于11:38:49自然执行成功；不将该任务成功等同于本次A3成功。

### 本轮发现、尚未修复的后续项

1. 核对嘉立创源端历史：若历史实际存在，应有界补齐并验证；若真实不足，则应在A3模型前按单股数据缺口分流，不能让模型反复修正文案以替代缺失技术数据。
2. A3量化送审契约应与最终技术数据验收保持一致，且单股缺项不能无差别拖垮整个批次；不可直接移除大周期或MA60验证来放行。
3. 公告全池刷新需要独立维护缓存与增量路径，保持真实更新时间及来源，降低竞价刷新关键路径耗时。
4. 下一交易日自然竞价刷新尚未验收；此次是明确授权的盘中手动运行，不能替代自然任务证明。

本轮仅部署已测试修复并执行所请求的重跑；以上新增A3契约和性能问题未在生产中临时改规则放行。未补发历史交易信号、未自动启用研究候选。

## 用户要求修复后的第二轮迭代

- 已将A3月/周/日技术数据契约提取为共享函数，模型前量化门控和最终输出校验调用同一实现。数据不足按单股记录 `DATA_GAP`，不送模型；没有放宽大周期、MA60条件，也不增加盘中行情依赖。
- 缺口记录保留因子准备状态、具体缺失项及“不依赖盘中数据”标志。正常股票仍继续送审，单股缺口不能再消耗整个批次的语义修正预算。
- 上游接口追加只读核验：请求嘉立创近800天不复权日线，接口返回完整成功，但仍只有29根，和本地一致。当前来源不能提供更早历史，不伪造补齐。
- 新增 `scripts/recover_a3_research.py`：校验原快照哈希、已通过A2的审计记录哈希，复用原A2输入关系，以当时可知日线重算A3。仅写入独立 `outputs/recovery` 验证文件，不更新主结果指针、不创建或激活A4计划、不重复抓取全市场公告。支持 `--quant-only` 先验收量化分流。
- 两轮审查：第一轮核对与原最终校验完全同口径，避免只把错误推迟到下游；第二轮核对恢复入口不加载实时新闻、不覆盖旧失败、不更改执行计划，并为恢复证据建立独立哈希。

恢复结果及部署验证在执行后补充。公告全池耗时优化不属于本轮单股缺口修复，仍单独列为待优化项。
