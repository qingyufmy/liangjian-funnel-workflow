# 9月14日剩余问题实施与验收

## 本轮改动

- 通达信连接改为独立 `tdxpy` 客户端。原 `StdQuotes` 构造会修改进程全局BESTIP并写入用户配置，不适合并发节点身份绑定；现在检查连接结果、显式证券市场、独立关闭连接。
- A4腾讯优先，短暂请求失败最多补一次重试；格式/身份错误不重试。分钟批次共享20秒准入预算，单个真实适配器超时压到2.5秒以内；超过预算的结果不进入判断。执行中的底层I/O仍需返回，20秒不是操作系统强制杀线程时限；故障期间不会无限叠加多节点重试。
- 通达信后备每次最多一个节点、两页，失败节点冷却60秒；健康账本跨分钟进程保存，避免每只股票重试整个坏节点池。并发节点预留互斥，冷却到期再探测。
- 15:00原始窗口保守标为待确认，不将非零成交量等同定稿。盘后A5前单独采集当日执行计划的1分钟/5分钟窗口，最多三次、连续两份一致才标“观测稳定”，不冒充供应商官方定稿；只写行情版本，不创建或替换冻结决策。
- 520的KDJ、15分钟MACD和三策略共同的5分钟量能中位数输入可按哈希去重归档，A5校验证券、周期、参数、截止时间与公式。适用指标未声明记缺失，不适用及尚未可评价分开统计。量能基线复算不等于所有组合买卖条件均独立验证。
- 新增聚合分钟取数故障/恢复告警，同一故障不逐计划、逐分钟刷屏；收盘待定稿不伪装全计划策略失败。
- 情绪研究资格与开仓权限拆开；情绪周期禁入的合格候选仍可交给模型研究，但保留服务器执行禁令。A3继承限制，发布时同时核对A3标识、A2审计记录、上下文及冻结情绪状态，不能由模型删除字段绕过。
- 多主题映射保留有证据的候选路线，不任意选唯一主题；未完成题材选择仍仅研究。月度A1主体不重写，日度补充成员重新绑定当次冻结目录。
- 通达信探针将未配置付费核验源标为独立核验未配置，而非免费源取数失败。

## 冻结样本验证

源：`snapshot-20260914T170300+0800-5153a2420231`，原研究 `2026-09-14-close-hot100-repaired-165854`。未调用模型，未发布计划。

- A1有效范围1519；A2量化研究入口123：88趋势、35情绪。
- 原12聚焦+76观察的88只与新趋势研究集合完全相同：新增趋势0、遗失趋势0。
- 新增35只情绪候选全部禁止执行，A2在A1之外数量0。
- 000839、000981、600088、600330、605069均为有成员证据的多主题匹配，分别有2、4、2、2、6条候选路线；全部进入受限研究，未伪造主营占比或题材结论。
- 全部冻结成员目录5571只核对：4617唯一策略归属、914多归属、40未匹配当前策略主题。后者不等于40只全部数据缺失，也不能为凑覆盖量强制加入策略主题。
- 详细凭据：`docs/evidence/remaining-20260914-frozen-a2.json`。

## 测试

第一轮相关20组测试341项通过。之后补强发布防绕过及提示词，针对性105项通过。覆盖短暂失败重试、过期响应丢弃、旧日窗口拒绝、节点隔离和冷却、指标去重与身份校验、原决策不被盘后归档覆盖、研究禁止执行、告警去重与恢复、现有T+1及T+N流程。

T+N已具备当前无成交观察和历史缺口分层，本轮相关回归通过，没有为了消除数据限制重写历史判断或填零收益。

## 外部限制与实施偏差

- Windows与VM的两个代表性通达信节点均失败；东方财富分钟接口两端均为协议断连。客户端隔离缺陷已修，但不能据此宣称免费第二源已恢复。
- 盘后定稿采集复用盘后A5入口，并未新增15:00后秒级调度；这是本轮的简化落地。若要15:02前后自动收口，还需扩展独立调度入口，不能把当前实现表述为已完成秒级补采。
- 情绪123只是量化研究输入，不是模型最终A2结果；5只多归属暂未完成主导题材论证，仍明确禁止执行。
- 本轮没有补发历史信号。明日自然任务只能到时验收，测试通过不代表已经经历明日交易。

## 生产验证

待部署后补充运行提交、安装模块一致性、新研究批次、盘后归档及计划发布检查。保留当前25个9月15日计划，只有新批次完整通过后才按原子发布流程替换。
