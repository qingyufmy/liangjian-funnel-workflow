# 9月14日剩余问题实施与验收

## 本轮改动

- 通达信连接改为独立 `tdxpy` 客户端。原 `StdQuotes` 构造会修改进程全局BESTIP并写入用户配置，不适合并发节点身份绑定；现在检查连接结果、显式证券市场、独立关闭连接。
- A4腾讯优先，短暂请求失败最多补一次重试；格式/身份错误不重试。分钟批次共享20秒准入预算，单个真实适配器超时压到2.5秒以内；超过预算的结果不进入判断。执行中的底层I/O仍需返回，20秒不是操作系统强制杀线程时限；故障期间不会无限叠加多节点重试。
- 通达信后备每次最多一个节点、两页，失败节点冷却60秒；健康账本跨分钟进程保存，避免每只股票重试整个坏节点池。并发节点预留互斥，冷却到期再探测。
- 15:00原始窗口保守标为待确认，不将非零成交量等同定稿。盘后A5前单独采集当日执行计划的1分钟/5分钟窗口，最多三次、连续两份一致才标“观测稳定”，不冒充供应商官方定稿；只写行情版本，不创建或替换冻结决策。
- 520的KDJ、15分钟MACD和三策略共同的5分钟量能中位数输入可按哈希去重归档，A5校验证券、周期、参数、截止时间与公式。适用指标未声明记缺失，不适用及尚未可评价分开统计。量能基线复算不等于所有组合买卖条件均独立验证。
- 新增聚合分钟取数故障/恢复告警，同一故障不逐计划、逐分钟刷屏；收盘待定稿不伪装全计划策略失败。
- 情绪研究资格与开仓权限拆开；情绪周期禁入的合格候选仍可交给模型研究，但保留服务器执行禁令。A3继承限制，发布时同时核对A3标识、A2审计记录、上下文及冻结情绪状态，不能由模型删除字段绕过。
- 多主题映射保留有证据的候选路线，不任意选唯一主题；未完成题材选择仍仅研究。月度A1主体不重写，日度补充成员重新绑定当次冻结目录。
- 通达信探针将未配置付费核验源标为独立核验未配置，而非免费源取数失败。

## 冻结样本验证

源：`snapshot-20260914T170300+0800-5153a2420231`，原研究 `2026-09-14-close-hot100-repaired-165854`。未调用模型，未发布计划。

- A1有效范围1519；A2量化研究入口123：88趋势、35情绪。
- 原12聚焦+76观察的88只与新趋势研究集合完全相同：新增趋势0、遗失趋势0。
- 新增35只情绪候选全部禁止执行，A2在A1之外数量0。
- 000839、000981、600088、600330、605069均为有成员证据的多主题匹配，分别有2、4、2、2、6条候选路线；全部进入受限研究，未伪造主营占比或题材结论。
- 全部冻结成员目录5571只核对：4617唯一策略归属、914多归属、40未匹配当前策略主题。后者不等于40只全部数据缺失，也不能为凑覆盖量强制加入策略主题。
- 详细凭据：`docs/evidence/remaining-20260914-frozen-a2.json`。

## 测试

第一轮相关20组测试341项通过。之后补强发布防绕过及提示词，针对性105项通过。覆盖短暂失败重试、过期响应丢弃、旧日窗口拒绝、节点隔离和冷却、指标去重与身份校验、原决策不被盘后归档覆盖、研究禁止执行、告警去重与恢复、现有T+1及T+N流程。

随后发现并修复两处链路遗漏：压缩模型输入必须携带执行限制（43项针对性测试通过）；聚合源故障告警之外，逐股数据阻断卡片也必须去重抑制（36项针对性测试通过）。以上批次包含重复用例，不相加宣称独立测试总数。

T+N已具备当前无成交观察和历史缺口分层，本轮相关回归通过，没有为了消除数据限制重写历史判断或填零收益。

## 外部限制与实施偏差

- Windows与VM的两个代表性通达信节点均失败；东方财富分钟接口两端均为协议断连。客户端隔离缺陷已修，但不能据此宣称免费第二源已恢复。
- 盘后定稿采集复用盘后A5入口，并未新增15:00后秒级调度；这是本轮的简化落地。若要15:02前后自动收口，还需扩展独立调度入口，不能把当前实现表述为已完成秒级补采。
- 123只是A2量化研究输入总数，其中情绪35只，不是模型最终A2结果；5只多归属暂未完成主导题材论证，仍明确禁止执行。
- 本轮没有补发历史信号。明日自然任务只能到时验收，测试通过不代表已经经历明日交易。

## 生产验证

- 已依次推送并通过现有 `deploy.sh` 部署 `062c1c9`、`1eeb289`、`cb05cf1`。最后一个为本轮运行代码版本；第一批15个Python模块、后续改变模块的安装内容与仓库文件一致。服务健康检查通过。
- 18:08左右完成当日32只执行计划的盘后采集：64组1分钟/5分钟窗口全部连续两次观测稳定。凭据：`outputs/close_finalization/2026-09-14/eec7f21d51d0468a720cb43a126acfd9c8183cbb87f02c1537425bb380c99418.json`。不是供应商官方定稿，不修改原冻结判断。
- 新研究批次 `2026-09-14-close-research-permission-final-181241` 使用 `cb05cf1`，复用封存A1、重新采集当日数据。原18:09批次在模型开始前由本次操作主动取消，以应用压缩提示词字段修复；取消凭据保留在 `outputs/recovery/remaining-repair-cancelled-180900.json`，没有发布计划。
- 新批次采集发现巨潮近期公告请求HTTP 403：最终快照记录1519只近期公告查询失败、3只主营公告查询失败，公告查询事实覆盖率0。公告查询缓存有命中，不等于取得新增报告；请求失败不改写为“无公告”或缓存更新成功。此次特征物化的结构化主营记录数为0，也不能据此宣称封存A1中的原始披露依据被删除或重新验证。本轮复用9月11日封存A1，未重新执行A1模型。
- 总览接口8秒客户端等待会超时，25秒等待实测返回200、耗时10.24秒。独立计时状态读取7.15秒、研究文件读取及脱敏0.68秒。状态剖析主要耗时在事实库覆盖统计与重复A1校验；服务健康接口可响应。本轮仅按计划定位，不改查询结构或隐藏数据质量状态。
- 原有每日盘后自动化暂增加9月15日09:35、11:35、16:35只读验收，末次恢复原15:35日程；盘中不发布代码或替换A4计划。自然任务尚未发生，不能提前宣称验收成功。
- 新研究在约34分45秒后完成，研究契约状态READY；该状态不代表所有外部数据完整。行情及人气数据日期均为9月14日，人气100使用16:58:27同日缓存，保留真实采集时间；未冒充18:30新采集。

## 新A2/A3结果与最终发布

批次：`2026-09-14-close-research-permission-final-181241`。快照：`snapshot-20260914T183000+0800-72257d014f07`。

| 层级 | 数量与验收 |
| --- | --- |
| 有效A1 | 1519只：1463只半年报基本面路径、56只日度情绪补充路径。不是将1519全部称为月度基本面新入选。 |
| A2 | 聚焦5、观察118，共123只；趋势88、情绪35。模型完成全量123只分析，未因聚焦数少截断后续技术筛选。 |
| A3量化预筛 | 对123只执行：25只合格、44只观察、54只不符合前置。 |
| A3最终产物 | 核心计划25、次级观察0、未形成计划98。原44只量化观察在最终输出中归入未形成计划，不能将98只全部称为技术硬拒绝。 |
| 次日正式计划 | 25个，目标9月15日，全部待盘前审核、未激活；22个趋势五日线、3个520、0个龙头。 |

- A2在有效A1之外0只，A3核心在A2之外0只；A2、A3核心无重复；执行计划在A3核心之外0只。
- 35只情绪受限股票全部保留在A2观察，进入A3技术筛选后均未形成计划；原因含题材分化/退潮、非早期阶段、首板仅观察等。5只多主题股票仍缺主导题材阶段，不能因已有多条成员关系就宣布完成题材定性。
- 35只受限股票发布执行计划数为0；没有通过删除情绪风控、降低技术门槛或强制增加龙头数量凑结果。
- 25个新计划的来源批次均为本次新运行；旧25个待审计划已失效，连同更早24个旧版本共49个失效记录保留，当前没有多批待审计划并存。
- 3个520计划：云南白药、科士达、乐心股份，历史五分钟数据均144/144、缺口0、目标交易日9月15日。
- 自建量化轮动前5为培育钻石、医药生物、数字经济、电力设备、商贸零售。此为本项目自有评分，不能冒充截图软件的原始强度。医药生物资金覆盖采用成员数口径，缺成交额分母，降级标记保留。
- 本轮没有重写9月14日A4原始判断或补发买卖信号；A5新指标归档与次日自然任务尚待实际交易时段验证，不能以盘后代码测试代替自然运行证据。

主要产物位于VM：`outputs/runs/<批次>.json`、`outputs/research/research_<批次>_lane_1.json`、`outputs/runs/<批次>-indicator-preparation.json`。
