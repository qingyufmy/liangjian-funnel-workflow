# 控制台事实口径修复与验收

## 范围

本轮落实对比报告中五项已复现的问题，以及关联的错误缺字段提示。修复只读投影、索引、中文和页面组织，不修改 A1–A4 策略、模型提示词、生产研究指针或计划。完整图表式单股档案、轮动研究页和策略绩效页属于后续产品迭代，不将它们冒充本轮已完成能力。

## 生产证据

2026-09-07 22:21 北京时间，虚拟机版本 db066f6，生产批次 `2026-09-07-close-34ab6b52c8d2`。A3 outcome 明确输入 49，但明细索引输入 5；A1 outcome 输入 4164，明细却显示 738。隆平高科正式计划已发布为待盘前复核，实际到期为 9 月 8 日 15:00，研究行引用的 9 月 7 日 15:00 并非正式有效期。最新可见 A5 记录仍为 9 月 4 日，最近飞书成功为 A4 消息，不能证明当日 A5 成功。通达信能力探针时间为 8 月 30 日。

## 实施步骤与验收标准

1. 统一阶段输入：优先本阶段 outcome 的 input，旧明细索引读取时从同批次 lane 修正；新索引保留 outcome。不以 A2 聚焦数代替 A3 实际输入，不用结果数补造输入。
2. 中文词典保留技术含义，均线按周期解释，未知标识明确待补充；原始诊断标识可识别，不再抹成“系统内部状态”。
3. 研究详情关联同运行、同模型、同股票的真实发布记录，显示状态与正式有效期；无匹配、歧义和状态不可读分别说明，绝不按同股票跨批次拼接。
4. 服务、研究、A3 发布、盘中观察、当日 A5 与对应飞书投递分开；过往成功不能替代今日成功。服务绿灯只代表控制台可读。
5. 能力探针明确为历史检测；独立显示当前研究批次的数据充分性、覆盖字段、缺口与统计边界，缺失指标显示未提供。
6. 回归索引和旧格式、未知术语、跨日/跨批次计划、缺失/失败状态；完成类型检查、构建、浏览器桌面与窄屏验收后执行原 deploy.sh，核对提交、健康、线上接口与实际页面。

## 两轮设计复核

第一轮：不能只改弹窗为前端数字，索引也需修复；不能将研究行 plan_id 存在当作落盘证明。补充索引与真实发布投影。

第二轮：历史状态列表可能有界，查不到不能直接断言“未发布”；缺字段也不能变成新的策略门禁。保留“未核验到”及来源边界，按当前技术契约识别周线事实。

## 验收结果

首轮 72 项 Node/Web 测试、4 项 Python 索引测试、类型检查与生产构建通过；前端机械检查无报告项。5210d3d 已通过 deploy.sh 部署并在生产浏览器核验 A3 输入 49、输出 26，隆平高科正式记录为 9 月 8 日到期、待早盘复核，研究参考日期仍保留为 9 月 7 日。生产集合与计划没有改变。

浏览器复查追加：列表主题绕过词典、均线信息重复、阶段“可行动”容易误读。增加全部策略/轮动主题中文覆盖回归、日周均线表、正式发布限定文字，并补充冻结运行元数据（行情交易日、全市场/研究/采集数量、基本面/主营资料源记录），不把记录数冒充覆盖率。新增测试后共 74 项 Node/Web 测试。

边界仍明确：本轮没有补跑当日 A5；当前窗口没有其完成记录或对应送达记录。缺失的逐字段事实截止不编造；最新能力探针仍显示原检测日期。完整单股 A1–A5 图表档案、轮动视图和按样本性质分层的三策略绩效档案尚未实施。
