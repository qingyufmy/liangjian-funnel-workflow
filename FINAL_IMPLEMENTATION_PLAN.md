# 量见 A 股独立漏斗工作流：最终实施合同

版本：`LIANGJIAN_INDEPENDENT_FUNNEL/1.0.0-draft`  
日期：2026-08-24  
目录：`D:\dev_A股\liangjian_funnel_workflow`  
状态：方案冻结候选；完成两轮复核并通过 P0 实测门禁后方可进入长期模拟运行。

> 本工作流只用于研究和内部虚拟模拟，不连接 GM、券商、掘金模拟盘或真实证券账户，不构成投资建议。

## 1. 已确认的产品决策

1. 工作流与 `D:\dev_A股\liangjian-astock-ai` 并列，原项目只读，不修改原项目代码、数据库、任务和配置。
2. 同花顺金融数据 API 负责证券域、日线、实时快照、竞价、复权事件与基本面；mootdx 通达信公共行情负责沪深 `1m/5m` 原始分钟线；交易规则、公告披露时间和政策原文使用对应官方事实源。
3. 每个交易日只启动两个研究时段：09:25 和 15:10。
4. 三个研究模型各自独立完成 A1 → A2 → A3，不投票、不合池后再回灌、不共享模型结论：
   - `deepseek-v4-pro-0813`
   - `moonshotai/kimi-k3-free`
   - `z-ai/glm-5.3-free`
5. `deepseek-v4-flash-0731` 独立盯盘，每分钟对每个非空模型 lane 各执行一个隔离批次，只有否决权，没有促成权。
6. 四个模型必须开启思考模式。不能证明已启用时，对应模型或盯盘周期失败关闭。
7. 使用内部虚拟账户模拟入场、离场、T+1、费用、滑点与成交，不产生任何外部委托。
8. 最终用户结果为 Markdown；模型原始合同为严格 JSON。
9. 盯盘 Markdown 只保留有效状态变化，不记录每分钟重复的 `NO_ACTION`。
10. V1 关闭 FAST_TRACK。买入候选必须严格满足 `A3 ⊆ A2 ⊆ A1 ⊆ G0`。

## 2. 不可破坏的系统不变量

以下约束由代码和数据库约束执行，不能只依靠提示词：

```text
I01  任何买入候选必须有完整 G0→A1→A2→A3 血缘。
I02  下游只能缩小上游候选集；早盘只能收紧收盘计划。
I03  模型不计算均线、ATR、价格区间、盈亏比、股数、费用或交易规则。
I04  所有数值由确定性代码计算，模型只解释、分类和否决。
I05  只使用已收盘 K 线；未收盘 Bar 永远不能用于结构确认。
I06  同一研究时段在 DATA_BOUND 后只使用一个冻结快照；三个研究 lane 共享它。A4 每分钟使用独立冻结分钟快照。
I07  三个模型使用相同 snapshot/config/prompt 版本，但状态和账户完全隔离。
I08  A4 无权新增候选、放宽计划、改变价格区间或决定股数。
I09  Flash 缺失、超时或输出无效时，不允许新买入。
I10  确定性硬止损和账户风险退出不受 Flash 故障阻塞。
I11  没有已签核交易规则或数据超过 SLA 时，相关动作失败关闭。
I12  模拟账户不能产生负现金、卖空、T+1 违规或重复成交。
I13  配置、提示词、模型、数据快照和规则版本在一次运行中不可变。
I14  任何模型输出必须先过 JSON Schema、子集、枚举、时间和数值只读校验，之后才可发布。
I15  任何外部文本均视为不可信数据，疑似提示词注入必须阻断相关证据。
I16  密钥不得进入源码、命令行参数、日志、Markdown、SQLite 或 Git。
I17  运行结果必须先持久化成功，之后才允许切换 active plan 指针。
I18  所有任务、信号、模拟意图和成交均有幂等键。
I19  无法证明正确时选择不产生新交易，不用估算或回退补齐。
I20  Agent 5 只能生成提案，永远不能自动修改正式配置或提示词。
```

## 3. 总体架构

```text
数据平面
  同花顺金融 API + 交易所/巨潮/政府官方源
      ↓
  原始快照 → 四时间戳 → 一致性校验 → 冻结 SnapshotManifest
      ↓
  G0 门禁 + 确定性因子引擎

研究平面
  同一冻结输入
      ├─ DeepSeek lane: A1 → A2 → A3 → 独立计划
      ├─ Kimi lane:     A1 → A2 → A3 → 独立计划
      └─ GLM lane:      A1 → A2 → A3 → 独立计划
      ↓
  确定性模型比较器（不改变任何模型结果）

盘中平面
  当前有效计划并集 + 三账户现有持仓
      ↓
  每分钟确定性 Trigger Engine
      ↓
  Flash 批量上下文复核（VETO_ONLY）
      ↓
  有效信号

模拟执行平面
  确定性 RiskGovernor → PaperBroker → 三个隔离账户
      ↓
  现金/持仓/信号/成交/权益对账

复盘平面
  确定性结果标签和统计 → Agent 5 提案 → 人工审批 → Shadow
```

## 4. 数据源职责与能力门禁

### 4.1 单一权威源

| 字段 | 权威源 | 校验或补充 | 缺失动作 |
|---|---|---|---|
| 证券列表、行情快照、日线、竞价、财务、估值、指数 | 同花顺金融 API | 内部一致性检查 | 按字段严重度 BLOCK |
| 沪深 1m/5m 原始分钟线 | mootdx 通达信公共行情节点 | 同花顺实时快照校验最新价格 | 节点轮换仍失败、缺 Bar 或价格冲突时 BLOCK |
| 交易日历、停复牌、交易规则、风险状态 | 沪深北交易所官网 | 同花顺仅校验 | 冲突时全局或标的 BLOCK |
| 公告及真实披露时间 | 巨潮/交易所官网 | 同花顺财务值 | 缺披露时间时历史基本面不可用 |
| 政策原文 | 中国政府网和发布部委官网 | 财经媒体只作线索 | 无原文不得作为 T0/T1 事实 |
| 政府采购、中标、招投标 | 官方采购/招标平台 | 公司公告 | 无官方证据不得进入订单传导链 |

禁止字段级自动回退。某字段只能有一个 authority，其他来源只能验证，不能“哪个有值用哪个”，不能取平均值。

### 4.2 P0 实测能力门禁

正式编写 A1–A4 前，先用真实账户完成并保存脱敏能力报告：

```text
CAP01  全市场证券列表字段是否含市场、板块、上市日、状态。
CAP02  实时快照的 timestamp 语义和实测 p50/p95 延迟。
CAP03  mootdx 1m/5m 历史与当日 Bar 是否可取，单页上限、分页规则、节点轮换和空响应语义。
CAP04  15m/120m 由闭合 5m Bar 聚合时，午休、停牌、半日市和跨日边界是否正确。
CAP05  日线能否请求不复权价格。
CAP06  是否提供可版本化的复权因子或除权除息事件。
CAP07  120m MA255 预热所需至少 12,240 根 5m Bar 能否分页取得并落入本地版本化缓存。
CAP08  财务字段是否含真实首次披露时间；若没有，必须接官方披露时间线。
CAP09  集合竞价是否能区分 09:15–09:20 与 09:20–09:25。
CAP10  API 日额度、分钟额度、并发、429 规则和稳定批量大小。
```

若 CAP03、CAP05、CAP06、CAP07 任一失败，A3/A4 不允许伪降级运行；必须停止实现交易计划链，提交数据源缺口报告。mootdx 只按未复权原始分钟价接入，复权仍由同花顺事件流统一推导并版本化。

### 4.3 四时间戳和冻结快照

每个输入值必须携带：

```text
event_time    事件或 Bar 实际发生时间
publish_time  权威源首次发布时点
fetch_time    本机请求获得时点
ingest_time   校验完成并可供系统使用时点
```

`publish_time` 确实不适用于某类实时数据时，必须由字段合同明确其替代语义，不能留空后假装完整。

一次 Agent 调用只接收一个冻结的 `snapshot_id`。快照内记录字段覆盖率、校验结果、数据 checksum、复权因子版本、交易规则版本、行业和概念成分版本。

### 4.4 数据 SLA

| 数据 | 最大 system_lag | 失败行为 |
|---|---:|---|
| 实时报价 | 3 秒 | 买入与模拟撮合 BLOCK；现有持仓产生高优先级事故 |
| 1m Bar | 15 秒 | A4 结构确认 BLOCK |
| 5m Bar/均线 | 30 秒 | A4 买入确认 BLOCK |
| 梯队快照 | 60 秒 | 只能降级/否决，不得促成 |
| 板块/资金流 | 90 秒 | 只能作分钟级否决依据 |
| 日线 | 完整结算前不可用 | A3 不发布下一交易日计划 |

本机时钟与权威时间偏差超过 2 秒时，盘中买入链整体 BLOCK。

## 5. 股票域和确定性门禁 G0

### 5.1 股票域

- `research_universe`：沪、深、北全部在市 A 股，用于研究和统计。
- `tradable_universe_v1`：`SHSE.60xxxx`、`SHSE.68xxxx`、`SZSE.00xxxx`、`SZSE.30xxxx`。
- 北交所初期 `RESEARCH_ONLY`，完成规则、手数、涨跌幅和撮合回归后另行评审。

### 5.2 G0 排除项

退市整理、长期停牌、规则未知、上市不足 60 日、无涨跌幅新股期、历史数据不足、价格或成交量非法、复权版本缺失、关键字段冲突、最低流动性不达标的股票不得进入可交易域。

研究域与交易域分别保存，禁止用交易门禁删除仍有研究价值的公司，也禁止把研究域直接当可买域。

## 6. 运行状态机和原子发布

### 6.1 单模型 lane 状态

```text
CREATED
  → DATA_BOUND
  → A1_RUNNING → A1_VALIDATED
  → A2_RUNNING → A2_VALIDATED
  → A3_RUNNING → A3_VALIDATED
  → READY_TO_PUBLISH
  → PUBLISHED

任意阶段可进入：BLOCKED / FAILED / EXPIRED
```

禁止跳级。只有 `A3_VALIDATED` 才能进入 `READY_TO_PUBLISH`。模型的自然语言“看起来完成”不改变状态，只有验证器可以推进状态。

### 6.2 计划生命周期

```text
收盘完整计划：DRAFT_CLOSE → PENDING_MORNING_REVIEW
次日早盘收紧：PENDING_MORNING_REVIEW → ACTIVE_TODAY
盘中失效：ACTIVE_TODAY → INVALIDATED/CANCELLED/EXPIRED
```

收盘计划不能直接产生次日买入；必须通过当日 09:25/09:26 收紧复核。

### 6.3 原子发布规则

1. 每个 lane 在临时版本中完成全部写入和验证。
2. 三个 lane 各有独立 active pointer，但必须在同一个发布事务批次中切换；它们共用一个最早发布时间和早盘截止时间，确保比较窗口公平。
3. 09:32 前全部完成时，可在 09:32 同时发布成功 lane。
4. 未全部完成时等待到 09:40 统一发布已成功 lane；失败 lane 保持 BLOCKED。
5. 09:40 后才完成的早盘结果视为过期，不得激活当日新增计划。
6. 指针切换使用单个数据库事务。发布失败时仍指向原版本，但原版本没有通过当日早盘复核，因此只能用于持仓风险参考，不能用于新买入。

## 7. 两个研究时段

### 7.1 09:25 早盘：三层只收紧

08:40 可运行确定性数据预取和因子计算，但不调用三个研究模型，不算第三个研究时段。

09:25 创建早盘 run 并采集最后一分钟竞价数据；09:26 形成包含隔夜事实、上一完整收盘因子和最终竞价的单一冻结快照，之后三个独立 lane 才开始模型调用：

- A1：只处理隔夜公告、政策、减持、解禁、停复牌等失效证据；只能保持、降级、移除。
- A2：读取同一冻结快照中的 09:26 最终竞价；只能降低主题阶段、角色、优先级或删除。
- A3：只能取消计划、收紧触发条件、降低风险单位或缩短有效期。
- 任何早盘新增股票、扩大 trigger zone、提高 risk unit 的输出均由验证器拒绝。
- A1–A3 使用原提示词加版本化 `MORNING_TIGHTEN_ONLY` 系统覆盖层；覆盖层只能减少权限，不能改变输出 Schema 或原提示词的硬否决规则。

如果早盘复核未发布，09:32 后仍可继续管理已有虚拟持仓，但禁止所有新买入。

### 7.2 15:10 收盘：完整漏斗

15:10 创建收盘 run 并进入 `DATA_PREPARING`，但此时不冻结、不调用研究模型。日线结算屏障通过后，将 event_time 为 15:00 的收盘事实、结算日线和其他输入一次性冻结并进入 `DATA_BOUND`，之后执行：

```text
G0 → A1 完整增量 → A2 收盘主题聚焦 → A3 次日计划
```

- 同一个任务内部等待日线结算，不新增第三次研究调度；等待期间更新的是待冻结数据，不允许修改已经 DATA_BOUND 的快照。
- 最迟 20:30 仍无完整日线，收盘 run 失败关闭，次日没有新买入计划。
- 每周最后一个交易日的 A1 周度全量重建合并进本次收盘 run。
- 月初和季度复盘同样合并进收盘 run，不额外启动日内研究流水线。

## 8. 三模型独立流水线

### 8.1 公平性合同

三个 lane 必须共享：

- 同一个 `snapshot_id`、`config_hash`、`prompt_hash`、`exchange_rule_snapshot_id`。
- 同一批候选顺序、批大小、字段裁剪策略和 token 上限。
- `temperature=0`；网关支持 seed 时使用相同 seed，并记录是否真实生效。
- 相同阶段截止时间。

禁止共享：模型输出、候选排序、论证、分数、淘汰理由、计划和账户状态。

“每天运行两次”指两个研究时段，而不是只允许两次 HTTP 请求。每个时段内部可以按固定 batch 调用 A1/A2/A3，但所有调用都属于同一个逻辑 research run 并受同一截止时间约束。

每个 batch 调用前先做确定性的 token/字节预算预检；超限时按固定键分批，禁止由模型自行截断候选。每个 lane 的分批边界保持一致。

### 8.2 A1：基本面、政策、产业链

不能把五千只股票逐只交给模型。执行方式：

1. 确定性 G0 和财务质量预筛。
2. 确定性行业/主营映射形成产业链候选节点。
3. 模型分批研究 40–80 个节点。
4. 每节点只复核确定性 Top-N 公司。
5. 分批 JSON 经校验后由确定性合并器汇总，不能让另一个模型重写结果。
6. 输出约 150–300 只 A1 ACTIVE/MONITOR 池。

### 8.3 A2：题材、情绪、龙头中军

- 唯一输入股票域为本 lane 的 A1 ACTIVE。
- 按主题分批，确定性合并。
- 情绪、资金、舆情、拥挤是四套独立字段，禁止复用同一数据冒充。
- `LOW_IDENTITY` 是硬否决维度。
- V1 关闭 FAST_TRACK，池外强信号只写研究提示，不进入任何当日/次日可买池。
- 输出约 30–60 只聚焦池。

### 8.4 A3：技术结构与计划

所有因子由代码预先计算：

- 周/日：MA5/20/60。
- 120m：MA5/20/99/128/255。
- 15m：MA5/20/60。
- 5m：MA5/20/99/128/255，仅用于次日确认条件。

120m Bar 固定为上午、下午各一根，绝不跨午休。历史不足以计算 MA255 时，标的直接 BLOCK。

A3 输出触发区、失效位、禁止追高、第一压力位、盈亏比、四情景和风险单位，但这些数值全部是确定性输入的原样引用。任何篡改都导致计划拒绝。

进入分钟盯盘的计划上限按 regime 固定为每 lane：TREND_MAINLINE 8、ROTATION_NO_MAINLINE 5、REPAIR 3、RISK_OFF_RETREAT 0。超限视为 A3 发布错误，不能在 A4 入口静默截断；三个 lane 的理论最大监控计划数为 24。

### 8.5 模型调用和输出验证

- 启动时对每个模型做思考模式 capability probe，记录请求模板和返回证据，不保存隐藏推理正文。
- 严格 JSON Schema；禁止 Markdown、前言和代码围栏。
- JSON 无效时允许同一模型做一次“只修格式、不改事实”的修复调用。
- 第二次仍无效则该阶段失败；不切换其他模型代答。
- 429/5xx 使用有上限的退避重试，不设置跨时段熔断，不暂停未来正常调度。
- 超过阶段截止时间的结果不可发布。

## 9. 盘中 Flash 盯盘

### 9.1 运行窗口

```text
09:25–11:30
13:00–15:00
```

- 09:25–09:31：竞价和预检，不产生买入。
- 09:32 起：只有已激活计划可以进入买入确认。
- 14:15 起：收紧买入条件。
- 14:45 起：禁止 BUY/ADD。
- 14:57–15:00：只处理风险退出。
- 午休、非交易日和盘后不调用 Flash。

### 9.2 每分钟处理顺序

1. 获取一个冻结的分钟快照。
2. 校验行情、Bar、规则、时钟和 active plan 版本。
3. Trigger Engine 对所有计划逐条确定性判定。
4. 数据抓取可对三 lane 的股票并集去重，但模型上下文严格隔离；对每个非空 lane 各发一个 Flash 批量请求，最多三个请求并行，使用同一 `minute_snapshot_id`。
5. Flash 必须遵守原 `IntradaySignalV1` 动作枚举：`NO_ACTION/START_CONFIRMATION/BUY_SIGNAL/ADD_SIGNAL/SELL_SIGNAL/REDUCE_SIGNAL/CANCEL_SIGNAL/BLOCK`。否决通过 `llm_veto=true` 表达，不能发明 `KEEP` 或 `VETO` 动作。
6. Trigger Engine 未通过时只接受 `NO_ACTION` 或 `BLOCK`；任何 BUY/ADD 都被判越权。Trigger Engine 通过后，Flash 仍只能保持非否决或否决，不能改写触发条件。
7. 输出通过 Schema 和权限校验后，生成有效信号。
8. 写入信号与审计事件后，交给 RiskGovernor。

任一 lane 前一批次超过 50 秒未结束时，该 lane 下一分钟不重叠调用，记录 `MONITOR_OVERRUN`；仅该 lane 的买入失败关闭，其他 lane 不受影响。没有 active plan 且没有持仓的 lane 记录 `EMPTY_SCOPE` 心跳，不调用模型。

### 9.3 买入与退出的非对称故障规则

- 新买入：必须 `Trigger Engine PASS + Flash 有效响应且未否决 + RiskGovernor PASS`。Flash 超时等于不买。
- A4 上下文卖出：可作为退出建议进入 RiskGovernor。
- 确定性硬止损、计划失效、总风险门和账户风控退出：由 RiskGovernor 直接生成强制退出，不等待 Flash，也不允许 Flash 否决。
- 因 T+1 当日不可卖时，记录 `BLOCKED_T1` 并持久化为次交易日首要风险任务，不能标记已退出。

### 9.4 “只保留有效结果”的实现

用户可见 `effective_signals.md` 只保留：

```text
BUY_SIGNAL / ADD_SIGNAL / SELL_SIGNAL / REDUCE_SIGNAL
LLM_VETO / PLAN_INVALIDATED / DATA_BLOCK / FORCED_RISK_EXIT
```

重复 `NO_ACTION` 不进入 Markdown。内部 SQLite 只保存每分钟的结构化条件位图、延迟、快照 hash 和聚合计数，用于 A5 的漏信号、消融和否决质量统计；不保存重复的模型叙述文本。

## 10. 确定性 RiskGovernor 和 PaperBroker

### 10.1 三账户隔离

三个账户使用相同初始资金、费用、滑点、规则和风险参数，但分别维护现金、持仓、可卖数量、信号、意图、成交和权益。默认比较资金建议为每账户 1,000,000 元；该值只影响模拟规模，不影响候选筛选。

每次买入成交必须创建独立、持久化的 `PositionRiskPlan`，固化所属 lane、原 plan_id、成本、失效位、硬止损、允许加仓次数和退出规则。A3 原计划到期或股票退出研究池后，该持仓风险计划仍持续到仓位完全关闭；任何真实持仓永远自动进入对应 lane 的监控范围。

### 10.2 定仓公式

```text
risk_cash = equity × base_risk_pct × risk_unit
raw_qty   = floor(risk_cash / abs(entry_reference - stop_level))
qty       = 按交易规则整手向下取整，并再次受现金、单票、板块、总仓上限约束
```

`base_risk_pct`、总仓、单票、板块、每日新增次数和现金保留比例必须在模拟配置中显式给值、版本和人工批准状态；未经批准不能启动模拟。三个账户始终使用同一版本。

### 10.3 模拟撮合

- 信号先创建 `PENDING` 意图，不能直接写成成交。
- 采用信号之后下一根完整 1m Bar 的确定性成交模型；不得使用信号前价格。
- 买入施加不利滑点，卖出施加不利滑点，成交价必须处于该分钟高低价和当日合法价格范围。
- 无成交量、一字板、停牌、价格锁定或行情陈旧时不得成交。
- 部分成交只有在数据合同能证明可成交量时启用；否则 V1 只支持全成或不成。
- 费用由版本化规则计算，不在提示词中写死。
- 每个意图和成交使用唯一幂等键；重放不会重复改变现金和持仓。
- 禁止卖空、负现金、亏损摊平；加仓最多一次且已有浮盈、原计划允许、账户风险仍通过。
- 同一账户、股票、分钟同时出现互斥动作时，确定性优先级为 `FORCED_RISK_EXIT > SELL/REDUCE > CANCEL > ADD > BUY`；退出与买入不能在同一分钟互相抵消。
- 跨除权除息、送转、配股或证券代码/状态变化时，由版本化公司行动事件调整模拟持仓与成本；缺少可靠事件时冻结该持仓的绩效统计和新动作，不能让价格跳变伪造成盈亏或止损。

### 10.4 对账

每次成交后和每日收盘后校验：

```text
期初现金 + 卖出净额 - 买入净额 - 费用 = 期末现金
期初持仓 + 买入成交 - 卖出成交 = 期末持仓
持仓数量 = 可卖数量 + T+1 锁定数量
账户权益 = 现金 + 持仓市值
```

任何不一致立即冻结对应账户的新买入，生成 CRITICAL incident；不得自动用余额覆盖差异。

## 11. Agent 5 复盘与配置治理

- 每日：确定性代码回填结果标签和数据质量，不要求 LLM 计算统计量。
- 每周/月度/季度：在 15:10 收盘任务内部，根据样本门槛使用 `deepseek-v4-pro-0813` 运行复盘提示词；它是独立评审者，不改变三个研究 lane 的候选、账户和比较指标。
- 三模型表现比较使用相同基准、相同起止时间和相同成本口径。
- A5 只能输出 `CONFIG_CHANGE_PROPOSAL`、`PROMPT_CHANGE_PROPOSAL`、`DATA_SOURCE_CHANGE_PROPOSAL`。
- 每个提案必须有样本量、置信区间、适用 regime、证伪条件、影响面和回滚方案。
- 每周期最多两项，先 Shadow 至少 20 个交易日，经人工批准后才能形成新版本。
- 数据质量因素未排除时，任何模型/参数优化提案自动拒绝。

## 12. 存储、幂等和恢复

### 12.1 存储

- SQLite WAL：运行、状态、血缘、计划、信号、账户、意图、成交、提案和 active pointer。
- Parquet：日线、分钟线、复权因子、截面和因子快照。
- JSON：模型合法原始输出、SnapshotManifest、DataQualityReport。
- Markdown：用户结果。

### 12.2 幂等键

```text
research_run: trade_date + slot + model
stage_call:   research_run_id + stage + batch_no + attempt_no
signal:       plan_id + action + condition_state_hash + state_generation
intent:       account_id + signal_id + intent_type
fill:         intent_id + fill_sequence
report:       trade_date + slot + report_type + source_version_hash
```

`snapshot_hash/prompt_hash/config_hash` 是 research run 在 `DATA_BOUND` 时固定的不可变列，不属于可反复创建 run 的键；同一交易日、时段和模型只能有一个逻辑 run，数据就绪重试属于该 run 的 attempt。`state_generation` 只在信号状态真正关闭后重新进入同类状态时增加，持续数分钟的同一条件不会重复生成信号。数据库唯一约束阻止重复，不使用“先查询再插入”作为唯一防重手段。

### 12.3 重启恢复

- 已持久化并验证的阶段可以继续，不重复调用已完成 batch。
- 进程崩溃时处于 `RUNNING` 且无完整响应的模型调用标记 `ABANDONED_ATTEMPT`，允许同模型新 attempt 重跑，但旧 attempt 永远不能发布。
- active pointer 只指向完整版本；临时版本永远不被盘中读取。
- 未知状态的模拟意图不重复成交，先根据本地事件账本确定是否已应用；无法证明时冻结账户并人工检查。
- 盘中服务中断后只使用恢复时的最新完整快照重建状态，不补发停机期间已经过期的 BUY/ADD。确认计时要求连续合格 Bar；出现数据缺口、午休、重启或快照不连续时自动重置确认计时。
- Parquet 和 Markdown 使用临时文件写入、校验后原子重命名；半写文件永远不进入 manifest 或报告索引。

## 13. 调度与单实例

Windows 任务只负责启动三个入口，业务日历和时段由程序再次校验：

```text
09:20  monitor_service      运行至 15:05，内部处理午休
09:25  morning_research     三模型只收紧
15:10  close_research       三模型完整漏斗 + 当期复盘
```

每个入口使用命名互斥锁和数据库 lease。重复启动只能由一个实例取得 lease。lease 含心跳、到期时间和 owner；接管前必须确认旧进程不存在或 lease 已过期。

错过任务处理：

- 09:25 任务在 09:40 后才启动：整次标记 MISSED，不激活新买入计划。
- 15:10 任务可在当日 20:30 前补跑，仍必须使用完整结算数据。
- 非交易日全部 SKIPPED，不调用模型。

## 14. Markdown 输出

```text
outputs/YYYY-MM-DD/
├── morning_0925/
│   ├── deepseek/final.md
│   ├── kimi/final.md
│   ├── glm/final.md
│   └── model_comparison.md
├── close_1510/
│   ├── deepseek/final.md
│   ├── kimi/final.md
│   ├── glm/final.md
│   └── model_comparison.md
├── watch/effective_signals.md
├── simulation/
│   ├── deepseek_account.md
│   ├── kimi_account.md
│   ├── glm_account.md
│   └── account_comparison.md
└── review/daily_review.md
```

Markdown 由确定性渲染器从已验证 JSON/数据库生成。渲染失败不能改变研究、信号或账户状态；修复后可按同一 report id 幂等重建。

## 15. 故障处理矩阵

| 故障 | 新买入 | 已有持仓风险 | 发布/恢复 |
|---|---|---|---|
| 同花顺实时行情陈旧 | BLOCK | 不模拟虚假成交，生成事故并持续拉取 | 恢复新鲜后重新判断 |
| 分钟 Bar 缺口 | BLOCK 相关标的 | 硬风控保留，无法定价时不伪造成交 | 补齐前不发布信号 |
| 日线未结算 | 不生成次日计划 | 不影响已有持仓风险管理 | 20:30 后收盘 run 失败 |
| A1/A2/A3 模型超时 | 对应 lane BLOCK | 不影响其他 lane 和已有持仓 | 成功 lane 在共同屏障发布 |
| 模型 JSON 二次无效 | 对应阶段 FAILED | 不影响已有持仓 | 不跨模型代答 |
| Flash 超时/无效 | 当分钟不买 | 确定性硬退出继续 | 下一分钟正常重试 |
| 429/5xx | 截止时间内退避重试 | 不扩大权限 | 不设置跨时段熔断 |
| 配置中途改变 | 当前 run 继续使用已固定版本 | 同上 | 新版本只影响新 run |
| 重复调度 | 无重复执行 | 无重复成交 | lease + 唯一键阻断 |
| 进程重启 | 未发布版本不可用 | 从事件账本恢复 | 继续合法阶段或标记 abandoned |
| SQLite/磁盘写失败 | 全部新动作 BLOCK | 不能进行无审计模拟成交 | 恢复存储后人工确认 |
| 对账不一致 | 冻结对应账户 | 保留风险事件 | 不自动改账 |
| T+1 不可卖 | 不适用 | `BLOCKED_T1`，次日优先处理 | 不标记已退出 |
| Markdown 渲染失败 | 不影响内部状态 | 不影响 | 幂等重渲染 |

## 16. 安全边界

- 密钥只从本机 `.env` 或 Windows Credential Manager 读取；日志统一脱敏。
- HTTP 客户端禁止把认证头写入异常字符串。
- 模型网关只允许配置中的 HTTPS host，禁止模型提供 URL 或工具调用。
- 外部新闻、公告和政策正文先做长度限制、HTML 清理、指令型文本检测和引用标识。
- 模型不能访问文件系统、Shell、数据库写接口或网络工具。
- 输出 Markdown 不包含密钥、请求头、完整原始模型思考内容或不必要的外部原文。

## 17. 测试与验收合同

### 17.1 必测类型

1. 数据 API 契约测试：字段、类型、分页、额度、时间戳、空值和 429。
2. 因子黄金样本：复权、午休、停牌、涨跌停、除权日、MA255、未收盘 Bar。
3. Schema 测试：三层子集、枚举、数值只读、早盘 tighten-only。
4. 性质测试：任意输入下都不能越级、负现金、卖空、T+1 违规或重复成交。
5. 故障注入：API 超时、部分返回、时钟漂移、模型超时、非法 JSON、进程崩溃、磁盘满、重复任务。
6. 重放测试：相同冻结快照与模型录制响应必须产生完全一致的血缘、计划、信号和模拟账本。
7. 端到端 Shadow：模型真实调用、真实行情，只产生内部模拟结果。

### 17.2 零容忍指标

```text
未来数据泄露                         0
越级加入候选                         0
未收盘 Bar 用于结构确认              0
无规则快照仍产生动作                 0
Flash 促成确定性引擎未通过的买入      0
信号层输出股数或仓位                 0
T+1 违规                             0
负现金或卖空                         0
重复信号、意图或成交                 0
账户对账不一致                       0
未持久化先发布                       0
密钥进入源码、日志或报告             0
```

### 17.3 上线到长期模拟前的门槛

- P0 同花顺职责内能力、mootdx 分钟能力和跨源价格校验全部关键项通过。
- 四个模型思考模式和严格 JSON 实测通过。
- 至少 60 个交易日历史回放；历史数据缺 point-in-time 的阶段必须明确排除，不能伪造覆盖率。
- 至少 20 个交易日连续 Shadow。
- 覆盖 TREND_MAINLINE、ROTATION_NO_MAINLINE、RISK_OFF_RETREAT；没有覆盖时继续观察，不宣称完成。
- Agent 5 至少完成一轮校准并产生一个真正被证伪或被拒绝的提案。

## 18. 分阶段实施顺序

### Phase 0：能力与合同

目录、配置、Schema、密钥加载、同花顺真实能力探测、模型思考模式探测、规则快照和数据质量合同。任何关键能力失败立即停在本阶段。

### Phase 1：数据与确定性底座

证券域、增量缓存、四时间戳、复权、分钟聚合、G0、因子引擎、快照冻结和重放。

### Phase 2：三模型 A1–A3 Shadow

独立 lane、批处理、严格子集、JSON 验证、血缘、原子发布、Markdown 和模型比较。不启用盘中信号和模拟账户。

### Phase 3：A4 盘中 Shadow

每分钟 Trigger Engine、Flash 批量否决、有效事件 Markdown、超时和重启恢复。不进入模拟账户。

### Phase 4：内部模拟

RiskGovernor、三个账户、T+1、费用、滑点、撮合、对账和账户比较。仍无外部委托。

### Phase 5：A5 复盘与长期模拟

结果标签、归因、校准提案、Shadow 变更治理。达到全部验收门槛后才进入无人值守长期模拟。

## 19. 复核记录

### 第一轮：架构、权限和时序复核

检查项：三模型独立、严格子集、早盘只收紧、收盘完整漏斗、A4 只有否决权、风险执行独立、两个研究时段、原子发布。

修正结果：

1. 不再让未完成早盘复核的收盘计划直接产生新买入；早盘失败时只管理已有持仓。
2. 增加 09:32 最早发布和 09:40 统一截止，避免模型完成时间不同导致比较窗口偏差。
3. 关闭 FAST_TRACK，消除与严格层层过滤的冲突。
4. 把确定性硬风险退出从 Flash 链中分离，避免模型超时阻塞止损。
5. 明确 120m Bar 不跨午休。
6. 修正快照时序：09:25/15:10 只创建 run，必须等 09:26 竞价或收盘日线结算完成后一次性 DATA_BOUND，防止边等数据边改变模型输入。
7. 明确“每天两次”是两个逻辑研究时段，batch 调用不能另建 run。

结论：架构逻辑闭合，未发现仍可越级扩大权限的路径。

### 第二轮：故障、恢复和审计复核

检查项：数据陈旧、分钟缺口、模型超时、非法 JSON、429、重复任务、进程崩溃、T+1、磁盘失败、对账不一致、报告失败。

修正结果：

1. 增加持久化先于 active pointer 的事务顺序。
2. 增加 lane/stage/signal/intent/fill 五级幂等键。
3. 明确 Flash 故障对买入和硬风险退出的非对称行为。
4. 将用户可见“只保留有效结果”和 A5 所需内部统计分层保存。
5. 增加时钟漂移、磁盘写失败和未知模拟意图的失败关闭规则。
6. 把 mootdx 分钟历史与节点轮换、同花顺复权事件、财务披露时点和模型思考模式列为 P0 实测门禁，不以 Mock 测试替代真实证明。
7. 修正信号幂等键，持续数分钟的同一条件不再按分钟重复建信号。
8. 增加 `PositionRiskPlan`，避免 A3 计划过期或股票退出研究池后持仓失去退出规则。
9. 增加同分钟冲突动作优先级、公司行动处理、停机后不补发过期买入和确认计时重置。
10. 将 Flash 按模型 lane 隔离调用，防止某个研究模型的计划污染另一个模型的盯盘判断。

结论：在 P0 门禁通过、实现满足本合同且验收测试全部通过的前提下，流程不存在已知的未关闭交易权限路径。外部 API 权限、数据时效和模型能力仍需真实环境证明，未验证前不得宣称工作流可长期运行。

## 20. 当前剩余风险

1. mootdx 公共节点没有 SLA，北交所分钟能力未证明；节点池、分钟历史覆盖、同花顺复权事件和实时延迟必须持续监测。
2. 三个研究模型及 Flash 的思考模式参数、最大上下文、最大 JSON 输出和稳定延迟尚未验证。
3. 历史概念成分与财务首次披露时间未必能完整回填；不足时 A1/A2 的历史评估只能标记不可靠。
4. 初始风险参数只适用于模拟比较，不能被解释为已经验证的最优参数。
5. 任何密钥已在聊天或其他渠道出现过时，正式实施前必须轮换。

这些风险均已有阻断或降级路径，不允许被静默忽略。
