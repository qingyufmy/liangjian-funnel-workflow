# 量见 A 股工作流：事实数据源接入优化方案

版本：`FACT_SOURCE_OPTIMIZATION/1.0.0-draft`  
日期：2026-08-25  
适用目录：`D:\dev_A股\liangjian_funnel_workflow`  
状态：设计完成，尚未实施  

> 本文件只定义事实源接入与验证方案，不修改原项目 `liangjian-astock-ai`，不连接券商、掘金或真实账户。所有交易结果仍为内部虚拟模拟。

## 1. 优化目标

当前调度、冻结快照、三模型 A1→A2→A3、Flash 分钟盯盘和虚拟账户已经可以运行，但研究输入中仍有多项 `SOURCE_NOT_CONFIGURED`。本轮优化的目标是：

1. 最大化复用现有同花顺金融数据 API Key，不重复购买已有能力。
2. 巨潮先使用免费公开公告与 PDF；正式数据 API 只作为未来可选增强，不作为 V1 依赖。
3. 模型不直接访问任何外部事实源；所有事实先由确定性代码采集、校验、冻结。
4. 三个研究模型读取同一个 `snapshot_id`，保证模型对比公平。
5. 原始文档不整篇塞入提示词，只注入结构化摘要、证据 ID 和可追溯链接。
6. 不把热榜当新闻、不把龙虎榜当全部资金流、不把同花顺行业冒充申万行业。
7. 缺失字段必须显式标记，绝不填零、猜测或跨来源自动替代。
8. 在不降低三份提示词硬约束的前提下，让 A1、A2、A3 获得足够完整的事实输入。

## 2. 核心架构

```text
外部事实源
  ├─ 同花顺 REST API
  ├─ mootdx 通达信分钟线
  ├─ 巨潮/交易所公开公告
  ├─ 中国政府网及部委政策
  └─ 申万行业分类文件
          ↓
Source Adapter（每个源独立限速、重试、契约校验）
          ↓
Raw Store（不可变原始响应/PDF/文件哈希）
          ↓
Normalizer（统一代码、单位、时间和枚举）
          ↓
Fact Store（去重、四时间戳、证据血缘）
          ↓
Snapshot Builder（按 as_of 冻结一次）
          ↓
Deterministic Aggregator（板块、情绪、K线、价位、拥挤度）
          ↓
Prompt Input Projector（裁剪为 A1/A2/A3 所需字段）
          ↓
DeepSeek / Kimi / GLM 共享事实，各自独立分析
```

禁止以下路径：

```text
模型 → 临时联网搜索
模型 A 的结论 → 模型 B 的事实输入
缺失源 → 用另一个含义不同的字段代替
原始 PDF/网页全文 → 不经校验直接拼入提示词
```

## 2.1 提示词事实字段总映射

| 提示词字段 | 生成方式/权威源 | 优先级 | V1 缺失动作 |
|---|---|---:|---|
| `SNAPSHOT_MANIFEST` | 本地 Snapshot Builder | P0 | 全局 BLOCK |
| `MACRO_POLICY_FEED` | 中国政府网、发改委、工信部、统计局、证监会 | P0 | A1 不得建立政策主题 ACTIVE 结论 |
| `INDUSTRY_PROFIT_DATA` | 国家统计局行业月度数据 | P1 | 对应产业链节点 RESEARCH_ONLY |
| `SW_INDUSTRY_MAP` | 申万官方分类与成分文件 | P0 | 未映射股票 RESEARCH_ONLY |
| `EXISTING_CHAIN_GRAPH` | 内部版本化产业链注册表 | P0 | A1 不得输出 ACTIVE 公司池 |
| `THEME_REGISTRY` | 内部版本化主题注册表 | P0 | A1/A2 不得确认主题阶段 |
| `COMPANY_FUNDAMENTALS` | 同花顺财务三表、财务指标、主营构成；巨潮补披露时间 | P0 | 对应股票排除 |
| `DISCLOSURE_EVENTS` | 巨潮/交易所公开公告与 PDF | P0 | 公告查询失败的股票排除 |
| `RISK_EVENTS` | 巨潮/交易所监管、减持、解禁、质押、诉讼等 | P0 | 查询失败的股票排除 |
| `RESEARCH_CONSENSUS` | 合法授权的一致预期源 | P2 | 明确不可用，不补值；该评分维度为 unavailable |
| `FUND_HOLDINGS` | 同花顺基金持仓与基金定期报告 | P1 | 拥挤/机构维度不可用，不冒充零持仓 |
| `FAST_TRACK_REQUESTS` | 内部任务队列；V1 固定关闭 | - | 传入显式 disabled，不视为数据故障 |
| `PRIOR_OUTCOME_FEEDBACK` | 本地虚拟模拟结算与确定性复盘 | P1 | 样本不足时显式 `INSUFFICIENT_SAMPLE` |
| `MARKET_REGIME_SNAPSHOT` | 全市场行情与确定性状态机 | P0 | A2/A3 BLOCK |
| `MARKET_EMOTION_SNAPSHOT` | 同花顺全市场行情、涨停池、连板天梯，本地聚合 | P0 | A2 BLOCK |
| `SECTOR_CYCLE_SNAPSHOT` | 同花顺指数目录、成分、行情，本地计算阶段 | P0 | 相关主题 RESEARCH_ONLY |
| `CAPITAL_FLOW_SNAPSHOT` | 龙虎榜、融资融券、ETF 份额/申赎及明确口径资金流 | P0 | 不得形成可入场 focus pool |
| `NEWS_HEAT_SNAPSHOT` | 独立新闻/互动易/政策传播事实；热榜仅作关注度辅助 | P1 | 不得复制热榜或资金数据补齐 |
| `CROWDING_SNAPSHOT` | 基金持仓、龙虎榜集中度、融资占比、换手结构 | P0 | 不得形成可入场 focus pool |
| `AUCTION_SNAPSHOT` | 同花顺 09:26 `stage=final` 竞价接口 | P0 早盘 | 早盘禁止新增激活 |
| `KLINE_PATTERNS` | 本地闭合日线/5m K线形态引擎 | P0 | A3 对应股票 REJECT |
| `PRICE_LEVELS` | 本地确定性触发区、失效位、阻力和盈亏比 | P0 | A3 对应股票 REJECT |
| `SECTOR_PERMISSIONS` | 市场状态、主题阶段、冷却期确定性生成 | P0 | 默认 `NO_NEW_ENTRY`，不能默认 ALLOW |
| `TRADABILITY_FLAGS` | 同花顺行情状态 + 交易所规则 + G0 | P0 | 对应股票排除 |
| `EXCHANGE_RULES` | 交易所规则版本快照并人工签核 | P0 | 全局或市场 BLOCK |
| `DATA_SLA_POLICY` | 本地版本化配置 | P0 | 配置缺失全局 BLOCK |

该表是字段级 authority 合同。后续实现不得因为某个源“看起来也有类似字段”而私自改变映射。

## 3. 数据分层

### 3.1 Raw 层

保存外部响应的最小必要原件：

- JSON API：保存业务 `code`、请求参数摘要、响应数据、HTTP/业务时间戳和内容哈希。
- 公告：保存公告元数据、原始 PDF URL、PDF 哈希；只下载候选公司和指定事件类型的 PDF。
- 政策：保存标题、发文机构、文号、发布日期、正文 URL、正文哈希。
- 行业分类：保存官方文件原件、版本日期和哈希。
- 不保存 API Key、认证头、Cookie、模型思考正文。

建议目录：

```text
storage/facts/
├─ raw/
│  ├─ hithink/YYYY-MM-DD/
│  ├─ cninfo/YYYY-MM-DD/
│  ├─ government/YYYY-MM-DD/
│  └─ swsindex/YYYY-MM-DD/
├─ normalized/YYYY-MM-DD/
├─ snapshots/YYYY-MM-DD/
└─ registries/
   ├─ industry_chain_registry.yaml
   ├─ theme_registry.yaml
   └─ source_contracts.yaml
```

### 3.2 标准事实层

每条事实至少包含：

```json
{
  "fact_id": "sha256:...",
  "source_id": "cninfo_public",
  "source_tier": "T1",
  "symbol": "600519.SH",
  "fact_type": "DISCLOSURE_EVENT",
  "event_time": "2026-08-24T00:00:00+08:00",
  "publish_time": "2026-08-24T18:32:00+08:00",
  "fetch_time": "2026-08-25T06:00:00+08:00",
  "ingest_time": "2026-08-25T06:00:01+08:00",
  "available": true,
  "reason_code": "OK",
  "source_url": "https://...",
  "content_hash": "...",
  "payload": {}
}
```

约束：

- `event_time`、`publish_time`、`fetch_time`、`ingest_time` 分开保存。
- 数值缺失使用 `null`，不得使用 `0`。
- 原始百分数和金额必须记录单位，标准化后统一为比例值和人民币元。
- 同一字段只能指定一个 authority；校验源不能覆盖 authority。
- 文档修订采用 append-only，历史回放必须使用当时可见版本。

### 3.3 冻结快照层

每个研究时段只冻结一次：

- `09:26`：上一收盘完整事实 + 隔夜公告/政策 + 最终竞价。
- `15:10`：当日收盘行情、板块、涨停结构、公告和政策增量。
- 三个研究 lane 共享相同 `snapshot_id`、`source_checksums`、`coverage` 和 `as_of`。
- 快照冻结后，任何晚到数据只能进入下一次运行，不得修改已绑定快照。

## 4. 同花顺 API 扩展方案

现有 `HithinkClient` 继续作为统一 REST 客户端，新增端点不新建第二套鉴权逻辑。

### 4.1 P0：直接接入

| 数据能力 | REST 端点 | 生成事实 |
|---|---|---|
| 行业/概念指数目录 | `/api/a-share-index/catalog/ths-index-list` | 同花顺行业与概念注册表 |
| 指数成分股 | `/api/a-share-index/constituents/ths-stock-list` | 概念/行业成员关系 |
| 指数行情快照 | `/api/a-share-index/prices/snapshot` | 板块涨跌、成交额 |
| 涨停池 | `/api/a-share/special-data/limit-up-pool` | 涨停数、封单、连板、原因 |
| 连板天梯 | `/api/a-share/special-data/limit-up-ladder` | T1/T2/T3+ 梯队结构 |
| 集合竞价 | `/api/a-share/auction/snapshot` | `AUCTION_SNAPSHOT` |
| 龙虎榜 | `/api/a-share/special-data/dragon-tiger-list` | 龙虎榜集中度和机构/游资净额 |
| 热榜/飙升榜 | `/api/a-share/special-data/hot-stock-list` 等 | 独立的 `MARKET_ATTENTION`，不冒充新闻 |
| 资产负债表/现金流量表 | 财务报表端点 | 补齐 `COMPANY_FUNDAMENTALS` |
| 基金持仓 | `/api/fund/portfolio/stock-history` 等 | `FUND_HOLDINGS` |

官方文档：

- https://fuyao.aicubes.cn/docs/api-reference/a-share-index/
- https://fuyao.aicubes.cn/docs/api-reference/auction/
- https://fuyao.aicubes.cn/docs/api-reference/hot-list-data/
- https://fuyao.aicubes.cn/docs/api-reference/fund-portfolio/

### 4.2 调用优化

1. 指数目录每天只拉一次，成分股只在目录版本变化或每日收盘后刷新。
2. 指数快照按批量接口获取，禁止对每只股票逐个拉板块行情。
3. 涨停池和连板天梯每个研究时段各拉一次，三个模型共享。
4. 财务数据只针对 G0 后候选增量更新；报告期和披露版本未变化时复用缓存。
5. 基金持仓按季度构建 `股票 → 基金数量/持仓比例` 反向索引，不在每次研究时遍历全部基金。
6. 热榜单独保存为市场关注度；`NEWS_HEAT_SNAPSHOT` 仍需独立新闻事实。
7. 同花顺行业只能标记为 `THS_INDUSTRY`，不能写入 `SW_INDUSTRY_MAP`。

## 5. 巨潮公开公告接入方案

V1 不购买 CNINFO Data Service 正式 API，使用巨潮公开公告页面和公开 PDF。

### 5.1 采集范围

仅采集以下类别：

- 定期报告、业绩预告、业绩快报。
- 重大合同、中标、订单、产能建设。
- 停复牌、退市风险、ST 风险。
- 股东减持、解禁、质押、控制权变更。
- 监管问询、纪律处分、立案调查。
- 重大诉讼、担保、审计意见。
- 投资者调研和主营业务说明。

### 5.2 免费接入边界

- 公告网页和 PDF 是公开信息，可作为 V1 事实源。
- 公开网页不是稳定正式 API，必须独立适配、限速、缓存和契约测试。
- 页面或参数变化时返回 `CNINFO_CONTRACT_CHANGED`，不得把空响应当作“没有公告”。
- 正式 API 需要授权时再单独评估，不把未来付费服务写成现有能力。

### 5.3 性能优化

1. 全市场只查询公告元数据增量，不下载全量 PDF。
2. 使用 `announcement_id + publish_time + adjunct_url` 去重。
3. 只下载 G0 候选及高风险事件 PDF。
4. PDF 解析结果按内容哈希永久缓存。
5. 输入模型的只是结构化事件卡片和短证据摘要，不注入整篇 PDF。
6. 原始公告标题和正文按不可信外部文本处理，过滤提示词注入模式。

官方入口：

- https://www.cninfo.com.cn/new/index
- https://webapi.cninfo.com.cn/

## 6. 政策与行业事实接入

### 6.1 `MACRO_POLICY_FEED`

第一版权威源：

- 国务院政策文件库。
- 国家发改委。
- 工业和信息化部。
- 国家统计局。
- 财政部、商务部、证监会等相关部委。

每条政策保存：发文机构、文号、政策层级、发布日期、生效日、政策工具、正文 URL、内容哈希。财经媒体只允许触发待核实任务，不能作为 T0/T1 事实。

政策不直接由模型映射股票。确定性代码先映射到产业链节点，模型只复核政策阶段和传导证据。

官方入口：https://sousuo.www.gov.cn/zcwjk/

### 6.2 `INDUSTRY_PROFIT_DATA`

- 国家统计局行业月度营收、利润、产能利用率、PPI。
- 同花顺公司财务只用于公司层，不冒充行业统计。
- 月度数据以官方发布日期为 point-in-time 边界，历史回放不能使用后续修订版。

### 6.3 `SW_INDUSTRY_MAP`

- 只接受申万官方分类文件或有明确授权的镜像。
- 保存一级/二级/三级行业代码、成分股、有效起止日期和版本哈希。
- 同花顺行业可作为验证和板块行情来源，但不能替代申万字段。
- 申万映射缺失的股票只能 `RESEARCH_ONLY`，不能默认为“其他行业”。

## 7. 内部注册表

### 7.1 `EXISTING_CHAIN_GRAPH`

建议结构：

```yaml
version: "chain-2026-08-25.1"
nodes:
  - node_id: semiconductor_equipment_etch
    theme_id: semiconductor_domestic_substitution
    upstream: []
    downstream: []
    evidence_ids: []
    valid_from: "2026-08-25"
    status: RESEARCH_ONLY
```

### 7.2 `THEME_REGISTRY`

保存：

- 主题 ID、名称和同义词。
- 关联产业链节点。
- 上一交易日阶段。
- 首次发现时间、最近确认时间和过期时间。
- 政策、公告、板块和资金证据 ID。
- 不允许手工把某只股票直接设为可买；股票必须经确定性行业/主营映射进入节点。

注册表变更使用新版本，不原地覆盖。模型只能读取，不能写正式版本。

## 8. A2 四类独立情绪事实

| 情绪通道 | 事实来源 | 允许指标 | 禁止替代 |
|---|---|---|---|
| 资金情绪 | 龙虎榜、融资融券、ETF 份额/申赎、明确口径的资金流 | 净额、占比、连续性 | 不能用涨跌幅代替 |
| 价格情绪 | 全市场行情、涨停池、连板天梯 | 涨跌家数、涨停、连板、封单、溢价 | 不能用新闻热度代替 |
| 舆情热度 | 新闻、互动易、政策/公告传播量 | 去重条数、来源数、情感分布 | 热股榜只能作为关注度辅助 |
| 持仓拥挤 | 基金持仓、龙虎榜集中度、融资占比、换手结构 | 集中度、分位数、变化率 | 不能只用一个龙虎榜净额代替 |

四个通道各自带 `available/coverage/staleness/reason_code`。任何一个通道缺失时，不得复制其他通道数据补齐；相关主题只能观察，不能形成可入场 focus pool。

## 9. A3 确定性技术事实

不新增外部数据源，直接使用现有日线、5m 和确定性因子：

### 9.1 `KLINE_PATTERNS`

检测以下形态并绑定均线位置：

- 放量突破、缩量回踩、突破回踩。
- 长下影线、十字星、吞没、失败突破。
- 平台整理、趋势回调、量价背离。

每个形态必须包含：周期、闭合 Bar 时间、形态参数、`ma_context`、量能分位、失效条件。没有均线位置的形态不得注入 A3。

### 9.2 `PRICE_LEVELS`

确定性计算：

- `trigger_zone`
- `invalidation`
- `stop_distance_pct`
- `first_resistance`
- `reward_risk`
- `max_chase_price`
- `source_bar_time`

模型只能选择或拒绝系统生成的价位，不能重算或修改数值。

### 9.3 `SECTOR_PERMISSIONS`

由市场状态、主题阶段和冷却期生成：

```text
ALLOW → PROBE_ONLY → NO_NEW_ENTRY → EXIT_RISK
```

模型只能收紧，不能提升权限。

## 10. 调度优化

### 10.1 盘前预取

```text
08:40  更新交易日历、行业/概念成员、隔夜公告、政策增量
09:20  更新行情和板块快照
09:26  获取最终竞价并冻结 morning snapshot
09:26+ 三模型执行 tighten-only A1→A2→A3
```

08:40 和 09:20 只是数据预取，不调用研究模型，不算新增研究时段。

### 10.2 收盘处理

```text
15:05  更新收盘行情、涨停池、连板天梯、龙虎榜可用数据
15:10  检查日线结算与事实覆盖率
15:10+ 冻结 close snapshot，执行完整三层研究
结算后  生成 PRIOR_OUTCOME_FEEDBACK
```

### 10.3 分钟盯盘

- 仍只获取 active plan 和持仓涉及股票的 1m 数据。
- 不在每分钟重复拉公告、行业成分、基金持仓或完整板块历史。
- 盘中出现公告提示时，只记录事件并收紧/否决；不把新股票加入计划。

## 11. 缓存、限流与故障策略

| 数据 | 缓存/刷新 | 失败行为 |
|---|---|---|
| 股票/指数目录 | 每日一次 | 使用当日已验证版本；无当日版本则 BLOCK |
| 行业/概念成分 | 每日收盘后或版本变化 | 缺失成员的主题 RESEARCH_ONLY |
| 财务报表 | 按报告期增量 | 缺真实披露时间时该期不可用 |
| 政策/公告 | 增量游标 + 内容哈希 | 查询失败不等于无事件 |
| 涨停/板块/龙虎榜 | 每个研究时段一次 | 超 SLA 时 A2 BLOCK |
| 竞价 | 09:26 一次 | 缺失时早盘不能新增激活 |
| 分钟线 | 已有本地缓存 | 缺 Bar 时 A4 买入确认 BLOCK |

数据源请求策略：

1. 429 遵守 `Retry-After`；没有该头时指数退避并加抖动。
2. 5xx、超时、连接错误最多三次，随后记录稳定 reason code。
3. 不同数据源使用独立限速器，单个源失败不拖死全部采集线程。
4. 同一请求使用稳定幂等缓存键，重启后不重复下载成功原件。
5. 过期缓存只能用于审计，不能伪装成当前快照。
6. 模型侧继续保持现有规则：流式传输；429 同模型直接重试；无熔断和跨模型代答。

## 12. 模型输入压缩

为降低 5xx、延迟和 token 消耗：

1. A1 不注入所有原始公告，只注入每公司最近高相关事件卡片。
2. 政策正文先由确定性规则抽取元数据，再附最多必要短摘要和证据 ID。
3. A2 注入板块聚合统计，不注入全市场逐笔或全部股票行情。
4. A3 只接收 A2 focus pool 的因子、形态和价位。
5. 每个字段同时保留 `coverage` 和 `source_health`，模型不需从原始数据猜完整性。
6. Raw Store 永久保留可审计原件，Prompt Input 只保留决策所需视图。

目标：

- A1 单 lane 输入建议控制在 300–500 KB 以内。
- A2 单 lane 输入建议控制在 150 KB 以内。
- A3 单 lane 输入建议控制在 100 KB 以内。
- 三个 lane 使用同一份序列化输入内容和哈希。

## 13. 实施阶段

### Phase 0：合同与目录

- 定义 `FactEnvelope`、`SourceHealth`、`FactSnapshotManifest`。
- 建立 `storage/facts` 目录和原子写入规则。
- 为所有提示词占位符建立字段合同、authority、SLA 和缺失动作。
- 修正配置中“日线 authority 写 mootdx、运行时实际使用同花顺”的不一致。

验收：字段合同测试通过，不改三份提示词。

### Phase 1：现有同花顺能力扩展

- 指数目录、成分、指数快照。
- 涨停池、连板天梯。
- 竞价、龙虎榜、热榜。
- 完整财务三表、基金持仓。
- 批量、缓存、分页、429 和业务错误契约测试。

验收：真实小样本端点全部保存脱敏能力报告；无 Key 泄露。

### Phase 2：本地确定性聚合

- `MARKET_EMOTION_SNAPSHOT`
- `SECTOR_CYCLE_SNAPSHOT`
- `CROWDING_SNAPSHOT`
- `KLINE_PATTERNS`
- `PRICE_LEVELS`
- `SECTOR_PERMISSIONS`

验收：同一冻结输入重复计算结果哈希完全一致。

### Phase 3：内部主题与产业链

- 建立产业链和主题注册表初始版本。
- 同花顺概念成员只作市场映射，不直接提升结构性主题证据等级。
- 注册表写入必须走版本变更，不接受模型直接写入。

验收：任何 A1 ACTIVE 公司都有 `theme → node → company → evidence` 血缘。

### Phase 4：免费官方公开源

- 巨潮公告增量与 PDF。
- 中国政府网/部委政策增量。
- 申万行业分类版本文件。
- 文档去重、四时间戳、发布时点和注入防护。

验收：查询失败与“零记录”能够严格区分；历史回放无未来数据。

### Phase 5：三模型端到端验收

- 用新的 20 只冻结快照跑三模型完整 A1→A2→A3。
- 至少 DeepSeek 和 Kimi 能返回严格 JSON；GLM 非法 JSON按独立模型故障处理。
- 至少一个 lane 能在事实完整时形成 A3 模拟计划；若没有计划，必须能从业务否决原因解释，而不是源缺失。
- 运行结果继续写 Markdown，不连接任何外部交易账户。

## 14. 验收门槛

### 14.1 数据门槛

| 门槛 | 要求 |
|---|---:|
| G0 股票名称/市场/状态覆盖 | 100% |
| 选中候选财务覆盖 | ≥90% |
| 选中候选真实行业映射覆盖 | ≥90% |
| 板块成员覆盖 | ≥80% |
| 公告查询成功状态 | 100%，允许确实零记录 |
| 关键事实四时间戳 | 100% |
| 未来数据泄漏 | 0 |
| 缺失值填零 | 0 |
| 权威源自动回退 | 0 |

### 14.2 流程门槛

- `A3 ⊆ A2 ⊆ A1 ⊆ G0` 始终成立。
- 三模型共享完全相同的事实快照和输入哈希。
- 单一数据源失败不会产生假空集。
- 未收盘 K 线不能进入形态或价位计算。
- 早盘只能收紧，不能新增或扩大计划。
- A4 仍只有否决权。
- 所有最终结果为 Markdown；盯盘只保留有效变化。

### 14.3 性能门槛

- 数据预取不计为第三次模型研究运行。
- 三个模型不重复拉取外部事实。
- 公告和政策采用增量游标，不做每日全量重抓。
- 财务和基金数据按报告期缓存。
- 09:26 冻结后不等待低优先级补充源无限延长早盘窗口。

## 15. 成本策略

V1 目标为零新增付费源：

- 同花顺：使用现有 Key。
- mootdx：使用现有公共节点。
- 巨潮：先用公开公告与 PDF，不购买正式 API。
- 政策：使用政府公开网站。
- 申万：优先官方公开分类文件。
- 新闻/一致预期：没有合法、稳定免费源时如实标记不可用，不使用来源不明的聚合数据。

只有出现以下情况时才评估付费：

1. 巨潮公开公告适配连续发生结构变化，维护成本高于正式 API。
2. 需要有 SLA 的公告首次披露时间和批量增量推送。
3. 需要合法授权的研报一致预期或商业新闻全文。
4. 免费源无法满足提示词明确要求的事实完整性。

任何付费评估必须先做小样本试用和字段契约报告，再决定是否接入。

## 16. 风险与回退

| 风险 | 对策 |
|---|---|
| 巨潮公开页面结构变化 | 独立契约测试；失败标记 `CNINFO_CONTRACT_CHANGED` |
| 同花顺额度/429 | 批量、缓存、分层预取、遵守 Retry-After |
| 申万分类不可公开下载 | 相关股票 research-only，不用同花顺行业冒充 |
| 新闻源不稳定 | 独立标记缺失，不拿热榜或资金流补齐 |
| PDF 解析错误/注入 | 原文不直接进系统提示；内容分类与提示词隔离 |
| 模型输入再次膨胀 | 分阶段投影、证据卡片、严格体积预算 |
| 外部源晚到 | 进入下一快照，不修改已冻结运行 |
| 模型 429/5xx/非法 JSON | lane 隔离，当前运行失败关闭，下一调度正常重试 |

回退原则：新事实源上线后如异常，只关闭该适配器并回到 `SOURCE_UNAVAILABLE`；不得删除现有冻结快照、模拟账户或提示词，也不得用旧数据伪装当前数据。

## 17. 两轮方案复核

### 第一轮：来源语义与流程复核

检查结果：

- 同花顺可覆盖指数、涨停、竞价、龙虎榜、热榜、财务和基金持仓，现有 Key 未被充分利用。
- 巨潮正式 API 不能按免费开放接口假设，V1 改为公开公告/PDF。
- 同花顺行业和申万行业严格分离。
- 热榜、新闻、资金和拥挤度严格分离。
- 所有源先冻结再供三模型共享，未发现跨 lane 数据差异路径。

本轮调整：

1. 将巨潮正式 API 从 P0 必需项降为未来可选项。
2. 增加 `MARKET_ATTENTION`，避免热榜污染 `NEWS_HEAT`。
3. 基金持仓改为季度反向索引，避免每次运行扫描全基金。
4. 公告 PDF 改为候选和风险事件按需下载。
5. 申万映射缺失时明确 research-only，不做自动替代。

结论：通过。

### 第二轮：故障、成本与验收复核

检查结果：

- 两个研究时段不变；新增的 08:40/09:20/15:05 只做预取。
- 三模型不直接取数，不会因模型数量放大外部 API 调用量。
- 数据源 429/5xx、空响应、结构变化和晚到数据均有稳定失败语义。
- 原始证据和模型输入分层，既保留审计，又控制模型请求大小。
- V1 不要求新增付费账户即可推进大部分 P0/P1 能力。
- 事实仍不完整时不会为了“出单”降低约束。

剩余风险：

1. 申万官方分类文件的公开可得性和自动更新方式需实测。
2. 巨潮公开公告入口没有正式 API SLA，需要真实契约测试。
3. 新闻热度和一致预期可能最终需要授权数据源。
4. 产业链图谱初始内容必须有证据支持，不能由模型一次性自由生成后直接生效。

结论：方案可进入 Phase 0/Phase 1 实施；剩余风险均有失败关闭路径，不阻碍先复用同花顺现有能力。

## 18. 推荐执行顺序

下一步严格按以下顺序实施：

```text
1. FactEnvelope/SourceHealth/目录和字段合同
2. 同花顺指数、涨停、竞价、龙虎榜、财务、基金适配
3. 本地板块/情绪/K线/价位聚合
4. 产业链与主题注册表
5. 巨潮公开公告
6. 政策与申万行业
7. 新冻结快照三模型全链验证
8. 09:25、15:10、分钟盯盘连续运行复核
```

Phase 1–2 完成后先做一次中间验收；通过后再接巨潮和政策，避免同时引入太多外部变量导致问题无法定位。
