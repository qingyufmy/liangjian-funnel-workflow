# A1–A3 研究漏斗详细修复方案

> 版本：1.0
> 日期：2026-08-28
> 适用项目：`liangjian_funnel_workflow`
> 本文只定义修复与验收方案，不授权连接外部交易、发送委托或修改真实账户。

## 1. 结论与修复目标

当前系统已经具备“全市场 G0 → A1 → A2 → A3、三模型独立 lane、确定性计算优先、结果落盘”的正确骨架，但最近一次真实运行暴露出四个关键断点：

1. A1 数量达到 133 只，但部分评分使用代理值，月度产业轮动的验收只能保证最低数量与部分行业覆盖，不能保证每个重要轮动方向都被明确纳入或排除。
2. A2 把完整产业瓶颈评分卡作为所有核心候选的事实要求，而现有数据只能稳定证明 8 个因子中的 4 个，导致 DeepSeek 把 130 只全部留在观察池，核心池为 0。
3. A2 核心池为 0 时仍被标记 `VALIDATED`，A3 未实际执行却同样显示 `VALIDATED`，最终 lane 显示 `READY`，状态语义与用户看到的业务结果不一致。
4. Kimi 和 GLM 在宏观发现阶段分别因输出形状和总时限失败，三模型对比没有形成完整结果。

本轮修复必须实现：

- A1 真正按照“月度宏观与政策 → 月度产业周期 → 产业链节点 → 主营与财务传导”形成 100–250 只可持续研究池。
- A2 真正按照“板块强度 → 龙头/中军辨识度 → 产业链卡点 Alpha”从 A1 池中形成当前市场候选，不因卡点资料不全误杀已被事实证明的龙头或中军。
- A3 对非空 A2 池完成周线、日线、120m、15m、5m 的确定性技术筛选，并产出可审计的条件计划。
- 三个模型依次独立运行，不共享结论、不投票、不跨 lane 补结果；研究模型开启思考，盯盘模型关闭思考。
- 所有业务状态、计数、原因、股票代码、名称、证据和血缘在 JSON、Markdown 和前端保持一致。
- 不使用 Redis，不连接掘金、券商、GM 或真实账户；只保留内部模拟。

## 2. 不可变约束

以下约束在修复过程中不得改变：

1. `A3 ⊆ A2 ⊆ A1 ⊆ G0`，任何阶段禁止越级新增股票。
2. G0 必须从同花顺完整证券目录开始；质量门可以过滤 ST、停牌、新股限制、无效行情和低成交额，但不得用性能 Top-N 静默裁剪全市场。
3. 全部 G0 股票必须保留确定性判定记录；LLM 代表样本数量不是 A1 股票池审批配额。
4. 价格、均线、技术分、触发区、失效位、止损距离、盈亏比只能由代码计算，LLM 只能解释或否决。
5. 新闻和社交热度是 T3 线索，不能替代政策、公告、财报、主营或正式经营数据。
6. 缺失值保持 `UNKNOWN/UNAVAILABLE`，不得自动写成 0，也不得由模型凭常识补齐。
7. 三个 lane 独立保存 checkpoint、输出和失败原因，禁止用成功模型的结果填充失败模型。
8. 研究 lane 顺序执行，单 lane 内模型批次顺序执行；只允许数据抓取层有限并发。
9. 每次运行冻结 `as_of`、源地址、发布时间、抓取时间、内容哈希、数据质量和缺失原因。
10. 任何生产修复先在本地和冻结快照上验证，再推送、部署；部署和启动任务必须是单独的明确操作。
11. V1 `FAST_TRACK` 关闭。A2 发现池外信号只能形成下一轮 A1 研究请求，不能进入本次 A2/A3。

## 3. 目标流程

```text
完整证券目录 P0
  ↓ 交易规则、ST、停牌、新股、行情有效性、成交额质量门
G0 全市场研究域
  ↓
M0 确定性月度宏观与大类资产四象限
  ↓
M1 每 lane 一次月度政策/产业周期主题发现
  ↓
Q1 A1 全市场确定性主营、财务、催化、估值与证据评分
  ↓
A1 LLM 代表样本复核 + 本地确定性覆盖层合并
  ↓ 仅 A1 ACTIVE
Q2 A2 板块强度、龙头中军、梯队与产业链证据计算
  ↓
A2 LLM 语义复核：市场核心路线 / 瓶颈 Alpha 路线
  ↓ 仅 A2 focus
Q3 A3 多周期技术、价位、盈亏比、交易限制计算
  ↓
A3 LLM 条件计划复核
  ↓
内部模拟计划 → A4 每分钟只有否决权
```

早盘 09:26 不重新执行完整 A1–A3，只对前一收盘计划做失效、追高和竞价收紧；收盘 15:10 执行完整主链。这样满足每日两个研究时段，同时避免任务重叠和重复拉取全市场事实。

## 4. A1 修复方案

### 4.1 月度策略输入合同

新增服务端结构 `MonthlyStrategyDecisionSet`，必须包含：

- `strategy_month`
- 股票、黄金、债券、现金的 20/60 日动量与可用资金指标
- PMI、CPI、PPI、M1/M2、社融、信贷、国内外利率的最新值、观察期和发布时间
- 美元指数、美联储预期、美/韩/台/日先行市场的可用状态；不可用时逐项标记缺失
- 最近 90–120 天正式政策文件及政策层级、工具、阶段
- 同花顺行业最近 5/10/20 日收益、相对强弱、成交持续性、Top10 出现率
- 上月主题的延续、强化、弱化和退出状态

所有月度轮动候选必须生成一条 `MonthlyIndustryDecision`：

```json
{
  "industry_thscode": "881105.TI",
  "industry_name": "煤炭开采加工",
  "decision": "INCLUDE | EXCLUDE | DEFER",
  "mapped_theme_ids": [],
  "reason_codes": [],
  "supporting_source_refs": [],
  "contradicting_source_refs": [],
  "data_gaps": []
}
```

验收不再只看“主题不少于 6 个、Top10 覆盖 40%”，而是要求：

- 月度轮动前 20 个行业 100% 有 `INCLUDE/EXCLUDE/DEFER` 决策。
- 前 10 个行业 100% 有明确证据解释，不能静默忽略。
- `INCLUDE` 行业必须映射主题和产业节点；`EXCLUDE` 必须给出反证；`DEFER` 必须给出缺失数据。
- 主题目标 8–15 个，产业节点目标 40–80 个；不足时阻断，禁止用近义词拆主题凑数。
- 不硬编码“煤炭、化工、创新药”等行业白名单；这些方向由月度轮动和政策证据驱动，但验收报告必须能说明它们为什么被纳入或排除。

### 4.2 A1 确定性评分重构

删除当前对未实现因子的通用平均值代理。每项评分必须有独立计算器和来源：

| 因子 | 确定性计算 | 缺失处理 |
|---|---|---|
| `structural_theme` | 月度行业轮动、政策生命周期、主题节点映射 | 无映射则 0，并进入观察 |
| `business_mapping` | 主营分产品/分行业收入占比、公告中的项目与客户证据 | 无主营证据不得 ACTIVE |
| `barrier_and_bottleneck` | 专利/认证/产能/客户验证/资源约束的已证实部分 | 未证实保持未知，不得用主题分代替 |
| `financial_quality` | ROE、毛利率、净利率、营收利润增长、现金流、负债率 | 按可用权重重算并降低置信度 |
| `catalyst_confirmation` | 政策 L3/L4、招投标、订单、产能投产、业绩预告 | 只有新闻则最高为线索级 |
| `valuation_expectation_gap` | 估值历史分位、盈利增长与估值匹配、可用一致预期 | 不可用时未知，不能用财务均值替代 |

评分结果必须同时保存：

- `raw_value`
- `normalized_score`
- `available`
- `source_refs`
- `as_of`
- `calculation_version`

综合分只在可用权重达到 70% 时有效。低于 70% 的股票只能进入 `MONITOR_DATA_GAP`，不能因其他高分进入 ACTIVE。

### 4.3 A1 股票池形成

1. 全部 G0 逐只计算，不丢记录。
2. 产业节点内排序用于构建研究覆盖层，不作为性能截断。
3. 每个节点优先保留主营证据强、财务质量有效、成交容量足够的公司。
4. LLM 每主题最多复核 8 只代表样本，重点复核主营语义、政策传导和证据冲突。
5. 本地证据完整的股票不依赖 LLM 批准；LLM 不能提升缺乏主营证据的股票。
6. A1 ACTIVE 目标 100–250，只是发布验收范围，不是凑数配额。
7. ACTIVE 少于 100 时状态为 `BLOCKED_DATA_COVERAGE`；超过 250 时按研究容量降为观察并保留原因。

### 4.4 券商月度金股盲测

复用现有 `evaluation/broker_gold.py`，补充正式工作流入口：

- 数据放在 `storage/benchmarks/broker_gold/YYYY-MM.json`。
- 只接受带券商、月份、股票、发布时间和来源的人工核验或公开原文清单。
- A1 完成后自动计算覆盖率、ACTIVE 覆盖率、排名分位和未覆盖原因。
- 报告固定写入 `benchmark_not_runtime_input=true`。
- 金股名单不得参与 A1 打分，也不得改变股票池，只作为研究框架的盲测基准。

## 5. A2 修复方案

### 5.1 将 A2 拆成两条合法入池路线

当前“每只 focus 股票必须具备完整瓶颈评分卡”的要求过于单一。修复后，A2 focus 可通过两条路线之一进入：

#### 路线 A：`MARKET_CORE`

用于已经被市场事实确认的龙头、中军和趋势核心，必须满足：

- 上游 A1 ACTIVE 且主营映射有效。
- 所属主题分达到门槛。
- `market_role ∈ {LEADER, CORE_ARMY, TREND_CORE, FIRST_MOVER}`。
- 辨识度达到 60。
- 板块宽度、成交占比、梯队结构、相对强弱和流动性达到最小证据覆盖。
- 至少两类市场事实支持，且必须包含价格/成交/梯队中的一类和主营/财务中的一类。
- 不要求证明公司控制稀缺卡点，但必须明确 `bottleneck_status=NOT_REQUIRED_FOR_MARKET_CORE`，不能伪装成稀缺供应商。
- 仍必须复用 A1 的主题、产业节点、主营证据和事实来源；双路线只改变“是否必须证明稀缺卡点”，不降低上游血缘、主营真实性、市场角色或反证要求。

#### 路线 B：`SUPPLY_CHAIN_ALPHA`

用于 Serenity 方法识别的卡点公司，必须满足：

- `supply_chain_role` 为控制或供应稀缺环节。
- 完整 8 因子瓶颈评分卡由服务端重算。
- 至少两条产业瓶颈证据，其中至少一条为强或中等证据。
- 明确 `scarce_layer`、`value_chain_position`、`missing_proof` 和 `kill_switches`。
- 供应商集中度、扩产难度、客户认证、订单或毛利率传导等结论必须有冻结来源。

`STORY_ONLY`、纯概念映射和低辨识度股票不能通过任何路线进入 focus。

### 5.2 A2 确定性评分引擎

服务端按配置真实计算，不再使用当前硬编码代理权重：

- 板块宽度
- 成交额占比
- 真实资金流（仅数据可用时）
- 龙头结构
- 梯队结构
- 赚钱效应
- 催化新鲜度
- 指数与产业链共振
- A1 质量

缺失资金流时：

1. `capital_flow.available=false`。
2. 模型输入中不给可填写的虚构数值位置。
3. 综合分按可用权重重新归一化，同时降低 `evidence_coverage`。
4. 可用权重不足 65% 时主题最高只能 `WATCH_ONLY`。
5. 模型输出非零资金流时，整条主题输出进入自动修复；修复失败则该主题降级，不能只记原因后继续当作有效主题。

### 5.3 A2 事实装配器

新增按 A1 ACTIVE 子集运行的 `A2EvidenceAssembler`，不得再次处理全市场：

- 同花顺行业/概念指数日线和成员关系
- 板块涨跌家数、涨跌停、炸板、梯队高度与宽度
- 板块成交额占全市场比例和持续性
- 个股相对行业指数、沪深300的 5/10/20 日强弱
- 龙虎榜机构席位、市场热榜和成交额排名
- 巨潮公告、定期报告主营页码、订单、产能、客户认证和项目投产证据
- 财务指标、现金流、毛利率变化和营收传导
- 资讯只作为待验证线索，不进入瓶颈事实计数

证据查询结果按 `symbol + fact_type + as_of + source_version` 落盘，后续只增量更新。

资金流数据必须通过单独的来源资格测试：字段定义能够证明是净流入/流出、时间窗口明确、板块代码可稳定映射、历史值可复现。板块成交额、成交额占比、换手率、大单成交或新闻热度都不能改名为“资金净流入”。当前没有通过资格测试的来源时继续标记不可用，优先使用已注册同花顺API中有正式字段定义的能力；其他公开来源只能作为可替换适配器，经字段审计和冻结回放通过后启用。

### 5.4 A2 数量和状态

- focus 目标 30–80，观察池不限于此目标。
- 生产逻辑不允许降低门槛凑数。
- `focus=0` 且由数据缺失造成：`BLOCKED_EVIDENCE_GAP`。
- `focus=0` 但全部主题被完整事实证明处于退潮、风险关闭或无新开仓：`VALIDATED_NO_OPPORTUNITY`。
- `1–29` 只且数据覆盖不足：`DEGRADED_UNDERFILLED_DATA_GAP`，可以继续 A3 研究，但结果不能激活到A4或内部模拟。
- `1–29` 只且数据完整、只是市场机会较少：`VALIDATED_UNDERFILLED_MARKET`，允许继续A3并发布其中通过的计划，但前端必须显示“市场机会较少”，不能伪装成目标数量达标。
- `30–80` 只：`VALIDATED`。
- 超过 80 只：执行全局角色与证据排序，超出部分进入观察，禁止静默删除。

## 6. A3 修复方案

### 6.1 确定性技术计算

仅对 A2 focus 读取分钟历史并计算：

- 周线、日线 MA5/20/60
- 120m MA5/20/99/128/255
- 15m setup
- 5m MA5/20/99/128/255，仅用于触发确认
- MACD、ATR、量能、相对强弱、平台和前高压力
- 触发区、失效位、止损距离、第一压力位、盈亏比
- 停牌、涨跌停、北交所只研究、财报窗口等交易限制

120m 必须按上午、下午分别聚合，不能跨午休。MA255 历史不足时标记 `REGIME_UNKNOWN` 并禁止新增。

### 6.2 LLM 权限收敛

A3 模型不再自由生成数值，只输出：

- setup 分类
- 对确定性周期状态的解释
- 四种开盘情景的行为约束
- 盘中确认条件
- 风险和证伪说明

服务端将触发区、失效位、止损、盈亏比和风险单位回填为冻结值；任何模型修改都被覆盖并记录原因。

### 6.3 A3 状态语义

- A2 因数据缺失为空：A3=`NOT_RUN_UPSTREAM_BLOCKED`。
- A2 经过完整事实判断后合法无机会：A3=`VALIDATED_NO_ACTION`。
- A2 非空但技术数据不可用：A3=`BLOCKED_TECHNICAL_DATA`。
- A2 非空且全部候选被技术规则否决：A3=`VALIDATED_NO_SETUP`，必须保存逐股拒绝原因。
- 至少一只形成核心或影子计划：A3=`VALIDATED`。

只有后两种经过真实 A3 逐股处理的状态，才算 A3 已执行。

## 7. 三模型可靠性修复

### 7.1 顺序与隔离

- 固定 DeepSeek → Kimi → GLM 顺序执行。
- lane 内 `batch_workers=1`。
- 每个 lane 独立宏观发现、A1、A2、A3 checkpoint。
- 一个 lane 失败不阻止下一个 lane，也不能复制其他 lane 的主题。

### 7.2 错误分类和恢复

| 错误 | 处理 |
|---|---|
| 429 | 读取 `Retry-After`；指数退避加随机抖动；原 checkpoint 重试 |
| 5xx | 首次失败切换流式传输；仍失败再退避 |
| 输出容量拒绝 | `393216 → 262144 → 131072` 三级降档 |
| JSON 截断 | 从最后完整 JSON 边界修复；失败后用最小修复提示词，不重传全量事实 |
| 字段类型错误 | 服务端只修复安全的机械类型，如 `analysis_summary` 字符串包装为对象；禁止改业务结论 |
| Schema 缺字段 | 发起一次仅包含缺字段路径的结构修复请求 |
| 总时限 | 保存当前阶段 checkpoint，下次从失败阶段续跑 |

每次修复都要记录：原始响应哈希、错误类型、修复方式、修复前后Schema差异和最终状态；不保存模型思考正文。

### 7.3 宏观发现容错边界

Kimi 当前输出已包含 8 个主题和 14 个节点，但因局部Schema问题整条作废。修复后：

- 机械类型错误可以规范化。
- 主题或节点缺少证据、月度覆盖不足仍必须阻断。
- 不能因已经“接近合格”而跳过证据和月度轮动验收。

## 8. 状态机与前端修复

统一阶段状态：

```text
PENDING
RUNNING
VALIDATED
VALIDATED_NO_OPPORTUNITY
VALIDATED_NO_ACTION
DEGRADED_UNDERFILLED_DATA_GAP
VALIDATED_UNDERFILLED_MARKET
VALIDATED_NO_SETUP
NOT_RUN_UPSTREAM_BLOCKED
BLOCKED_DATA_COVERAGE
BLOCKED_EVIDENCE_GAP
BLOCKED_MODEL
BLOCKED_TECHNICAL_DATA
```

lane 只有在以下情况可显示绿色 READY：

1. A1、A2、A3 都完成真实处理并通过；或
2. A2 被完整事实证明为 `VALIDATED_NO_OPPORTUNITY`，A3 明确为 `VALIDATED_NO_ACTION`。

`VALIDATED_UNDERFILLED_MARKET` 可以生成计划，但 lane 显示黄色 `READY_DEGRADED`；`DEGRADED_UNDERFILLED_DATA_GAP` 只保留研究结果，禁止激活到A4。

数据缺失造成的空池、上游阻断和未执行阶段不得显示 `VALIDATED/READY`。

前端进度和明细弹窗增加：

- 阶段输入数、确定性通过数、LLM复核数、核心数、观察数、拒绝数
- 当前路线：`MARKET_CORE` / `SUPPLY_CHAIN_ALPHA`
- 可用证据权重和缺失事实
- 股票代码、名称、所属主题、产业节点、市场角色
- 选择原因、反对证据、原因码、来源与上游血缘
- A3 未执行、无机会、无技术形态三种不同空状态
- lane 失败时显示失败阶段和是否存在可续跑 checkpoint

## 9. 数据落盘与性能方案

不引入 Redis。继续使用“文件事实库 + SQLite 索引”：

### 9.1 数据分层

```text
storage/raw/                 原始响应，内容哈希命名，不可变
storage/facts/               归一化政策、公告、财务、主营和行业事实
storage/market/              日线、分钟线、行业指数与成员关系
storage/features/            SQLite 因子、主题、角色和评分索引
storage/snapshots/           运行清单和引用，不重复嵌入全部历史正文
storage/benchmarks/          券商金股等独立评估数据
outputs/research/            每次运行 JSON/Markdown
```

### 9.2 增量策略

- 证券目录和行业成员：每日检查版本哈希，有变化才重写。
- 日线：按最后成功交易日增量拉取。
- 分钟线：只对 A2 focus 补齐，按交易日分区。
- 财务三表和指标：按最新报告期/公告时间水位增量。
- 主营 PDF：按公告 ID 和 PDF 内容哈希去重，解析结果长期复用。
- 政策、新闻和公告：按发布时间与内容哈希增量。
- A2 产业证据：按候选和事实类型增量，不对 G0 全市场预抓 PDF。

### 9.3 性能目标

- 全市场确定性扫描不调用 LLM，目标 5 分钟内完成缓存命中运行。
- 每 lane：宏观发现 1 次；A1 复核 3–12 批；A2 复核 1–5 批；A3 复核 1–3 批。
- 单 lane P95 目标不超过 20 分钟；三个 lane 顺序执行总目标不超过 60 分钟。
- 前端进度更新时间不超过 30 秒；超过阈值显示具体阶段和最后 checkpoint，而不是笼统“进度失联”。
- 进程中断后从最后完成的阶段/批次续跑，不重复已经验证的调用。

上述时间是运行SLO，不是降低研究范围或证据门槛的理由。超过SLO时应报告最慢阶段、模型等待、缓存未命中和来源耗时，不得自动减少G0、主题、股票或技术周期。

## 10. Markdown 与持久化结果

每次收盘运行输出：

1. `research_<run_id>.md`：总报告。
2. `research_<run_id>_lane_N.json`：完整机器审计。
3. `research_<run_id>_A1.md`：主题、行业决策、ACTIVE 股票及原因。
4. `research_<run_id>_A2.md`：主题阶段、核心/观察/拒绝股票、入池路线和证据。
5. `research_<run_id>_A3.md`：技术计划、条件价位、风险和拒绝原因。
6. `research_<run_id>_broker_benchmark.md`：券商金股盲测结果；未配置时明确写缺失原因。

总报告不能只展示最终 A3 空 JSON，必须给出每阶段计数、主要阻断原因和对应详细文件。

## 11. 实施阶段

### 11.1 代码变更地图

| 模块 | 主要修改 |
|---|---|
| `src/liangjian_funnel/pipeline/monthly_strategy.py` | 生成月度行业逐项决策、数据覆盖和宏观降级状态 |
| `src/liangjian_funnel/pipeline/deterministic.py` | 拆分A1真实因子计算器；实现A2真实主题与角色因子；删除通用代理分 |
| `src/liangjian_funnel/pipeline/bottleneck.py` | 保留完整瓶颈评分卡；新增路线判定，不再强制市场核心伪造瓶颈分 |
| `src/liangjian_funnel/pipeline/research.py` | 阶段编排、双路线、模型机械修复、状态传播、空池语义、checkpoint续跑 |
| `src/liangjian_funnel/workflow.py` | 按候选装配A2事实、发布门禁、券商基准和分阶段报告 |
| `src/liangjian_funnel/runtime/state.py` | 新状态枚举、向后兼容读取和原子状态更新 |
| `src/liangjian_funnel/runtime/progress.py` | 进度计数、数据缺口、路线与checkpoint摘要 |
| `src/liangjian_funnel/evaluation/broker_gold.py` | 保留评估逻辑，补工作流调用和缺失基准报告 |
| `config/funnel_config_v2.yaml` | 关闭FAST_TRACK；增加路线、覆盖权重和状态阈值配置 |
| `prompts/agent_1_macro_chain_v2.txt` | 要求月度行业逐项决策，减少模型自由评分 |
| `prompts/agent_2_theme_sentiment_v2.txt` | 输出路线、事实声明和反证；数值由服务端计算 |
| `prompts/agent_3_technical_planner_v2.txt` | 维持只读数值，补明确空状态和场景Schema |
| `server/files.ts`、`server/api.ts` | 返回新状态和阶段明细，不暴露模型原文或密钥 |
| `web/src/App.tsx`、`web/src/styles.css` | 展示路线、证据覆盖、空状态、续跑信息和分阶段报告 |

状态Schema采用新增字段和新增枚举，不原地改写历史运行文件。旧状态读取时映射为 `LEGACY_UNKNOWN` 并保持原文，禁止把历史 `VALIDATED` 自动解释为新语义下的真实执行完成。

SQLite变更只允许新增表、索引或可空字段，并使用显式Schema版本；部署前备份状态库。禁止删除、重命名或批量改写历史审计记录。事实文件保持内容寻址，回滚代码时仍可读取旧事实版本。

### Phase 0：冻结基线

- 固定当前提交、配置哈希、提示词哈希和 2026-08-28 冻结快照。
- 保存当前运行产物作为失败基线。
- 建立 A1=133、A2=0、A3未执行、Kimi/GLM阻断的回归夹具。

### Phase 1：状态与模型恢复

- 修复阶段状态语义和 READY 判定。
- 增加安全的 JSON 类型规范化和缺字段修复请求。
- 完善429、5xx流式回退、容量降档和checkpoint续跑。
- 验证不会重复执行已成功批次。

### Phase 2：A1 月度研究与评分

- 实现 `MonthlyIndustryDecision` 全覆盖。
- 替换 A1 代理评分。
- 增加A1覆盖不足阻断。
- 接入券商金股后验盲测。

### Phase 3：A2 双路线和事实装配

- 实现 `MARKET_CORE` 与 `SUPPLY_CHAIN_ALPHA`。
- 实现真实主题因子和可用权重重算。
- 接入按 A1 子集执行的产业证据装配器。
- 修复模型虚构资金流的自动降级和状态传播。

### Phase 4：A3 非空闭环

- 用非空 A2 冻结池验证分钟历史、120m午休聚合和MA255预热。
- 验证确定性价位回填、四情景和A4确认条件。
- 区分未执行、无机会和无技术形态。

### Phase 5：报告、前端和全量验收

- 完善Markdown分阶段报告。
- 更新前端状态、进度和弹窗字段。
- 本地全测试、冻结快照三模型重放。
- 推送后再单独执行虚拟机部署和一次正式收盘链验证。

## 12. 测试与验收矩阵

### 12.1 单元与合同测试

- G0 每只股票都有判定记录。
- A1未实现因子不能使用代理平均值。
- 月度轮动前20行业全部有纳入/排除/延后决策。
- A1 ACTIVE 缺主营、财务或来源时强制降级。
- A2两条路线互斥且都不能越过A1。
- `MARKET_CORE` 不得冒充瓶颈公司。
- `SUPPLY_CHAIN_ALPHA` 缺完整评分卡时不得进入核心池。
- 缺失资金流不会变成0，也不能由模型填非零值。
- A3价格和风险数值必须等于冻结服务端结果。
- 午休、停牌、涨跌停、新股、北交所只研究和T+1均有黄金样本。
- Kimi字符串型summary可机械修复，业务字段缺失仍阻断。
- lane顺序执行，断点恢复不重复成功调用。

### 12.2 2026-08-28冻结快照验收

- G0全量评估数保持一致，除非质量门版本明确变化。
- 月度轮动前20行业有完整决策，煤炭、电子化学品、有色、科技和医药方向均能看到纳入或排除理由。
- A1 ACTIVE 在100–250范围，且每只都有真实评分来源。
- A2能够区分市场核心和瓶颈Alpha；生产门槛不为凑数放宽。
- 如果A2仍为0，必须证明是市场无机会而不是事实缺失，否则验收失败。
- A3至少在构造的非空A2验收池上完成全流程；正式快照允许因市场条件返回0计划，但必须逐股执行过技术门。

### 12.3 三模型验收

- DeepSeek、Kimi、GLM分别至少完成一次同快照 A1–A3。
- 允许结论不同，不允许输入域、配置和事实版本不同。
- 任一模型失败，报告显示具体失败阶段、尝试、错误分类和checkpoint。
- 三模型结果不得合池后回灌，也不得用多数投票改变单lane结果。

### 12.4 前端与报告验收

- 阶段计数与持久化JSON逐项一致。
- 点击任意阶段可查看代码、名称、原因、证据和血缘。
- A2=0时明确区分数据缺失与市场无机会。
- A3未运行时不得显示已验证。
- Markdown包含A1、A2和A3详细结果入口。

## 13. 部署、续跑和回滚

1. 本地完成代码和全部测试，不直接在虚拟机修改文件。
2. 提交前确认工作区只包含本轮相关变更。
3. 推送到GitHub后，在虚拟机只执行明确提交的拉取。
4. 部署前检查是否有健康运行任务；不直接中断。
5. 若必须中断，先确认最新checkpoint已持久化，再停止并从checkpoint续跑。
6. 重启Node控制面后验证 `/api/health`、版本、进度文件和日志。
7. 先运行冻结快照回放，再运行一次正式close。
8. 回滚时恢复上一提交和兼容的状态Schema；不删除事实缓存、快照和审计产物。

## 14. 完成定义

只有同时满足以下条件，才算本轮修复完成：

- A1 不再使用未声明代理分，月度行业决策可审计。
- A2 双路线生效，不因卡点材料不足误杀全部市场核心。
- A2 数据缺失空池不能显示VALIDATED，A3未执行不能显示VALIDATED。
- A3在非空冻结候选上真实跑通并验证价格来源。
- 三模型在同一冻结快照上各自完成一轮，或准确阻断且可以续跑。
- 券商金股盲测自动生成但不影响运行时选股。
- JSON、Markdown、前端计数和原因完全一致。
- 本地测试、冻结回放、虚拟机健康验证全部通过。
- 全程不连接外部交易。

## 15. 第一轮复核记录

复核范围：业务目标、证据边界、状态语义、实施可操作性和安全边界。

发现并调整：

1. 初稿将 A2 `1–29` 只统一视为降级，可能误伤数据完整但市场机会确实较少的正常交易日。现已拆分为“数据缺口降级”和“市场机会不足但有效”两种状态。
2. A2 双路线可能被误读为放宽主营标准。现已明确两条路线都必须继承 A1 主题、产业节点、主营事实和反证，只豁免市场核心对“稀缺卡点”的强制证明。
3. 初稿未明确当前 `FAST_TRACK=true` 的冲突，现已加入V1关闭约束和配置修改项。
4. 初稿缺少文件级实施地图和历史状态兼容方案，现已补齐。

第一轮结论：架构方向可实施，未发现连接外部交易或用模型补写缺失事实的权限扩张；进入第二轮流程闭环复核。

## 16. 第二轮复核记录

复核范围：A1→A2→A3子集链、空池语义、数据资格、模型恢复、持久化、部署与验收闭环。

发现并调整：

1. 初稿对“真实资金流”只有缺失处理，没有来源资格标准，容易再次把成交额代理当资金流。现已增加字段定义、时间窗口、映射和可复现性四项资格测试，并明确成交额等指标禁止改名替代。
2. 状态枚举存在一个未定义用途的通用 `DEGRADED_UNDERFILLED`，现已删除，只保留数据缺口和市场机会不足两种可解释状态，并补入 `VALIDATED_NO_SETUP`。
3. 初稿没有明确历史SQLite和JSON状态的迁移边界，现已规定只做加法迁移、保留旧审计原文和部署前备份。
4. 性能目标可能被误用为缩小股票池的理由，现已明确SLO超时只能优化缓存、投影、续跑和调用效率，不能裁剪研究范围或证据周期。

第二轮结论：流程、状态、数据和发布门禁已闭环；方案可以进入分阶段实施。实施时必须先完成Phase 1状态修复，再进行A1/A2业务改造，避免新结果继续被旧状态误报。

## 17. 剩余风险

1. **免费/公开数据稳定性**：跨市场、美元指数、降息概率和板块资金流可能持续不可用。系统必须允许降级，但不能把缺失伪装为零或代理值。
2. **产业瓶颈可证实性**：供应商集中度、客户认证和扩产难度通常散落在年报、公告、问询和产业资料中，不能保证每个主题都形成足够的 `SUPPLY_CHAIN_ALPHA`；这不影响证据完整的 `MARKET_CORE`。
3. **A1估值数据**：一致预期或历史估值分位可能缺失。可用权重不足时只能观察，首次修复后A1数量可能暂时下降，需要通过补数据而不是降门槛解决。
4. **券商资料授权与点时性**：券商金股只能使用有来源、发布时间和使用权限的清单；无法证明发布时间的数据不能用于历史盲测。
5. **模型供应商波动**：免费Kimi和GLM仍可能限流、超时或改变输出兼容性。机械修复可以提高成功率，但不能保证外部服务可用，也不能由其他模型替代其结论。
6. **双路线偏差**：`MARKET_CORE`可能偏向高成交和历史知名公司，`SUPPLY_CHAIN_ALPHA`可能偏向披露充分的公司。验收需分别统计市值、行业和证据覆盖分布。
7. **历史回放偏差**：当前同花顺行业成员和部分宏观序列未必能完整恢复历史披露时点。无法证明点时性的回放必须标记不可靠。
8. **首次完整A3样本不足**：正式市场快照可能合法没有计划，因此还必须保留一个非空冻结验收池验证技术链，但不得把构造样本冒充正式推荐结果。
9. **状态兼容风险**：前后端、SQLite和JSON若不是同一提交部署，可能出现未知状态。API必须对未知枚举失败关闭并显示原始状态。
10. **部署中断风险**：拉取和重启会中断正在运行的模型请求。必须依赖阶段/批次checkpoint，在持久化确认前不得停止健康任务。

## 18. 本地实施与验证记录（2026-08-28）

本轮已完成 Phase 1–5 的本地代码改造，尚未提交、推送或部署虚拟机。

已实施：

1. A1 改为真实因子可用权重门禁，关闭快速通道，保留全 G0 判定记录，并增加月度前20行业逐项纳入、排除或延后决策合同。
2. A2 实现 `MARKET_CORE` 与 `SUPPLY_CHAIN_ALPHA` 双路线；资金流缺失保持不可用，模型填入非零资金流分时主题失效、候选降级。
3. A1/A2/A3 状态拆分为无机会、数据缺口、市场不足、无技术形态、上游未运行及模型/技术数据阻断，禁止下游伪装为已执行。
4. `READY_DEGRADED` 只允许市场机会不足且数据完整的 lane 发布；数据缺口 lane 继续失败关闭。
5. 输出新增 A1/A2/A3 分阶段 Markdown，前端阶段详情新增 A2 路线、瓶颈状态、因子覆盖率及新状态中文语义。
6. 券商月度金股作为独立盲测基准接入，不参与运行时选股和阈值调整。

本地验证：

- Python 全量测试通过：459项。
- 前端测试通过：25项；TypeScript 类型检查和生产构建通过。
- Python `compileall` 与 `git diff --check` 通过。
- 使用 `snapshot-20260827T024824+0800-e0026d45a7ec` 做离线确定性全市场验证：A1评估3886只，耗时1.604秒；A2处理28只A1复核候选，耗时0.657秒；A3对3只非空A2候选逐股执行并因冻结快照缺少技术因子和价位合同而明确失败关闭，没有伪装成“无机会”或“已验证”。

尚待独立授权后完成：

- 同一新鲜冻结快照的三模型真实 A1→A2→A3 调用验收。
- Git提交、推送、虚拟机部署、Node重启和正式close验证。
