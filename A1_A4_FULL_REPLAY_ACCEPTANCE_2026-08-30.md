# A1-A4 全量回放、模型选型与 8 月 31 日运行验收

> 验收日期：2026-08-30（Asia/Shanghai）  
> 数据截面：2026-08-28 收盘  
> 性质：内部研究与模拟盘，未连接真实证券账户，未发送真实委托。

## 1. 结论摘要

1. 2026-08-28 全量 A1→A2→A3 主链已经完整执行并落盘，主模型 DeepSeek 为 `READY_DEGRADED`；“降级”来自 A2 市场机会数低于目标，不是数据空值或流程中断。
2. A3 候选域修复后为 39 只，其中 38 只技术数据完整并进入 LLM 复核，1 只因技术数据不就绪硬拒绝；没有全市场裁剪或人为限量。
3. 修复后 DeepSeek A3 完成 3 个批次和 3 次结构化语义尝试，结果为 `VALIDATED_NO_SETUP`：`0 core / 1 secondary(NO_ENTRY) / 38 rejected`。
4. 当前数据截面下不存在可合规发布的 8 月 31 日入场计划。计划表为 0 是真实无机会结果，不是发布失败。
5. 8 月 31 日被交易日历确认为交易日，Node 调度器已启用；09:26 早盘复核和交易时段每分钟 A4 会正常调度。但在没有活动计划时，A4 只能记录 `EMPTY_SCOPE`，不能产生有效买入信号。
6. 当前生产选型建议为：**A1 DeepSeek，A2 DeepSeek，A3 DeepSeek**。Kimi/GLM 保留为离线对比，不参与主结果发布。

## 2. 审计身份

| 项目 | 值 |
|---|---|
| 冻结快照 | `snapshot-20260828T210944+0800-6c5ba68b1490` |
| 原始三模型全量运行 | `2026-08-28-close-6c5ba68b1490` |
| 修复后 DeepSeek 全链运行 | `2026-08-28-close-ade0425-primary` |
| 最终 A3 续跑 | `2026-08-28-a3-resume-db53fca` |
| 部署主分支 HEAD | `52fee5f2ce03798ae1abefda246266327986a004` |
| A3 计划修复 | `ade04256a5ceb9ef3f5d56cb2d91e645880412c7` |
| A3 结构化补全修复 | `2a2dea7469c4421b9d66b6e84db5920585ca7384` |
| A3 可审计续跑/发布 | `dd7ff079b929c18b4975c413f7af7669eecde482` |
| A3 第三次有界语义修复 | `db53fcae443fd34b03b6161d48222ad8fda48a75` |

## 3. 三模型全量结果

| 模型 | A1 | A2 | A3 | 结论 |
|---|---:|---:|---:|---|
| `deepseek-v4-pro-0813` | VALIDATED，95 只 | VALIDATED_UNDERFILLED_MARKET，5 focus + 46 watch | VALIDATED_NO_SETUP | 唯一在时限内完整跑通的模型 |
| `moonshotai/kimi-k3-free` | BLOCKED，600080 ms | 未执行 | 未执行 | A1 月度主题/产业链合同不完整，并超时 |
| `z-ai/glm-5.3-free` | BLOCKED，600016 ms | 未执行 | 未执行 | A1 合同不完整，13 次传输尝试后超时 |

修复后的 DeepSeek 主链数据：

| 阶段 | 确定性输入/处理 | LLM/最终池 | 耗时 | 状态 |
|---|---|---|---:|---|
| A1 | 4017 只处理；93 只送 LLM；3837 只监控；19 只硬拒绝 | 90 active + 3871 monitor + 56 rejected | 1692061 ms | VALIDATED |
| A2 | 90 只处理；60 只送 LLM；21 只本地观察；9 只硬拒绝 | 6 focus + 44 watch + 40 rejected | 435443 ms | VALIDATED_UNDERFILLED_MARKET |
| A3 | 39 只候选；38 只技术就绪；1 只技术硬拒绝 | 首轮 0 core + 8 secondary + 31 rejected | 520168 ms | VALIDATED_NO_SETUP |
| A3 合同修复续跑 | 同一 39/38 只候选域 | 0 core + 1 secondary(NO_ENTRY) + 38 rejected | 350267 ms | VALIDATED_NO_SETUP |

## 4. A3 为什么没有发布计划

拒绝原因已与冻结 `PRICE_LEVELS`、日线和 5 分钟因子逐项核对：

- 低盈亏比为主要原因：`REWARD_RISK_BELOW_MINIMUM` 17 次，`RR_BELOW_2` 13 次。快照复算证明 `RR_BELOW_2` 的对应标的均低于 2，不是模型误拒。
- 剩余盈亏比合格标的被周线/120 分钟下行、空头排列、主趋势修复或技术分不足拒绝。
- 唯一 secondary `300305.SZ 裕兴股份` 的冻结盈亏比为 2.6842，止损距离 3.04%，但技术分为 57.5，周线 `BEAR_STACK`，所以是 `NO_ENTRY`，不具备发布资格。
- 发布器结果为 `created=[]`，并明确记录 `300305.SZ: SECONDARY_NOT_PROBE`。SQLite 计划表为 0，`PRAGMA quick_check` 和 `integrity_check` 均为 `ok`。

因此，从项目目标看，正确行为是“不发布买入计划”，而不是放宽技术分、盈亏比或趋势门槛来强行凑单。

## 5. 模型选型建议

### A1：DeepSeek

- 能在同一冻结数据上完成月度宏观/政策、产业主线和个股映射合同。
- Kimi 和 GLM 在 600 秒内未完成 A1 发现合同，不能作为 09:25/收盘主链模型。

### A2：DeepSeek（生产建议，仍需阶段隔离基准）

- DeepSeek 已实际完成 90→60 只模型复核，最终落盘 6 focus + 44 watch。
- Kimi/GLM 在全链中未通过 A1，因而没有同等的 A2 生产证据。当前选 DeepSeek 是可运行性选择，不等于已证明其 A2 策略质量绝对最优。

### A3：DeepSeek

- DeepSeek 在修复后 350267 ms 内完成三批次、通过结构与冻结事实校验。
- Kimi 的隔离 A3 比较运行超过 600 秒仍未返回，终止时约 11 分钟。
- GLM 的隔离 A3 比较同样超过 600 秒，终止时约 10 分钟。
- 这两次只是隔离比较，未发布计划，未修改运行库。

## 6. 8 月 31 日 A4 准备度

| 检查 | 结果 |
|---|---|
| 8 月 31 日 XSHG 交易日历 | `True` |
| Node 健康 | `status=ok` |
| Node 调度器 | `.env: LIANGJIAN_SCHEDULER_ENABLED=true`，日志已记录“Node 调度器已启动” |
| 早盘复核 | 09:26 调度 |
| A4 盯盘 | 交易时段每分钟调度，研究保护窗口除外 |
| A4 思考模式 | 代码默认关闭，监控模型仅具 veto 权，不能创造买入触发 |
| 非空计划生命周期测试 | 虚拟机 `tests/test_acceptance_nonempty_plan.py` 通过 |
| 运行库 | `quick_check=ok`，`integrity_check=ok` |

非空计划验收测试证明了：`PENDING_MORNING_REVIEW → 09:26 ACTIVE_TODAY → 09:32/09:33 两根 1m 确认 → BUY_SIGNAL → 模拟成交 → 次日模拟卖出`。测试仅使用临时 SQLite 和构造行情，不会污染生产运行库。

## 7. 项目目标验收

| 目标 | 结论 | 说明 |
|---|---|---|
| 全市场确定性初筛 | 通过 | 4017 只完整处理，未用测试上限裁剪 |
| A1 月度政策/产业主线 | 工程通过 | DeepSeek 完成且月度策略状态 READY；策略效果仍需多日回放 |
| A2 资金/梯队/龙头/产业链共振 | 部分通过 | 关键市场结构覆盖率为 1.0；`SUPPLY_CHAIN_ALPHA` 覆盖仍为 0，属于真实待补数据缺口 |
| A3 技术/价格/风险合同 | 工程通过 | 38/39 技术就绪，输出通过校验 |
| 8 月 31 日可执行计划 | **未达成（真实无机会）** | 无合法 CORE/PROBE，禁止伪造或放宽门槛 |
| A4 调度与模拟盘机制 | 通过 | 调度、状态迁移、买卖模拟测试通过 |
| 8 月 31 日 A4 有效入场信号 | **不可预先保证** | 当前无活动计划；即使有计划，也必须等真实 1m 触发且通过 Flash veto |

## 8. 剩余缺陷与建议顺序

### P0

1. **模型硬超时未完全执行。** Kimi/GLM 实测超过 600 秒仍保持流式连接，需在请求进程层加可强制终止的总 wall-clock 截止，不能只依赖迭代器收到新 chunk 时检查时间。
2. **本交易日没有可执行 A3 计划。** 这不应通过放宽门槛修“好”；应在前端明确呈现 `VALIDATED_NO_SETUP / 今日无入场计划`，A4 显示 `EMPTY_SCOPE`而非“阻断”。

### P1

3. **A3 模型技术分存在跨次波动。** 同一冻结数据的早期输出中，`001296.SZ/002138.SZ` 技术分为 74；最终运行却判定低于 70。建议将可量化的七因子分解收归为服务端确定性计算，LLM 仅负责形态/语义 veto 和解释。
4. **A2 稀缺产业链路由未就绪。** `MARKET_CORE` 可用，`SUPPLY_CHAIN_ALPHA available=false, coverage=0`；需继续补齐主营暴露、产能/订单/供应瓶颈及可引用来源。
5. **分阶段模型基准不完整。** Kimi/GLM 因 A1 失败没有全链 A2/A3 数据；本次 A3 隔离比较又超时。模型质量排名仍需在固定上游、硬超时下做 10 个交易日点时回放。

### P2

6. **A2 诊断计数与最终池计数口径不一致。** A2 诊断中 `watch_only_pool=14`，最终输出为 44，原因是后续追加本地观察行后未刷新诊断口径。不影响决策，但会误导前端观测。

## 9. 最终判定

- **工程可运行性：通过。** A1-A3 完整执行、快照/审计/报告落盘、数据库健康、Node 与调度器健康、A4 模拟生命周期测试通过。
- **本交易日策略输出：无入场。** 这是符合冻结事实的策略结论，不能被粉饰为“已生成 31 日可执行单”。
- **用户要求的“确保 31 日 A3 计划并产生 A4 有效信号”：未达成。** 原因不是流程失败，而是当前候选中无合法设置；市场条件和真实分钟触发也不可由系统伪造。

