# 量见 A 股研究漏斗确定性管线 V2 改造方案

> 文档状态：已确认，实施与全量验证中
> 编制日期：2026-08-27
> 适用项目：`liangjian_funnel_workflow`
> 目标管线：`deterministic_v2`

## 1. 文档目的

本文档定义 A 股全市场研究漏斗从现有“全量股票分批交给 LLM”模式，改造为“确定性代码全市场筛选，LLM 只做高价值语义研究与候选复核”的完整实施方案。

本方案首先保证全市场覆盖、数据可追溯和三模型独立，再解决当前 A1 执行数小时、模型调用量过大、断点状态不清晰和前端计数不准确等问题。

本文档仅定义方案、边界和验收标准。在方案确认前，不切换生产工作流。

## 2. 当前运行状态

2026-08-27 已人工停止虚拟机上正在执行的 `2026-08-27-close` A1-A3 研究任务：

- 研究任务服务已停止；
- 权限辅助服务已停止；
- 无残留研究进程；
- 既有进度文件和研究检查点已保留；
- 宝塔 Node 控制台继续正常运行；
- 未删除事实数据、缓存或已有研究结果。

旧检查点不能直接混入 V2 管线。后续只有在管线模式、数据版本、配置、提示词和分类目录哈希全部一致时，检查点才允许复用。

## 3. 核心结论

当前主要问题不是原始数据完全缺失，而是职责边界错误：约 3886 只 G0 股票被按每批约 20 只发送给三个 LLM 逐只分类。财务硬门槛、行业归属、成交能力、主营证据和技术指标等确定性任务被重复交给模型，造成大量重复输入、长输出、重试和尾部延迟。

改造后的核心原则是：

> 全市场必须由确定性代码逐只评估并保存结果，但不要求每只股票都发送给 LLM。

目标包含两层完整性：

1. **审计完整性**：每只 G0 股票都有阶段状态、评分、原因码和证据版本；
2. **研究有效性**：LLM 只处理经过代码筛选的高辨识度候选，不重复执行量化规则。

## 4. 当前问题与证据

### 4.1 A1 全量模型化

现有流程在完整覆盖模式下，将 `selection_limit` 扩展为全部研究记录，并关闭每节点 Top-N 的实际约束。A1 再按照约 20 只股票一批调用模型，单个模型约需 248 至 250 批。

这与项目已有提示词和实施计划中定义的职责不一致：

- 模型应负责宏观、政策、主题和产业链节点发现；
- 行业、概念、主营和股票的确定性挂接应由系统完成；
- 模型只复核各节点的少量 Top-N 候选；
- 不应把 5000 多只股票逐只交给模型。

### 4.2 实际性能不可接受

停止前一次运行的阶段性观测显示：

| 模型 | 已完成 A1 批次 | 已处理股票 | 平均单批耗时 | P95 单批耗时 |
|---|---:|---:|---:|---:|
| DeepSeek | 72 | 1220 | 196.2 秒 | 336.8 秒 |
| Kimi | 24 | 376 | 305.4 秒 | 707.3 秒 |

按当前进度线性估算，单次 A1 可能需要约 8 至 24 小时，并受到 429、5xx、长输出和尾部延迟进一步影响。这种耗时不适合每日收盘工作流。

### 4.3 原始数据不是主要瓶颈

当前本地事实缓存已经具备较好的基础：

- 日线约 215 万条，覆盖约 4086 只股票；
- 财务事实约 33 万条，覆盖约 4086 只股票；
- 同花顺行业约 320 个，成员关系约 1 万条，覆盖约 5551 只股票；
- 公告和 PDF 证据覆盖率约 97%；
- 本地事实库约 1.9 GB。

当前真正缺少的是版本化的派生特征层，包括：

- 概念成员关系的持久化版本；
- 主营业务和收入占比的结构化结果；
- 可直接查询的基本面质量特征；
- 主题内相对强度、龙头和中军辨识度特征；
- 按数据变化进行增量失效和重算的机制。

## 5. 改造原则

### 5.1 全市场不静默裁剪

P0 和 G0 覆盖完整证券目录。每只进入 G0 的股票都必须经过确定性规则并保存结果。未进入模型复核的股票仍保留本地状态、评分和原因，不得直接消失。

### 5.2 代码负责数值，模型负责语义

由代码负责：

- 财务指标和硬门槛；
- 行业、概念和主营映射；
- 成交能力和技术指标；
- 龙头、中军和相对强度评分；
- 入场、止损、盈亏比等数值计算。

由模型负责：

- 宏观与政策含义；
- 结构性主题和产业链节点发现；
- 业务受益逻辑复核；
- 主题持续性、预期差和风险语义判断；
- 基于确定性数值生成条件化交易计划。

### 5.3 模型只能收紧，不能越权扩池

始终满足：

```text
A3 ⊆ A2 ⊆ A1 ⊆ Q1 ⊆ G0 ⊆ P0
```

LLM 可以通过、降级、拒绝或要求补充研究，但不能加入上游本地候选集之外的股票。

### 5.4 三个研究 lane 严格独立

DeepSeek、Kimi 和 GLM 使用同一冻结事实快照、同一配置版本和同一提示词版本，但各自独立执行：

- 宏观主题发现；
- 主题到本地股票的映射；
- A1、A2、A3 候选和结果；
- 检查点与失败状态。

三个 lane 不投票、不合并股票池、不共享模型输出。

### 5.5 缺失数据必须显式降级

不得把缺失值填为 0，不得由模型猜测主营收入占比、价格、止损位或财务数据。缺失数据通过原因码进入观察、研究限制或阻断状态。

### 5.6 零候选是合法结果

任一 lane 在任一阶段输出 0 只股票都可以是正常业务结果，不能为了保持数量而放宽规则或引入上游之外的股票。

## 6. 目标工作流

```text
P0 全市场证券目录
  ↓
G0 确定性质量门
  ↓
M0 月度宏观大类资产四象限（代码）
  ↓
W1 周度政策/产业周期/跨市场线索（每 lane 一次 LLM 发现）
  ↓
Q1 本地 A1 全市场基本面、主营映射与覆盖扩展
  ↓
A1 LLM 基本面候选复核
  ↓
Q2 本地 A2 主题强度与角色辨识
  ↓
A2 LLM 主题情绪与辨识度复核
  ↓
Q3 本地 A3 技术面与交易条件计算
  ↓
A3 LLM 条件化交易计划复核
  ↓
A4 Flash 盘中否决式盯盘
```

## 7. 阶段详细设计

### 7.1 P0：全市场证券目录

以同花顺完整证券目录为主要来源，保留约 5500 只股票的基础信息：

- 股票代码和名称；
- 交易所和板块；
- 上市日期；
- ST、退市整理和停牌状态；
- 同花顺行业；
- 同花顺概念；
- 数据更新时间、来源和版本哈希。

P0 不进行策略选择，只负责完整、去重、规范化的证券目录。

### 7.2 G0：确定性质量门

代码逐只检查：

- ST、退市整理；
- 停牌和无法交易；
- 新股交易限制；
- 无效价格、成交量和成交额；
- 日成交额低于 5000 万元；
- 基础行情和关键数据可用性。

北交所不因交易所身份直接排除，但必须通过同一套质量规则。

每只股票保存 `PASS` 或 `REJECT`，并写入标准原因码，例如：

```json
{
  "symbol": "600000",
  "status": "PASS",
  "reason_codes": [],
  "source_versions": {
    "security_catalog": "...",
    "daily_bar": "..."
  }
}
```

### 7.3 M0/W1：月度宏观择时与每模型一次主题发现

M0 先由确定性代码计算股票、黄金、债券和现金的 20/60 日动量、资金指标与宏观倾斜。缺失项保持缺失，模型无权覆盖四象限结果。W1 再由每个研究模型输入：

- 国内外宏观事件；
- 政策和监管信息；
- 财经资讯；
- 行业景气信息；
- 同花顺行业目录；
- 同花顺概念目录；
- 数据截止时间。

输出只包含：

- 宏观或政策驱动；
- 结构性主题；
- 产业链节点；
- 合法的行业和概念代码；
- 主题逻辑、验证条件和失效条件。

模型不得在 M1 直接输出股票池。服务端必须验证行业和概念代码是否存在于本次冻结目录；无法映射的主题只能标记为研究线索，不能自动挂接股票。

### 7.4 Q1：本地 A1 确定性筛选

代码对全部 G0 股票执行：

- 行业和概念节点匹配；
- 主营业务和产业链相关度；
- 主营收入占比和证据完整度；
- 营收、利润增长；
- ROE、毛利率和净利率；
- 经营现金流质量；
- 资产负债率和流动性；
- 风险公告；
- 当前成交能力。

每只股票必须获得以下状态之一：

| 状态 | 含义 |
|---|---|
| `OUTSIDE_THEME` | 不属于当前模型发现的主题 |
| `HARD_REJECT` | 命中财务、风险或数据硬门槛 |
| `LOCAL_MONITOR` | 有相关性，但证据、得分或排名不足 |
| `REVIEW_CANDIDATE` | 进入 A1 模型复核范围 |

保留两级候选：

- 本地线索池：先以每个产业链节点前 15 只保证行业分散；当精确主营收入证据充足但总覆盖低于 100 只时，按全局稳定排序自适应扩展至最低覆盖线；
- LLM 复核池：每个主题最多 8 个代表样本，优先复核主营证据尚未结构化的候选。LLM 复核数量不是 A1 总池的审批配额。

未进入 LLM 的股票保存为 `LOCAL_MONITOR / NOT_SENT_TO_LLM`，同时保留评分、节点排名、原因和重新评估条件。

这不是仅为性能设置的隐藏截断，而是明确、可审计的研究优先级。全部 G0 均保存判定记录；A1 正式结果低于 100 只时产生 `A1_ACTIVE_COVERAGE_UNDERFILLED` 并阻断发布，不能通过降低数据质量、主题相关度或基本面门槛凑数。

### 7.5 A1：LLM 基本面候选复核

模型只接收 Q1 高排名候选的紧凑卡片，复核：

- 主营业务是否真正受益；
- 业绩兑现逻辑是否可信；
- 催化剂是否有效；
- 事实之间是否存在冲突；
- 是否降级、拒绝或要求继续观察。

模型不能修改财务数值，不能填补缺失主营占比，也不能加入 Q1 之外的股票。

### 7.6 Q2：本地 A2 主题强度与角色引擎

仅处理同一 lane 的 A1 通过股票，代码计算：

- 板块趋势持续性；
- 板块宽度和上涨家数；
- 板块成交额变化；
- 个股相对板块和大盘强度；
- 启动时间和先发优势；
- 回撤承接能力；
- 成交容量；
- 主题内排名；
- 龙头、中军、补涨和跟风角色；
- 已有事实可支持的需求拐点、业务耦合、证据质量和催化时点；
- 产业链卡点评分的已知项、未知项和证据就绪度；
- 拥挤度和可交易性风险。

确定性层不得凭行情热度推断供应商集中度、扩产难度或客户认证。上述字段没有
公告、财报、项目文件或可信产业资料时保持未知，由 A2 复核补证；未知不得写成 0。

输出示例：

```json
{
  "symbol": "600000",
  "role": "MID_ARMY",
  "role_score": 82.4,
  "theme_rank": 2,
  "reason_codes": [
    "HIGH_LIQUIDITY",
    "RELATIVE_STRENGTH",
    "THEME_CORE_MEMBER"
  ]
}
```

### 7.7 A2：LLM 主题情绪复核

模型只复核 Q2 高排名候选，判断：

- 先排价值链层级，再排公司；
- 哪个环节是供应商少、验证慢、扩产难或不可绕开的真实卡点；
- 标的是控制卡点、供应卡点、一般受益还是仅有题材叙事；
- 主题是主线还是短期脉冲；
- 主题是否仍在扩散；
- 龙头或中军辨识度是否成立；
- 市场预期是否已过度交易；
- 是否需要降级为观察；
- 主题失效条件。

本阶段吸收 MIT 项目 `muxuuu/serenity-skill` 的公开研究结构，但不导入其示例
股票或把模型叙事当事实。每个 focus 标的必须至少有两条冻结证据，其中至少一条
为强或中等证据，并给出缺失证明和降级条件。八因子评分由模型解释、服务端按固定
权重重算；证据不足的标的只能进入观察池。

### 7.8 Q3：本地 A3 技术和交易条件引擎

仅处理 A2 通过股票，代码计算：

- 日线趋势结构；
- 1m、5m、15m 多周期状态；
- 均线、量价和波动率；
- 突破和回踩条件；
- 支撑和压力；
- 入场触发条件；
- 止损位置；
- 盈亏比；
- 最大允许追高幅度；
- 仓位上限建议；
- 价格和数据是否过期。

交易数值必须由代码根据冻结行情计算，模型不得凭文本创造价格。

### 7.9 A3：LLM 条件化交易计划复核

模型只基于 Q3 的确定性结果整理：

- 入场前置条件；
- 触发方式；
- 禁止追高条件；
- 止损和退出逻辑；
- 计划失效条件；
- 风险说明。

没有满足条件的计划时，输出 0 只股票属于合法结果。

### 7.10 A4：盘中盯盘

维持现有业务边界：

- 模型使用 `deepseek-v4-flash-0731`；
- 关闭思考模式；
- 交易时段每分钟运行一次；
- 只允许否决、收紧或标记失效；
- 不允许新增股票、扩大仓位或生成独立候选；
- 不连接真实交易；
- 仅保存产生有效变化的结果。

## 8. 本地数据与特征存储

### 8.1 原始事实库

继续使用：

```text
storage/facts/market_fact_cache.sqlite3
```

原始事实库保存行情、财务、证券目录、行业、公告和证据等可追溯数据，不承担频繁变化的策略派生结果。

### 8.2 新增派生特征库

新增：

```text
storage/features/research_feature_store.sqlite3
```

建议包含以下表。

#### `taxonomy_membership_versions`

保存行业和概念成员关系、生效日期、失效日期、数据哈希、完整度和来源。现有同花顺行业采集器应泛化为同时支持 `industry` 与 `cn_concept`。

#### `theme_registry_versions`

保存每个模型 lane 发现的主题、版本、数据截止时间、提示词哈希和模型信息。

#### `chain_node_versions`

保存主题下产业链节点、定义、有效期和失效条件。

#### `theme_taxonomy_links`

保存模型主题与合法同花顺行业、概念代码之间的映射及验证结果。

#### `business_exposure_facts`

保存股票、报告期、产品或业务、收入占比、毛利占比、产业链节点、公告 ID、PDF 页码、证据引用、解析器版本和置信度。

主营证据采用“确定性表格或规则解析优先”。无法解析时保存缺失状态，不能让模型猜测。以后如引入一次性抽取模型，也必须绑定原文页码、解析版本和置信度。

#### `stock_fundamental_features`

保存基本面指标、数据可用性、报告期和来源版本。

#### `stock_market_role_features`

保存主题相对强度、板块内排名、龙头或中军评分、成交容量、回撤承接和行情特征。

#### `deterministic_stage_decisions`

保存 `run_id`、`model_lane`、`stage`、股票代码、状态、评分、排名、原因码、证据引用和数据版本。该表同时是前端阶段详情和审计追溯的主要数据源。

#### `dirty_entities`

保存需要增量重算的股票、节点或主题，以及触发原因和源数据版本。

## 9. 增量更新策略

### 9.1 每周完整重建

每周最后一个交易日收盘后：

- 更新全部原始数据；
- 重建全市场确定性特征；
- 对全部 G0 股票重新评分；
- 重建行业、概念和主营映射；
- 校验覆盖率和缺失率。

完整重建仍只把高排名候选发送给 LLM。

### 9.2 每日 15:10 收盘研究

```text
增量事实更新
→ 脏实体识别
→ 每模型宏观主题发现
→ Q1 确定性筛选
→ A1 少量复核
→ Q2 角色计算
→ A2 少量复核
→ Q3 技术计算
→ A3 交易计划复核
→ 结果落盘
```

### 9.3 每日 09:26 早盘复核

早盘不执行完整 A1-A3，只针对前一收盘计划：

- 检查集合竞价；
- 检查停牌和可交易性；
- 检查跳空和追高风险；
- 更新入场限制和失效条件；
- 允许取消计划；
- 不允许新增昨晚计划之外的股票；
- 只收紧，不放宽。

### 9.4 盘中每分钟

A4 只对当前仍有效的计划执行盯盘。无有效变化不保存结果。

## 10. 模型调用、重试与输出控制

### 10.1 调用规模目标

单个研究 lane 的建议调用规模：

| 阶段 | 建议调用量 |
|---|---:|
| M1 宏观发现 | 1 次 |
| A1 候选复核 | 约 3 至 12 批 |
| A2 主题复核 | 约 1 至 5 批 |
| A3 计划复核 | 约 1 至 3 批 |

### 10.2 输入控制

- 不允许任一模型批次包含全部 G0；
- 候选卡使用紧凑结构，只传该阶段必需事实；
- 大型原始快照只在本地引用，不重复嵌入每个请求；
- 请求前执行 token 和字节数预检；
- 超过容量时分批并保存明确批次边界。

### 10.3 输出控制

保留当前输出上限策略：

- 首次请求最多 384K；
- 因输出或网关容量限制失败时依次降至 256K、128K；
- 只有 128K 仍被明确拒绝时，才以 `OUTPUT_BUDGET_FALLBACK_REJECTED` 失败关闭；
- 正常输出依靠紧凑 Schema 和少量候选控制，而不是依靠截断。

### 10.4 失败处理

- 429：读取 `Retry-After`，使用带抖动的指数退避后自动重试；
- 5xx：优先使用流式传输重试；
- 网络超时：在同一幂等请求键下重试；
- JSON 不完整：在保留原始响应摘要后进行一次修复或缩小批次；
- 达到最大重试次数：只标记当前批次失败，不伪造 0 只结果；
- 上游阶段未验证：下游不得运行。

## 11. 检查点与恢复

进度阶段统一为：

```text
G0_READY
MACRO_DISCOVERY
A1_LOCAL_SCREEN
A1_LLM_REVIEW
A2_LOCAL_ROLE
A2_LLM_REVIEW
A3_LOCAL_TECHNICAL
A3_LLM_REVIEW
READY
```

检查点必须绑定：

- `pipeline_mode`；
- 数据快照哈希；
- 配置哈希；
- 提示词哈希；
- 行业和概念目录哈希；
- 主题树版本；
- 特征计算版本；
- 模型与参数版本。

恢复规则：

- 已完成且哈希一致的阶段直接复用；
- 只补跑失败或缺失批次；
- 数据版本变化只失效受影响股票、节点或主题；
- `legacy` 与 `deterministic_v2` 检查点禁止混用；
- 进程被停止后，前端应显示“已停止/可恢复”，不能长期显示“运行中”。

## 12. 前端可观测性

研究页面按阶段展示：

- 输入股票数；
- 已完成确定性评估数；
- 硬淘汰数；
- 本地观察数；
- 发送 LLM 数；
- LLM 通过、降级和拒绝数；
- 当前批次和总批次；
- 失败数和重试数；
- 已用时间和预计剩余时间；
- 最近更新时间和数据版本。

页面明确区分：

```text
代码筛选结果
LLM 复核结果
```

点击阶段或数量后使用大尺寸弹窗展示：

- 股票代码和名称；
- 当前阶段状态；
- 本地评分和节点排名；
- 入选或淘汰原因；
- 所属主题和产业链节点；
- 龙头或中军角色；
- 财务摘要；
- 主营业务证据；
- 公告引用；
- 模型复核意见；
- 风险和失效条件；
- 是否发送过 LLM。

“股票计数未提供”必须被消除。若进度文件损坏，前端应回退到检查点和阶段决策表聚合，显示数据来源和更新时间，而不是直接声明进度不可用。

## 13. 预计代码改造范围

优先复用：

- `src/liangjian_funnel/pipeline/local_fact_cache.py`
- `src/liangjian_funnel/pipeline/market_aggregates.py`
- `src/liangjian_funnel/pipeline/research.py`
- `src/liangjian_funnel/workflow.py`
- `src/liangjian_funnel/runtime/progress.py`

建议新增：

- `src/liangjian_funnel/pipeline/feature_store.py`
- `src/liangjian_funnel/pipeline/taxonomy.py`
- `src/liangjian_funnel/pipeline/a1_screen.py`
- `src/liangjian_funnel/pipeline/a2_role_engine.py`
- `src/liangjian_funnel/pipeline/incremental.py`
- 确定性阶段决策与原因码 Schema。

建议调整：

- 将 `data/ths_industry.py` 泛化为行业和概念通用分类采集器；
- 重构 `pipeline/research.py`，拆开宏观发现与候选复核；
- 重构 `workflow.py`，由明确阶段编排取代全量 A1 模型批处理；
- 扩展 `runtime/progress.py` 的阶段、计数、ETA 和停止状态；
- 调整 `server/files.ts`、`server/types.ts`、`server/api.ts`；
- 调整 `web/src/App.tsx`、类型定义和样式；
- 提示词主体尽量保持不变，主要调整运行时注入上下文和输出 Schema。

## 14. 实施阶段

### 阶段一：契约和存储

- 增加 `LIANGJIAN_RESEARCH_PIPELINE_MODE=legacy|deterministic_v2`；
- 定义阶段状态、原因码和结果 Schema；
- 建立派生特征库；
- 建立版本、幂等和增量失效机制；
- 旧工作流继续可回滚。

### 阶段二：数据特征化

- 泛化同花顺行业和概念采集；
- 建立主营业务证据表；
- 物化基本面特征；
- 物化主题和行情特征；
- 执行一次全量特征回填；
- 输出覆盖率和缺失率报告。

### 阶段三：A1 重构

- M1 只发现主题和节点；
- Q1 代码覆盖全部 G0；
- 建立每节点本地 Top 15 线索池；
- 默认每节点 Top 3 进入 LLM；
- 保存所有未发送模型股票的状态和原因。

### 阶段四：A2 和 A3 重构

- 建立本地主题强度和角色引擎；
- A2 只做语义和情绪复核；
- A3 的价格与风险数值全部由代码生成；
- LLM 只形成条件化交易计划。

### 阶段五：恢复、调度和前端

- 完成阶段级和批次级断点恢复；
- 完成 429、5xx 和网络错误重试；
- 实现周度全量、每日增量和早盘收紧；
- 修复进度、计数和停止状态；
- 完成阶段股票详情弹窗。

### 阶段六：影子验证与切换

- 使用同一冻结快照运行 V2；
- 使用已有旧检查点作为对比证据，不重新执行旧全量 LLM 流程；
- 验证全市场决策完整性；
- 比较候选质量、调用量和耗时；
- 验收通过后切换默认模式；
- 短期保留 `legacy` 回滚开关。

## 15. 性能目标

| 模块 | 目标 |
|---|---:|
| G0 全市场质量门 | 30 秒以内 |
| 热缓存全量 Q1 | 120 秒以内 |
| 日常脏数据比例低于 5% 时的 Q1 | 60 秒以内 |
| 收盘完整流程 P95 | 45 分钟以内 |
| 收盘流程硬截止 | 60 分钟 |
| 早盘复核 | 3 分钟以内 |

超过硬截止的研究结果不得以完整计划发布，应保留中间结果并明确标记超时。

## 16. 验收标准

### 16.1 数据完整性

- 每只 G0 股票都有确定性状态和原因；
- `evaluated_count == G0_count`；
- 无静默截断；
- 缺失值不填 0；
- 回放不使用未来数据；
- 行业、概念和主营证据均绑定版本；
- 所有依赖主营受益逻辑的 ACTIVE 股票都存在有效证据引用。

### 16.2 流程约束

- `A3 ⊆ A2 ⊆ A1 ⊆ Q1 ⊆ G0`；
- LLM 不能加入本地候选池之外的股票；
- 三个 lane 不共享模型输出；
- 0 只候选是合法结果；
- 早盘只收紧；
- A4 只否决；
- 不连接外部真实交易。

### 16.3 性能

- 热缓存全市场确定性筛选不超过 120 秒；
- 不存在向模型发送全部 G0 的批次；
- 请求前执行输入容量预检；
- 收盘流程达到 P95 45 分钟目标；
- 超过 60 分钟不发布不完整计划。

### 16.4 恢复能力

- 重启后从有效阶段或批次检查点继续；
- 单批失败只补该批；
- 源数据变化只失效受影响实体；
- 旧管线和新管线检查点不可混用；
- 停止后前端状态能够正确收敛。

### 16.5 前端一致性

- 页面数量与阶段决策表一致；
- 每个阶段可查看股票代码、名称和原因；
- 明确展示代码筛选与模型复核的区别；
- 进度文件异常时有安全回退；
- 不再出现无法解释的“股票计数未提供”。

## 17. 测试策略

### 17.1 单元测试

- G0 原因码和边界值；
- 财务硬门槛和缺失数据；
- 主题到行业、概念成员映射；
- 主营证据解析和页码绑定；
- Top-N 排名、同分和候选爆炸；
- A1-A3 子集约束；
- 脏实体失效传播；
- 检查点哈希匹配。

### 17.2 集成测试

- 使用冻结快照完成单 lane 全流程；
- 三 lane 独立并发；
- 429、5xx、超时和残缺 JSON；
- 进程中断后恢复；
- 某一 lane 0 候选；
- 某一数据源不完整时的安全降级；
- 收盘结果到早盘只收紧；
- A4 不得创建候选。

### 17.3 回放验证

- 选择已完成交易日；
- 固定所有事实的 `as_of` 时间；
- 验证无未来数据泄漏；
- 对比旧运行已有检查点与新管线结果；
- 记录每阶段数量、耗时、失败率和原因分布。

### 17.4 前端验证

- 正常运行、失败、停止、恢复和完成状态；
- 大弹窗数据准确性；
- 长列表分页或虚拟滚动；
- 移动端或窄屏基本可用性；
- API 和文件异常时不误报成功。

## 18. 发布、回滚与安全边界

### 18.1 发布

1. 本地完成实现和测试；
2. 构建前端和 Node 服务；
3. 执行 V2 冻结快照影子验证；
4. 提交并推送指定分支；
5. 虚拟机拉取代码，但不自动启用 V2；
6. 回填派生特征库；
7. 校验覆盖率和哈希；
8. 切换 `deterministic_v2`；
9. 执行首轮 A1-A3；
10. 验证结果落盘和前端状态。

### 18.2 回滚

- 通过 `LIANGJIAN_RESEARCH_PIPELINE_MODE` 切回 `legacy`；
- 保留原始事实库；
- V2 派生库独立，不污染旧运行数据；
- 不复用不兼容检查点；
- 回滚只影响研究管线，不影响 Node 控制台和已有模拟账户数据。

### 18.3 安全边界

- 只运行内部模拟账户；
- 不连接外部券商或真实交易；
- 模型不能绕过确定性质量门、风险管理器或模拟经纪层；
- 部署、重启和历史数据清理均需要单独授权。

## 19. 两轮方案复核

### 19.1 第一轮：架构和业务复核

复核结论：

- 全市场覆盖通过确定性决策表保留，没有因为性能而裁剪股票池；
- LLM 从“逐股分类器”恢复为宏观发现和高价值复核器；
- 三模型 lane 保持独立；
- 数值计算、证据版本和模型判断边界清晰；
- A1-A3 子集关系和 A4 否决边界未改变；
- 真实交易仍不在项目范围内。

### 19.2 第二轮：失败、偏差和可恢复性复核

复核结论：

- Top-N 是可解释的业务排名边界，未入选股票仍完整落盘；
- 同分或候选爆炸不会被静默截断；
- 主营、概念、自由流通市值或资金数据缺失时显式降级，不伪造事实；
- 确定性评分规则必须版本化，避免形成不可审计的隐藏偏差；
- 数据更新通过脏实体传播，避免全量重复计算和陈旧缓存；
- 超时、停止和失败均有明确状态，不能把未完成结果展示为 0 只；
- 点时回放必须锁定事实版本，防止未来数据泄漏。

## 20. 建议确认的默认参数

| 决策项 | 建议默认值 |
|---|---|
| 本地每节点线索池 | Top 15 |
| A1 正式研究池 | 100 至 250；低于 100 阻断发布 |
| A1 线索与观察池 | 300 至 800；属于容量目标，不是凑数配额 |
| LLM 代表复核池 | 每主题最多 8 只，不决定本地研究池总量 |
| 模型关系 | 三 lane 独立，不投票 |
| 完整重建 | 每周最后一个交易日收盘后 |
| 日常运行 | 每日 15:10 增量研究 |
| 早盘运行 | 09:26，只收紧前一收盘计划 |
| 主营证据缺失 | `LOCAL_MONITOR/RESEARCH_ONLY` |
| 收盘 P95 目标 | 45 分钟 |
| 收盘硬截止 | 60 分钟 |
| 迁移方式 | 特征回填、影子验证、开关切换 |

## 21. 最终预期

完成改造后，工作流应实现：

1. 约 5500 只全市场目录完整保留；
2. 全部 G0 股票由代码逐只评估并可追溯；
3. LLM 调用从数百批下降到每 lane 的少量高价值请求；
4. A1 从数小时或十几小时下降到分钟级或可控的几十分钟；
5. 每一阶段都能查看股票代码、名称、评分、原因和证据；
6. 数据更新以本地落盘和增量计算为主；
7. 中断后能够安全恢复；
8. 早盘和盘中模型严格遵守只收紧、不扩池的边界；
9. 最终 Markdown 研究结果、模拟计划和前端展示保持一致；
10. 不以牺牲全市场客观覆盖换取性能。

## 22. 宏观与开放数据补充层

### 22.1 数据权威顺序

| 数据域 | 首选事实源 | 开源接入/校验层 | 失败策略 |
|---|---|---|---|
| PMI、CPI、PPI、工业数据 | 国家统计局公开发布/国家数据 | AKShare 统一字段与缓存 | 保留最近成功快照并标陈旧；不得以新闻替代 |
| M1、M2、社融、新增信贷、利率 | 中国人民银行公开统计 | AKShare 统一字段与缓存 | 单序列缺失时降级，不填 0 |
| 股票/黄金/债券/现金代理行情 | 交易所公开 ETF 行情 | AKShare ETF 日线适配器 | 单资产缺失不参与排名，四象限标记降级 |
| 美国、韩国、台湾、日本先行指标 | 对应市场公开指数 | AKShare 全球指数适配器 | 独立降级；不得用 A 股新闻伪装海外价格 |
| 同花顺行业与概念 | 同花顺金融数据 API | 本地版本化成员关系与月度轮动特征 | 不完整时阻断主题映射 |
| 公告、主营和正式披露 | 巨潮资讯/北交所 | PDF 结构化、本地事实缓存 | 缺失进入观察，不让模型编造 |
| 券商月度金股 | 券商公开原文或人工核验清单 | 独立盲测基准导入器 | 只评估覆盖和排名，不作为运行时输入 |

开放数据适配器必须输出 `as_of`、来源引用、内容哈希、新鲜度、字段覆盖和失败原因。AKShare 是 MIT 开源聚合器，但其上游接口可能变化，因此固定已验证版本并保留契约测试；它不能取代官方披露的事实权威性。

### 22.2 缓存与点时规则

- 宏观与 ETF 快照按自然日原子落盘，默认优先读取当日成功缓存；
- 月度数据只保留观察期不晚于 `as_of` 的记录；无法证明发布时间的序列必须标注 `publish_time_available=false`，历史回放不得把它冒充严格点时数据；
- 每个数据域独立记录成功、陈旧、缺失或接口错误，一个开源接口失败不能清空其他已成功事实；
- 最终研究快照把补充数据纳入冻结事实哈希，模型、规则和回放读取相同字节。

## 23. A1 覆盖验收

正式全量快照（G0 不少于 500）必须同时满足：

1. 发现层至少给出 6 个结构主题和 12 个产业链节点；
2. 当月轮动候选不少于 3 个时，发现结果至少覆盖其前 10 中的 40%；
3. 全部 G0 均有 `OUTSIDE_THEME/HARD_REJECT/LOCAL_MONITOR/LOCAL_ACTIVE_CANDIDATE/REVIEW_CANDIDATE` 之一；
4. 精确主营与基本面证据足够时，A1 `ACTIVE` 不少于 100 且不超过 250；
5. 证据不足时不得降门槛凑数，必须以 `A1_ACTIVE_COVERAGE_UNDERFILLED` 阻断正式发布；
6. 券商月度金股只在运行结束后计算覆盖率、可解释率和排名分位，且报告明确 `benchmark_not_runtime_input=true`。
