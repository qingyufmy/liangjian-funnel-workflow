# A3/A4 三策略改造实施验收报告

日期：2026-08-31  
范围：A3 日度技术计划、A4 盘中择时、提示词、计划/事件持久化、回放、CLI 与工作台  
结论：本地工程验收通过；未执行虚拟机部署、外部模型真实调用或真实交易

## 1. 交付结论

本轮已把 A3/A4 从“综合技术分 + 混合周期确认”收敛为三条相互独立的确定性策略路径：

- `LEADER_INTRADAY`：A2 已确认的题材/情绪龙头，A3 只核验日线风险，A4 处理盘中分歧转一致、回封或首个 15 分钟区间突破；
- `MA520_SWING`：520 仅指闭合日线 MA5/MA20，A4 只负责 15 分钟稳定与 5 分钟确认；
- `TREND_MA5`：月/闭合周/闭合日确认主升趋势，A4 处理日线 MA5 计划区附近的盘中止跌与反转。

每只股票每个目标交易日只能拥有一条 `strategy_profile`。A3 的确定性代码负责路由、价位、条件和资格；LLM 只能返回 `PASS`、`VETO` 或 `DATA_GAP`。A4 的确定性引擎先判断，模型只能否决入场/加仓候选，不能创造信号、改价格或改策略。

## 2. 已实现的关键边界

### 2.1 A3 时间尺度

- 正式资格只读取月线、已结束周线和闭合日线；
- 当月和当周未闭合聚合只进入 `monthly_partial_observation` / `weekly_partial_observation`；
- 120 分钟、15 分钟、5 分钟和 1 分钟不参与 A3 资格；
- 创新高走 `PRICE_DISCOVERY_TREND`，使用风险收益和失效位，不伪造上方压力位；
- `WATCH`、`REJECTED`、`DATA_GAP` 以及 secondary/watch 结果不能发布为可执行计划。

### 2.2 A4 时间尺度与状态机

- 15 分钟闭合 K 线决定盘中结构；
- 5 分钟闭合 K 线决定具体确认；
- 1 分钟只负责行情新鲜度、涨跌停可交易性、硬止损和参考价；
- 未闭合桶、未来 K 线、跨交易日、跨午休拼接全部失败关闭；
- 开盘尚无闭合 15 分钟 K 线时为 `A4_SESSION_WARMUP`，不是永久计划失效；
- 硬止损先于小周期完整性检查，退出信号不等待模型；
- 龙头、520 和趋势 5 日线三条路径不互相借条件，不用盘中 MA5/MA20 冒充日线 520；
- 禁止亏损加仓，锁死涨停不可假设成交。

### 2.3 数据、持久化和展示

- A3 计划保存策略版本、策略路由、资格、满足/未满足条件、否决条件、禁止追价线、失效位和创新高语义；
- A4 事件保存确定性策略状态、闭合 5/15 分钟截止时间和稳定原因码，不保存模型思考过程；
- CLI、Node 服务和工作台使用同一策略字段；
- 工作台不再展示 A3 综合技术分，改为展示策略、资格、条件与盘中状态；
- 旧快照只保留为非策略模型复核兼容输入，不会被升级成新策略计划。

## 3. 配置收敛复审

最终复审删除了配置中的旧 `minimum_technical_score`、`score_weights`、A3 的 120m/15m/5m 资格要求和跨策略通用确认条件，改为：

- `decision_timeframes: [MONTHLY_CLOSED, WEEKLY_CLOSED, DAILY_CLOSED]`；
- `allowed_strategy_profiles` 三策略白名单；
- `llm_permission: VETO_ONLY`；
- `one_strategy_per_symbol_per_day: true`；
- `secondary_watch_executable: false`。

这避免未来维护时把已废弃的综合打分或分钟周期重新接回 A3。

## 4. 验证结果

在 Windows 本地仓库执行：

```text
.venv\Scripts\python.exe -m pytest
875 passed in 31.14s

npm run typecheck
通过

npm test
3 test files / 50 tests passed

npm run build
Vite Web 与 Node Server 生产构建通过

git diff --check
通过；仅 Git 提示工作区 LF 将按本机设置转换为 CRLF，无空白错误
```

关键回归覆盖：

- 三种 A3 路由各自可形成唯一合格策略；
- 路由优先级、创新高、首板/高板、一字板、派发和死叉边界；
- 未结束月/周不能证明资格，正式已闭合周期与部分观察可以并存；
- 三种 A4 策略各自存在确定性有效触发；
- 未闭合 K 线、未来数据、午休、硬止损、涨停锁死和数据缺口；
- 新策略计划不再走旧 1 分钟触发区；
- 模型不能把未触发计划升级，只能否决；
- A4 全日隔离回放只能使用当时可见 K 线，并按下一根 K 线模拟成交；
- Workflow 计划发布、早盘激活、A4 上下文、模拟成交与状态持久化。

## 5. 工程验收与业务验收边界

本轮可以确认：代码契约、策略分工、持久化、回放隔离、前后端类型和构建均已跑通。

本轮不能据此宣称三种策略已经获得真实市场正收益。策略效果仍需使用至少 10 个交易日的点时数据，分别统计每条策略的候选数、触发率、未成交率、胜率、盈亏比、最大回撤、滑点和数据阻断率。该验证不能通过伪造非空计划、放宽门槛或使用未来数据完成。

此外，本轮没有：

- 调用真实外部模型/API 完成一次生产 A1→A4；
- 部署或重启虚拟机；
- 推送 Git 远端；
- 连接真实交易账户或发送外部订单。

这些属于下一阶段的生产验收，需在用户明确授权后执行。
