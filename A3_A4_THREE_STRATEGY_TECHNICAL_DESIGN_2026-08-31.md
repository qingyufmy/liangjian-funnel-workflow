# A3 日度计划与 A4 三策略盘中择时技术设计

日期：2026-08-31  
状态：已实施并通过本地工程验证  
适用范围：`liangjian_funnel_workflow` 的 A3、A4、A4 回放与工作台展示  
交易边界：仅本地内部模拟，不连接真实交易，不产生外部订单

## 1. 已确认的业务决策

1. A3 过滤后的合格结果允许形成次日计划，并在次日盘前复核后进入 A4。
2. A3 技术结论由确定性代码负责；LLM 只核对、解释、发现遗漏和执行否决，不负责打分、改价或放宽门槛。
3. A3 以月线、周线作为背景，以闭合日线作为最低决策周期；15 分钟、5 分钟和 1 分钟不参与 A3 选股。
4. A4 只在 A3 已批准计划中工作，以闭合 15 分钟和 5 分钟结构进行盘中择时；1 分钟只用于行情新鲜度、涨跌停状态、硬止损和信号执行参考。
5. 创新高股票按趋势强度处理，不因缺少历史上方压力位而机械拒绝。
6. 周线采用常规证券分析口径：已结束周为正式证据，本周截至当前交易日的数据仅作为周内观察，不冒充闭合周线。
7. A4 使用三种明确且相互区分的策略：龙头战法、520 战法、趋势行情 5 日线交易。

## 2. 第一性原理与职责边界

### 2.1 A3 回答的问题

A3 只回答：

- 这只股票的大周期和日线结构是否允许形成次日计划；
- 它适合哪一种交易策略；
- 次日允许观察的价格区、禁止追价线和日线失效位是什么；
- 计划在哪些市场、板块和个股前提下仍然成立；
- 哪些情况出现后，A4 无权继续寻找入场点。

A3 不回答：

- 明天具体哪一分钟买；
- 5 分钟金叉是否成立；
- 分时回封是否有承接；
- 盘中是否应该加仓或减仓；
- 实际股数、金额或账户仓位。

### 2.2 A4 回答的问题

A4 只回答：

- 已有 A3 计划当前是否满足该策略的盘中触发条件；
- 是否开始确认、继续等待、产生买入/加仓/减仓/卖出信号或取消计划；
- 当前板块、龙头、中军、量价和可交易性是否仍支持计划；
- 信号是否因数据缺口、涨跌停不可成交、T+1 或市场冲击而阻断。

A4 不得：

- 新增 A3 计划外的股票；
- 把一种策略临时改成另一种策略；
- 改写 A3 日线失效位和禁止追价线；
- 用小周期强势反向证明日线趋势成立；
- 因模型看多而越过确定性触发；
- 连接真实账户或发送外部订单。

### 2.3 正确的时间尺度

```text
月线、已结束周线：市场与个股的大背景
        ↓
闭合日线：A3 计划资格、策略路由、趋势失效
        ↓
次日集合竞价：计划有效性与追高门复核
        ↓
闭合15分钟：A4 日内结构与方向
        ↓
闭合5分钟：A4 具体确认与信号
        ↓
1分钟：数据安全、硬止损、涨跌停和信号参考价
```

同一个 MA5 在日线与 5 分钟上的含义不同。日线 MA5 是持股趋势线；5 分钟 MA5 只是盘中短动能，不能代替日线趋势资格。

### 2.4 禁止综合打分，采用“行情场景 → 唯一策略 → 条件确认”

系统不再把均线、MACD、KDJ、成交量、板块热度等转换成权重后累加为一个总分。原因是这类综合分数会掩盖指标之间的因果关系，容易在少量历史样本上过拟合，也无法解释某个信号究竟适用于哪种行情。

正式决策顺序固定为：

1. 识别市场环境：风险偏好、趋势/震荡、成交量和题材周期；
2. 识别板块状态：启动、确认、加速、分歧、退潮；
3. 识别个股角色：龙头、中军、趋势核心或普通跟随；
4. 选择唯一适用策略：龙头、520、趋势5日线或不交易；
5. 检查该策略必需条件与否决条件；
6. A3生成次日计划，A4等待对应盘中形态；条件不完整就等待或取消，不用其他指标加分补齐。

指标只能属于以下三种角色：

- `CONTEXT`：描述行情背景，不直接产生信号；
- `REQUIRED_CONFIRMATION`：该策略必须满足的确认条件；
- `VETO`：出现即否决或退出的风险条件。

不存在“弱条件累计到一定分数后替代关键条件”的路径。策略效果通过逐策略回放统计验证，而不是通过人为权重证明。

## 3. A3 确定性技术层

### 3.1 A3 输入

A3 只读取 A2 传入的合格候选及其冻结血缘：

- 股票代码、名称；
- 主题、行业与产业链节点；
- A2 市场角色：`LEADER`、`EMOTION_LEADER`、`CORE_ARMY`、`TREND_CORE` 等；
- A2 主题阶段、梯队、资金、拥挤度与板块权限；
- 前复权日线；
- 月线、周线均由日线按交易日历确定性聚合；
- 相对行业、沪深300的强弱；
- 企业行为、停牌、涨跌停、新股与交易资格；
- 计划日、目标交易日和所有数据截止时间。

### 3.2 数据口径

- 日线：使用前复权序列计算趋势，另保留不复权价格用于真实触发价和涨跌停校验。
- 月线：仅已结束月份用于正式趋势；本月数据标记 `MONTH_TO_DATE_OBSERVATION`。
- 周线：仅已结束周用于正式趋势；本周数据标记 `WEEK_TO_DATE_OBSERVATION`。
- 日线：收盘研究使用当日 15:00 已闭合日线。
- 任何未来 K 线、未结束周/月或企业行为未校正数据都不得进入正式资格判断。

### 3.3 A3 通用资格门

所有策略都先经过以下共同条件：

1. A2 血缘完整且没有越级新增股票；
2. 目标交易日和数据截止日明确；
3. 日线新鲜、完整且企业行为已处理；
4. 股票可交易，不是停牌、退市整理或无涨跌幅规则不明确标的；
5. 日均成交额、换手和滑点容量满足模拟策略要求；
6. 市场状态不是 `RISK_OFF_RETREAT`；
7. 板块权限不是 `NO_NEW_ENTRY` 或 `EXIT_RISK`；
8. 日线失效位、禁止追价线与计划有效期可以确定；
9. 计划必须明确唯一的 `strategy_profile`；
10. 不能仅因池容量未满而放宽资格。

### 3.4 A3 三策略路由

一只股票可以带多个研究标签，但次日只能有一个主执行策略，避免 A4 同一时刻出现相互冲突的触发条件。

确定性优先级：

1. `LEADER_INTRADAY`：A2 已确认的市场龙头/情绪龙头，且题材处于启动、确认或加速早期；
2. `TREND_MA5`：日线主升趋势已经明确，核心交易逻辑是沿日线 MA5 持有和回踩；
3. `MA520_SWING`：处于日线 MA5/MA20 趋势切换、金叉或 MA20 回踩修复阶段；
4. 不满足任何路由则 `NO_NEXT_DAY_PLAN`。

若同时满足趋势 5 日线和 520：

- 已站上 MA60、MA5>MA10>MA20 且主升明确，路由为 `TREND_MA5`；
- 仍处于 MA5/MA20 刚金叉、MA20 附近筑底或趋势切换，路由为 `MA520_SWING`。

### 3.5 A3 通用输出合同

每个计划至少包含：

```json
{
  "strategy_profile": "LEADER_INTRADAY|MA520_SWING|TREND_MA5",
  "strategy_version": "a3-a4-three-strategy/1.0.0",
  "symbol": "000000.SZ",
  "candidate_origin": "FOCUS|WATCH_ONLY",
  "market_role": "LEADER|CORE_ARMY|TREND_CORE|...",
  "market_regime": "...",
  "theme_stage": "...",
  "monthly_state": "...",
  "weekly_closed_state": "...",
  "weekly_partial_observation": "...",
  "daily_state": "...",
  "daily_ma": {"ma5": 0, "ma10": 0, "ma20": 0, "ma60": 0},
  "daily_macd": {"dif": 0, "dea": 0, "hist": 0},
  "daily_volume_state": "...",
  "relative_strength": {},
  "entry_reference_zone": {"low": 0, "high": 0},
  "no_chase_price": 0,
  "daily_invalidation": 0,
  "plan_premises": [],
  "a4_required_entry_rules": [],
  "a4_exit_rules": [],
  "plan_expiry": "ISO8601",
  "eligibility": "QUALIFIED|WATCH|REJECTED|DATA_GAP",
  "required_conditions": [],
  "met_conditions": [],
  "unmet_conditions": [],
  "veto_conditions": [],
  "llm_review": {"status": "PASS|VETO|DATA_GAP", "reason_codes": []}
}
```

数值、路由、资格、条件状态和价位都由代码生成。LLM 只能把 `PASS` 改成 `VETO` 或 `DATA_GAP`，不能把不合格计划变成合格计划。

## 4. 策略一：龙头战法

### 4.1 策略定位

龙头战法是题材情绪与市场辨识度策略，不是普通均线策略。股票必须先由 A2 证明其龙头身份，A3 只负责确认日线风险是否允许次日继续观察，A4 才判断盘中分歧、承接、回封和退潮。

### 4.2 A3 龙头计划资格

必须满足：

- A2 角色为 `LEADER` 或 `EMOTION_LEADER`；
- 板块存在清晰题材，且处于 `IGNITION`、`CONFIRMATION` 或非末端 `ACCELERATION`；
- 龙头在板块内具备启动时间、涨幅、成交额承载和关注度优势；
- 梯队不是 `BROKEN`，龙头与中军没有同时转弱；
- 不是一字板连续加速导致无真实换手；
- 日线没有明显高位派发、放量长上影连续出现或重大失效；
- 次日不是无条件追板计划，必须有可交易的分歧转一致或回封观察条件。

板位处理：

- 首板：主要识别和观察，不因首板本身发布追板信号；
- 二板：允许进入卡位/确认计划，但必须观察换手和板块梯队；
- 三板：确认为龙头候选，但新入风险最高为 `PROBE`；
- 四板及以上：默认不新开，除非后续回放单独证明有效，不用主观经验放宽。

### 4.3 A4 龙头入场状态机

```text
WAIT_THEME
  → LEADER_IDENTITY_CONFIRMED
  → WAIT_DIVERGENCE
  → INTRADAY_SUPPORT_HELD
  → RECLAIM_CONFIRMING
  → BUY_SIGNAL / NO_ACTION / CANCEL
```

硬前提：

- 板块当日强度与广度没有崩溃；
- 梯队未断层；
- 龙头仍是板块最强或最早核心之一；
- 中军没有同步放量下跌；
- 不是一字涨停、T字不可成交或价格锁死；
- 当前价不超过 A3 `no_chase_price`；
- 15 分钟结构未跌破当日关键支撑；
- 5 分钟使用闭合K线。

允许的具体触发：

#### L1 分歧转一致

- 早盘发生可交易换手，不是无量一字；
- 回落时板块梯队未破坏；
- 15 分钟低点不再降低；
- 5 分钟收回 VWAP；
- 随后两根闭合 5 分钟K线保持在 VWAP 上方；
- 5 分钟成交额从缩量回落转为温和放大；
- 价格仍在 A3 允许区间。

#### L2 炸板后有效回封

- 曾打开涨停，但不是连续剧烈开板；
- 开板时成交和换手真实；
- 板块核心股没有同步跳水；
- 回封前出现 5 分钟更高低点；
- 当前盘口不是锁死、无法模拟成交的涨停状态。

若已经重新封死且无法成交，只记录 `WAIT_NEXT_TRADABLE`，不得假设买入成功。

#### L3 早盘强势区间突破

- 仅适用于非一字、非大幅高开；
- 首个完整 15 分钟区间形成后再判断；
- 5 分钟放量突破首个15分钟高点；
- 板块强度、梯队和中军同步确认；
- 不得在高位加速末端触发。

### 4.4 龙头离场与取消

硬取消/卖出信号：

- A3 日线失效位被触发；
- 板块梯队断层且龙头不能在规定时间内修复；
- 龙头炸板后连续两根闭合 5 分钟K线不能收回 VWAP；
- 中军与龙头同步放量转弱；
- 高位天量长上影并跌破15分钟结构低点；
- 市场进入急剧风险退潮；
- 计划过期。

减仓而非清仓信号：

- 长上影但仍守住关键结构；
- 板块强度下降但梯队未彻底断裂；
- 盘中加速后首次明显量价背离。

同日新买持仓受 T+1 限制时，A4仍记录风险退出信号，但 Agent 6 标记 `DEFERRED_T1_EXIT`，不得伪造同日卖出成交。

## 5. 策略二：520 战法

### 5.1 策略定位

520 的“5”和“20”均指日线 MA5、MA20。A3 负责确认金叉、回踩与趋势修复，A4不能把5分钟MA5/MA20金叉冒充日线520。

### 5.2 A3 520 计划类型

#### M1 日线金叉启动

- 前一日 MA5 <= MA20，当日 MA5 > MA20；
- MA20 走平或向上，不能明显向下；
- 收盘价位于 MA20 上方；
- 成交量温和放大，不能是高位天量滞涨；
- 日线MACD用于确认，不单独产生计划。

#### M2 MA20 回踩企稳

- 日线原有上涨趋势未破坏；
- 收盘价回踩 MA20 附近但未有效跌破；
- MA5重新向上拐头；
- 回踩缩量，反弹量能温和恢复；
- 不属于高位派发。

#### M3 跌破后收复观察

- 曾短暂跌破 MA20；
- 当日重新收回 MA20；
- MA5没有持续下弯；
- 只能发布 `PROBE`，等待 A4 盘中确认。

禁止计划：

- MA5死叉MA20；
- MA20斜率明显向下；
- 放量跌破MA20且未收回；
- 剧烈妖股或均线失真；
- 仅KDJ金叉、MACD金叉但价格结构不成立。

### 5.3 A4 520 入场状态机

```text
WAIT_DAILY_520_PLAN
  → WAIT_INTRADAY_PULLBACK
  → M15_STRUCTURE_STABLE
  → M5_RECLAIM_CONFIRMING
  → BUY_SIGNAL / NO_ACTION / CANCEL
```

具体触发条件：

- 当前价格处于 A3 触发区且不高于禁止追价线；
- 闭合15分钟没有形成下降趋势或连续更低低点；
- 价格在 A3 日线 MA20/关键支撑上方；
- 5分钟回调后形成更高低点；
- 5分钟重新站上 VWAP；
- 两根连续闭合5分钟K线确认；
- 确认阶段成交量温和放大，但不超过近期中位数的异常倍数；
- 板块没有明显转弱。

MACD、KDJ的使用：

- 日线MACD只作为 A3 趋势确认；
- 15分钟MACD柱缩短或翻红可作为 A4 辅助证据；
- KDJ只记录观察，不作为硬门槛，避免震荡区频繁金叉死叉造成过拟合。

### 5.4 520 离场

- A3日线失效位触发：硬退出信号；
- 两根闭合15分钟K线位于关键结构下方且反抽失败：卖出/减仓信号；
- 5分钟失守VWAP本身不直接清仓，必须叠加15分钟结构转弱；
- 放量下跌并反抽不过关键位：卖出信号；
- 日线MA5下穿MA20属于收盘后的A3级别趋势失效，生成下一交易日退出计划；
- 当日新买且不可卖时，记录风险但遵守T+1。

## 6. 策略三：趋势行情5日线交易

### 6.1 策略定位

该策略只用于已经进入主升趋势的股票。它不是底部反转策略，也不是用5分钟MA5追涨。日线MA5是持有和回踩参考线，A4负责寻找日内靠近日线MA5后的企稳时点。

### 6.2 A3 趋势5日线资格

必须满足：

- 月/周背景不处于明确下降趋势；
- 日线收盘价站上MA60；
- MA5 > MA10 > MA20，且至少MA5、MA10斜率向上；
- 个股相对行业和沪深300保持强势；
- 已完成平台突破或主升段确认；
- 首次或第二次回踩日线MA5优先；
- 回踩缩量，反弹量能恢复；
- 距MA5和MA20不能过度延伸；
- 高位天量滞涨、连续长上影或筹码派发不得发布新计划。

满足以下任意三项可形成趋势回踩候选，但仍需通过通用门：

1. MA5上穿MA10、MA20并形成多头发散；
2. 放量上攻后首次回踩MA5不破；
3. 回踩缩量并出现止跌K线；
4. 回踩后收阳且量能温和恢复；
5. 日线MACD绿柱缩短或重新翻红。

### 6.3 创新高处理

创新高股票进入 `PRICE_DISCOVERY_TREND`，不再要求历史第一压力位。

服务端使用：

- 趋势强度；
- ATR14；
- 日线MA5/MA10/MA20；
- 突破位；
- 相对强弱；
- 成交量和换手；
- 距离均线的延伸度。

计划采用 R 倍数而非虚构压力位：

```text
R = 入场参考上沿 - 日线失效位
观察目标1 = 入场参考上沿 + 2R
观察目标2 = 入场参考上沿 + 3R
真正离场仍由趋势破坏和移动防守线决定
```

若创新高但距离日线MA5过远、ATR扩张异常或连续加速，则只等待回踩，不发布追高计划。

### 6.4 A4 趋势5日线入场状态机

```text
WAIT_TREND_PULLBACK
  → DAILY_MA5_ZONE_REACHED
  → M15_SELLING_PRESSURE_EASING
  → M5_REVERSAL_CONFIRMING
  → BUY_SIGNAL / NO_ACTION / CANCEL
```

具体触发：

- 当前价进入 A3 基于日线MA5生成的回踩区；
- 不能跌破A3日线失效位；
- 闭合15分钟不再形成更低低点；
- 回踩阶段成交量收缩；
- 5分钟形成锤头、阳包阴或更高低点之一；
- 5分钟重新站回VWAP；
- 随后一根闭合5分钟阳线量能温和放大；
- 不在禁止追价区；
- 板块与大盘没有急剧转弱。

### 6.5 趋势持有、加仓和离场

持有：

- 日线趋势仍成立；
- 盘中回踩后能收回A3日线MA5参考区；
- 没有放量下跌和板块退潮证据。

加仓信号只允许：

- 已有浮盈；
- 首次有效突破平台后缩量回踩不破；或
- 放量阳线后缩量回踩并再次形成5分钟确认；
- 不得亏损加仓、不得摊薄成本；
- 最终是否加仓和加多少由Agent 6决定。

减仓：

- 高位长上影但日线趋势尚未确认失效；
- 短期涨幅20%–30%且ATR、成交量异常扩张；
- 15分钟量价背离并跌破日内结构。

卖出/退出：

- 放量跌破A3日线MA5参考位且连续15分钟不能收回；
- 反抽日线MA5参考位失败后再次回落；
- A3日线收盘确认跌破MA5并且MA5斜率转下；
- 日线MACD顶背离只作为辅助，必须叠加价格结构破坏；
- 硬止损、市场冲击或板块主线结束。

## 7. A4 确定性执行架构

### 7.1 策略路由器

A4不使用综合投票，按A3的唯一 `strategy_profile` 调用对应纯函数：

```text
LEADER_INTRADAY → LeaderIntradayStrategy
MA520_SWING     → Ma520IntradayStrategy
TREND_MA5       → TrendMa5IntradayStrategy
```

每个策略分别实现：

- `evaluate_entry(context, plan)`；
- `evaluate_add(context, plan, position)`；
- `evaluate_reduce(context, plan, position)`；
- `evaluate_exit(context, plan, position)`。

### 7.2 A4 数据快照

每个活动计划在同一分钟共享一个不可变快照：

- 最新闭合1分钟K线；
- 本地聚合的闭合5分钟、15分钟K线；
- VWAP和5/15分钟成交量中位数；
- 当日开盘价、昨收、涨跌停价和盘口可交易状态；
- A3冻结的日线MA、策略路由和日线失效位；
- 板块强度、广度、梯队、龙头和中军状态；
- 市场指数和市场冲击状态；
- T+1可卖状态只提供给Agent 6，不进入LLM判断。

全市场历史数据不在每分钟重拉。A4只更新活动计划与持仓股票；5分钟、15分钟K线由1分钟缓存本地增量聚合。

### 7.3 触发频率

- 每分钟：数据新鲜度、价格区、涨跌停、硬止损、市场冲击；
- 每根闭合5分钟：具体入场确认和短周期异常；
- 每根闭合15分钟：结构方向、反抽失败、退潮确认；
- 收盘后：交回A3更新日线趋势，不在盘中伪造日线收盘结论。

### 7.4 LLM权限

A4 Flash模型保持思考关闭，并且只有否决权：

- 确定性条件未通过，LLM不能产生信号；
- 确定性条件通过，LLM只能返回 `PASS` 或 `VETO`；
- LLM不得选策略、改价格、改止损、改确认根数或创建股票；
- LLM不可用时失败关闭，不把缺失模型当通过；
- 硬止损、计划失效和市场级风险退出不受LLM否决。

## 8. 信号与状态语义

新增统一策略状态：

- `WAIT_STRATEGY_CONDITION`
- `WAIT_15M_STRUCTURE`
- `WAIT_5M_CONFIRMATION`
- `CONFIRMING`
- `ENTRY_SIGNAL_ACTIVE`
- `HOLDING_TREND`
- `ADD_SIGNAL_ACTIVE`
- `REDUCE_SIGNAL_ACTIVE`
- `EXIT_SIGNAL_ACTIVE`
- `DEFERRED_T1_EXIT`
- `PLAN_CANCELLED`
- `DATA_BLOCKED`
- `DONE`

每条事件记录：

- 策略类型和版本；
- A3计划ID与血缘；
- 触发的具体规则ID；
- 1m/5m/15m最后闭合时间；
- 日线证据截止时间；
- 市场、板块、个股证据；
- 所有未满足条件；
- LLM是否否决及证据；
- T+1状态；
- 后续模拟成交或未成交原因。

## 9. 工作台展示

A3计划详情增加：

- 策略标签：龙头 / 520 / 趋势5日线；
- 月、周、日趋势；
- A3确定性资格、已满足条件、未满足条件与否决条件；
- 日线MA5/10/20/60；
- 计划区、禁止追价线、日线失效位；
- 创新高趋势状态；
- LLM核对结论；
- 进入A4前的盘前复核状态。

A4详情增加：

- 当前策略状态机节点；
- 当前满足/未满足的15分钟与5分钟条件；
- 板块梯队和龙头/中军状态；
- START_CONFIRMATION、BUY、ADD、REDUCE、SELL全过程时间线；
- LLM veto记录；
- T+1导致的延迟退出；
- 模拟成交、滑点、MFE、MAE和最终结果。

## 10. 实施顺序与验收

### P0：合同和数据修正

1. A3移除120m、15m、5m选股门，增加月线与标准闭合周线；
2. 日线趋势改用前复权，真实价格保留不复权；
3. 新增 `strategy_profile`、创新高状态和A3计划合同；
4. A3确定性资格、路由、条件状态和价位成为唯一权威；
5. LLM改为核对/否决Schema；
6. 统一secondary PROBE是否允许进入A4：按本方案，A3过滤合格且可执行的PROBE允许进入A4模拟信号，但必须显式标记 `PROBE`。

### P1：三策略确定性引擎

1. 实现龙头策略状态机；
2. 实现520策略状态机；
3. 实现趋势5日线状态机；
4. 本地增量聚合5m/15m；
5. 分离1分钟硬风险与5/15分钟策略触发；
6. 增加T+1延迟退出状态。

### P2：回放和前端

1. A4回放扩展为日线背景+完整240根1分钟线；
2. 每种策略至少覆盖：有效入场、无信号、假突破、数据缺失、涨停不可成交、T+1退出；
3. 工作台展示策略状态机、条件清单和完整血缘；
4. 输出按策略分组的触发率、胜率、MFE、MAE、最大回撤、未触发机会成本和LLM否决质量。

### 验收标准

- A3和A4无任何股票越级注入；
- A3输入不含分钟级选股结论；
- A4不修改A3日线计划；
- 三策略路由唯一，不能同票同日产生冲突买入；
- 只用闭合5m/15m产生策略信号；
- 涨停锁死不模拟成交；
- T+1不伪造卖出；
- 创新高股票不会因无历史压力位被机械拒绝；
- 数据缺失只阻断对应股票/策略，不无故阻断整池；
- 模型只能否决，无法促成；
- 历史回放无未来函数；
- 工程测试通过与策略有效性分开报告。

## 11. 第一轮复审：过度设计与无用内容

复审结论：保留三套纯函数状态机，不创建三个独立LLM Agent，不引入复杂规则引擎或新数据库。

删减/限制：

- MACD、KDJ不组合成多指标投票；MACD只辅助，KDJ只观察；
- 不为每种策略单独拉取全市场数据；只处理活动计划池；
- 不引入分钟级全市场行业扫描，板块上下文按缓存节拍复用；
- 不使用模型重新计算均线、资格、价位或策略路由；
- 不把所有条件做成可自由配置的DSL，首版使用有版本的明确Schema和纯函数；
- 不一次性引入复杂机器学习参数优化，先完成可解释回放。

仍需保留：

- 策略版本；
- 不可变快照；
- 条件级原因码；
- T+1与涨跌停真实性；
- 可复现回放；
- 前端问题跟踪。

## 12. 第二轮复审：边界与异常

1. **策略重叠**：只允许唯一主路由，研究标签可以多选，执行策略不能多选。
2. **周/月未闭合**：只作观察字段，禁止进入正式趋势计算。
3. **企业行为**：技术序列使用前复权，真实触发价用不复权，二者必须绑定同一企业行为版本。
4. **创新高**：使用ATR和R倍数，不伪造压力位；过度延伸仍然禁止追高。
5. **涨停板**：识别强度但不假设成交；锁死涨停只记录观察。
6. **龙头退潮**：板块梯队、龙头和中军必须有同一时间戳，旧快照不能用于放行。
7. **T+1**：同日风险退出记录为延迟退出，不伪造成交。
8. **数据缺失**：单票缺5m/15m只阻断单票；市场级数据缺失才阻断全池。
9. **LLM超时**：入场失败关闭；硬风险退出不等待模型。
10. **重启恢复**：确认根数、策略状态和信号去重必须持久化，不能只放内存。
11. **午间休市**：5m/15m连续性按A股交易时段处理，午休不算数据缺口。
12. **尾盘限制**：14:45后禁止新买；风险退出继续评估。
13. **模拟成交**：信号后的下一根可交易K线成交，加入滑点和涨跌停限制。
14. **计划为空**：明确区分真实无机会、数据不足、计划未激活，不用统一“0只”掩盖原因。

复审结论：方案没有要求扩大项目到真实交易，也没有依赖新的商业数据服务；主要改动集中在A3技术周期、A4确定性触发和可观测性，边界可控、可测试、可回滚。
