# A1–A3 全市场首轮初始化与增量运行

## 目标与不可破坏的边界

首轮从同花顺当日完整证券目录出发，先执行确定性 G0 质量门：日成交额不低于 5000 万元，排除 ST、停牌/新股限制和价格/成交量/成交额无效记录。北交所不因交易所身份排除；通过同一质量门后保留为只研究。G0 内不设股票数量或 Top-N 性能上限。三个模型 lane 相互隔离，且每个 lane 必须满足：

```text
A1 输入 = 该次冻结的完整 G0
A2 输入 = 同 lane A1.active_research_pool
A3 输入 = 同 lane A2.focus_pool
```

批次仅是模型传输和失败恢复边界，不是选股裁剪边界。A2/A3 不得回到全量 G0，也不得引入上游池外代码。

## 数据分层

| 数据 | 落盘/更新 | 进入阶段 |
|---|---|---|
| 全市场代码、行情快照、市场情绪 | 每次冻结 | A1 |
| 日线 | SQLite 保留修订，首次回填 800 日，之后补增量 | A1（短投影）/A3（长投影） |
| 利润表、指标、资产负债表、现金流 | SQLite 保留报告期/发布时间/内容修订，默认 24 小时刷新 | A1 |
| 巨潮近期公告 | 按公司缓存 6 小时 | A1 |
| 巨潮年报查询 | 按公司缓存 7 天 | A1 |
| 巨潮 PDF 页码证据卡 | 流式下载解析；结构化页码证据与 PDF 哈希长期落库，默认不长期保留可重下载的原文 | A1 |
| 市场/RSS 资讯 | 每次冻结 | A1/A2 辅助 T3 |
| 个股资讯 | 仅 A1 ACTIVE 子集，缓存 2 小时 | A2 |
| 5m 长历史与技术因子 | 仅 A2 focus 子集，mootdx 本地补齐 | A3 |

本地事实库使用 SQLite `WAL + synchronous=FULL`。日线、财务和查询结果均保留内容哈希与抓取时间；模型快照只使用结构化、脱敏、有界的投影。

## 中断恢复

- 每个成功的日线/财务接口请求立即提交，进程中断后已完成股票不重拉。
- 巨潮查询、PDF 证据卡和个股资讯均使用可校验缓存。
- 模型检查点绑定 `run/lane/stage/model/prompt_hash/snapshot_hash/batch_symbols_hash`，只复用重新校验合格的成功批次。
- 同一交易日、同一时段的失败重试复用原冻结快照，避免因新快照哈希变化而使检查点失效。快照文件被篡改或移出配置目录时拒绝恢复。
- 429/5xx 仍由模型客户端重试，不设计额外熔断或自动换模型。

## 首轮执行顺序

```bash
# 1. 首次全市场日线+财务回填，可重复执行并断点续跑
.venv/bin/python -m liangjian_funnel sync-data

# 2. 正式收盘 A1→A2→A3；会复用第 1 步缓存，并补齐巨潮及阶段数据
.venv/bin/python -m liangjian_funnel run-close

# 3. 查看状态和缓存覆盖
.venv/bin/python -m liangjian_funnel status
```

Node 控制台持续读取 `state/workflow_progress.json`，可观察当前数据源、处理数、缓存命中/未命中、失败数、预估剩余时间和三个 lane 的 A1–A3 批次。

## 验收标准

1. `sync-data.catalog_universe_count` 与当日同花顺完整目录数相符；`data_universe_count == research_universe_count == A1 G0 数`；`trade_universe_count` 是排除北交所后的可模拟交易计数。
2. 再次执行时已完成股票显示缓存命中，不重新回填全部历史。
3. 三个 lane 的 A1 批次合并后覆盖完整 G0；审计文件证明 `A2⊆A1⊆G0`且 `A3⊆A2`。
4. A3 分钟数据请求范围与对应 lane 的 A2 `focus_pool` 一致，不存在全市场 5m 回填。
5. 进程中断后重跑，快照哈希不变，已验证批次由检查点复用。
6. 最终产生三个 lane 审计 JSON 和一份汇总 Markdown；空计划是合法的业务结果，但数据/模型/血缘失败不得被伪装为空计划。
7. A3 即使返回不可交易标的，计划发布也必须以 `PLAN_SYMBOL_NOT_TRADABLE` 或缺失证据错误失败关闭。

> 本工作流仅用于内部研究和本地模拟，不连接外部交易账户。
