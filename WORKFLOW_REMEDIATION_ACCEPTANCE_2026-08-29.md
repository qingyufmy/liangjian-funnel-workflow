# A 股研究工作流十项整改实施与验收报告

> **历史报告，已被复核替代。** 本文记录提交 `eb442d57` 时的阶段性结果，不能作为当前最终验收结论。当前整改与验收以 `WORKFLOW_REVIEW_MASTER_REMEDIATION_PLAN_2026-08-29.md`、`REMEDIATION_TRACEABILITY_2026-08-29.md` 及自动生成的最新验收报告为准。

> 验收日期：2026-08-29  
> 验收环境：本地 Windows + GitHub Actions + 虚拟机 `192.168.1.254`  
> 部署目录：`/www/wwwroot/Agu/liangjian-funnel-workflow`  
> 工程代码验收基线：`eb442d57c01c7281661f30068fa2bb5b6df0b410`
> 安全边界：只运行研究与隔离模拟流程，不连接券商、不提交真实订单

## 1. 验收结论

本轮十项整改的工程主体已经完成并部署。Feature Store v2、运行代际绑定、统一状态合同、A2 确定性特征、主模型发布解耦、券商金股盲测接口、10 日回放评估器、TEST_ONLY 模拟闭环、脏实体增量维护、CI 与前端状态展示均已有代码和自动化测试。

2026-08-28 点时快照的全量收盘研究已在虚拟机完成。主模型 DeepSeek 完成 A1→A2→A3，运行终态为 `READY_DEGRADED`，而不是失败：A1 形成 104 只研究池；A2 在资金流和梯队数据降级条件下未形成焦点池，明确返回 `VALIDATED_NO_OPPORTUNITY`；A3 因上游无焦点机会返回 `VALIDATED_NO_ACTION`。Kimi 和 GLM 对比 lane 失败，但没有阻断主模型发布，证明主模型与可选对比模型已经正确解耦。

当前可以确认“系统能正确、可解释、可追溯地运行”；尚不能宣称“策略效果已经充分验证”。正式券商月度金股、10 个独立交易日点时样本、A2 全市场历史资金流，以及自然产生的非空 A3 计划仍是业务验收缺口。

## 2. 十项问题的实施状态

| 编号 | 整改项 | 实施状态 | 本轮证据 | 仍需完成 |
|---|---|---|---|---|
| 1 | 修复特征库下游旧结果残留 | 已完成 | Feature Store v2 使用不可变 generation；运行严格绑定 generation；`legacy-v1` 仅审计保留、严格读取不可见 | 长期观察代际切换和磁盘回收策略 |
| 2 | 统一 Pipeline、Workflow、CLI、前端状态 | 已完成 | `research-outcome/2.0.0` 四轴状态由 Python 产生，Node/React 只投影；最新运行前端可读 | 继续移除过渡期 legacy projection |
| 3 | A2 真实资金流、梯队、龙头结构和行业链共振 | 部分完成 | 行业链、角色、辨识度、行情、情绪/梯队适配器及点时覆盖合同已接入 | 全市场历史资金流与稳定梯队源仍未达到生产覆盖 |
| 4 | 区分数据不足零结果和真实无机会 | 已完成 | `quality_state`、`opportunity_state`、`data_coverage` 和 reason codes 分离；本次 A2 明示数据降级 | 补齐数据后再验证真正的 `ABSENT` 场景 |
| 5 | 券商月度金股盲测 | 接口完成、数据未配置 | 严格导入、发布时间点时过滤、不反哺研究输入、`NOT_CONFIGURED` 语义及测试已完成 | 导入合法、可核验的至少四个月真实金股数据 |
| 6 | 主模型发布与可选模型对比解耦 | 已完成 | DeepSeek 1/1 必需 lane 就绪；Kimi/GLM 阻断只进入 comparison status | 改善对比模型结构化输出兼容和超时预算 |
| 7 | 至少 10 日回放和效果统计 | 工具完成、真实样本不足 | 严格 10 日评估器、点时/重复日/污染校验及 CI fixture 已完成 | 补齐 10 个独立真实交易日运行产物再出正式统计 |
| 8 | 非空计划、早盘、A4、模拟入离场 | TEST_ONLY 已完成 | 隔离模拟账户覆盖早盘、A4、入场、T+1、离场和幂等 | 等待自然非空 A3 计划做生产形态验收 |
| 9 | 脏实体增量更新和每周完整重建 | 已完成 | 脏实体队列、租约、增量重建、全量 generation 切换、失败保留旧代际均有测试 | 持续监控每周任务及数据源变化 |
| 10 | 代码拆分、CI、覆盖率、备份、文档 | 主要完成 | GitHub Actions、Python/Node 测试、构建、部署备份、方案和本报告已落地 | 关键模块 90% 覆盖率和恢复演练仍需继续 |

## 3. 自动化测试与持续集成

- Python：`564 passed`，整体覆盖率约 `75.56%`，高于当前 `65%` CI 门槛。
- Node：`31 passed`。
- TypeScript 类型检查、Python `compileall`、前端/服务端生产构建和 `git diff --check` 均通过。
- GitHub Actions Run `33251211293` 在提交 `eb442d57c01c7281661f30068fa2bb5b6df0b410` 上通过。
- 早期 CI 唯一失败来自回放 fixture 使用两个独立无序 glob；现已改为按同一 `run_id` 确定性配对并通过。

## 4. 部署、恢复点与特征代际

- 部署前备份：`/www/backup/liangjian-funnel-workflow/pre-a25311e-20260829T1928`。
- 虚拟机分支：`main`；工程运行代码验收基线：`eb442d57c01c7281661f30068fa2bb5b6df0b410`。其后的提交只收敛部署说明与本验收报告，不改变已验收的运行代码。
- Node 控制台：`127.0.0.1:3210`，健康接口返回 `ok`。
- 宝塔进程以 `www:www` 单实例运行，未连接外部交易。
- 工作区只有宝塔生成的未跟踪 `.htaccess`；未覆盖、删除或提交该文件。

全量 Feature Store 重建在性能优化前约 `42.36s`，预索引后约 `10.19s`。当前发布代际：

- generation：`feature-full-614bf4fb253344a89a8f6888`
- snapshot：`snapshot-20260828T210944+0800-2e278ff757d1`
- G0：`4017 / 4017`
- 业务暴露实体：`3083`
- taxonomy memberships：`62694`
- 无效 hash / payload：`0 / 0`

旧运行仍绑定其原始 generation `feature-777fe89a93c0542596b1141c`，历史结果不会被最新代际污染。物理保留的 `legacy-v1` 只用于审计，严格 API 不会读取。

## 5. 2026-08-28 全量 A1→A2→A3 验收

### 5.1 运行身份与资源

- run id：`2026-08-28-close-777fe89a93c0`
- snapshot：`snapshot-20260828T135331+0800-777fe89a93c0`
- 点时时间：2026-08-28 15:10（使用 13:53:31 冻结快照）
- 完整目录：5562 只；研究池：4098 只；可模拟交易池：4057 只
- 总耗时：2972 秒，约 49 分 32 秒；峰值内存约 1.1 GB
- 终态：`READY_DEGRADED / COMPLETED`

### 5.2 A1 基本面与月度研究

- 月度轮动决策 20；行业 40；结构性主题 12；行业链映射 17。
- 本地确定性筛选覆盖 4098；送模型复核 94 只，共 5 批。
- 最终 ACTIVE 104、MONITOR 3958、REJECT 36。

104 只不是重复计数：它由 94 只模型复核输入与 10 只本地确定性 MONITOR 补充合并而成，按代码去重。前端现可逐只查看代码、名称、分数、选择理由、风险、失效条件、证据引用和 lineage。

### 5.3 A2 主题情绪、龙头与资金确认

- 输入 104；本地角色层送模型 2；本地观察 98；本地拒绝 4。
- 最终焦点池 0；最终观察池 104。
- 状态 `VALIDATED_NO_OPPORTUNITY`；原因 `A2_NO_FOCUS_OPPORTUNITY`。

送模型的两个候选为：

1. `002172.SZ 澳洋健康`：主题分不足、资金流不可用、阶段退潮/回撤，进入观察池。
2. `002466.SZ 天齐锂业`：主题分不足、资金流不可用、阶段走弱，进入观察池。

二者 factor coverage 均为 `0.72`，缺少 `capital_flow` 和 `tier_structure`。因此本次零焦点结果必须理解为“在降级数据覆盖下未确认机会”，不能包装成“完整资金与梯队数据证明市场没有机会”。

### 5.4 A3 技术计划

A2 没有焦点候选，因此 A3 正确返回 `VALIDATED_NO_ACTION`、计划数 0、原因 `A3_NO_ACTION`。流程没有为了验收而放宽阈值或强制生成交易单。

### 5.5 多模型对比与发布隔离

- DeepSeek：主 lane 完整执行，发布就绪。
- Kimi：A1 共 3 次尝试，返回结构包含额外字段且未通过合同，最终总 deadline 超时。
- GLM：A1 共 7 次尝试，未得到有效合同输出，最终总 deadline 超时。
- Kimi/GLM 的 A2、A3 均标记为 `NOT_RUN_UPSTREAM_BLOCKED`，没有伪装成 0 机会。
- 必需 lane 1/1 就绪；记录 lane 3；comparison status `BLOCKED`；publication state `READY`。

这证明可选模型故障不会再阻断 DeepSeek 主模型结果发布。

## 6. 前端/API 验收与权限问题闭环

首次手工验收通过 transient systemd unit 以 `root` 启动。原子写入按安全设计生成 `0600` 文件，导致宝塔 Node 的 `www` 用户不能读取最新运行。表现为 Python 进度已完成，但 `/api/runs` 缺少最新 run、详情返回 404、前端仍显示旧结果。

本轮只对应用运行子树中由 root 产生的文件恢复 `www:www` 所有权：`outputs/runs`、`outputs/research`、`state/research_checkpoints`、`state/workflow_progress.json` 及 `storage/features` 中实际属于 root 的运行文件。没有修改 `.env`、`.git`、代码文件或服务器其他目录，也没有降低文件权限。

修复后验证：

- `/api/health`：`ok`
- `/api/runs?limit=5`：最新 run 为 `2026-08-28-close-777fe89a93c0`
- `/api/runs/2026-08-28-close-777fe89a93c0`：200，可读取完整结果和 lane
- `/api/overview`：最新运行正确、无阻断项、研究 Markdown 可读
- A1 approved 详情：104 只，可分页查看股票代码、名称、理由和风险
- A2 watch 详情：104 只，可分页查看股票代码、名称、理由和风险

`DEPLOYMENT.md` 已补充强制约束：手工回放、特征重建和验收 unit 必须使用与宝塔一致的 `www:www`，不得以放宽结果文件权限代替正确的进程身份。

## 7. 尚未完成的业务验收

以下项目不能用代码测试或单日运行伪装为完成：

1. **A2 全市场历史资金流与梯队覆盖。** 适配器和缺失语义已接入，但第三方历史全市场资金流仍不稳定或不可用。
2. **正式券商金股盲测数据。** 功能返回 `NOT_CONFIGURED` 是正确行为；需要合法、带发布日期的真实月度数据。
3. **10 个独立交易日点时回放。** 评估器和 CI 合同已通过，但当前只有一个可核验的真实点时日，不能制造其余九日。
4. **自然非空 A3 计划的生产形态闭环。** TEST_ONLY 状态机已验收；本次真实研究无 A2 焦点，不允许强行造单。
5. **Kimi/GLM 稳定对比。** 两个可选模型仍存在结构化输出兼容和总超时问题，但已从正式发布路径隔离。
6. **关键模块 90% 覆盖率。** 整体约 75.56% 已达当前门槛，关键状态、点时和模拟模块仍应继续补到计划目标。

## 8. 最终判定

| 维度 | 判定 | 说明 |
|---|---|---|
| 工程改造 | 通过 | 十项主体功能均已落地，未完成项有明确状态而非假完成 |
| 自动化测试 | 通过 | 本地与 GitHub Actions 全绿 |
| 部署健康 | 通过 | 指定提交已部署，Node/API 健康，结果可读 |
| 主模型 A1→A3 | 通过（降级） | 全流程终态可解释；A2 数据覆盖不足导致无焦点，A3 无动作 |
| 多模型对比 | 部分通过 | 隔离正确；Kimi/GLM 供应商输出仍失败 |
| 策略有效性 | 未完成 | 缺真实 10 日样本、金股基准与完整 A2 资金/梯队数据 |
| 模拟执行闭环 | 技术通过、自然样本未完成 | TEST_ONLY 已通过；真实非空计划仍待市场产生 |

项目目前适合继续以研究和模拟方式持续运行并积累点时样本，不适合宣称策略已达到生产投资有效性，更不得接入真实交易。下一优先级应是补齐 A2 真实数据覆盖和累计 10 个独立交易日，而不是扩大 LLM 调用量或人为放宽选股阈值。
